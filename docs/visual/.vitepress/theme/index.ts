import DefaultTheme from 'vitepress/theme'
import './custom.css'

// Default VitePress theme (clean, searchable chrome) + Atriom's warm sand/teal
// identity and the "picture" components (flow, lifecycle pills, T-account cards).
export default DefaultTheme

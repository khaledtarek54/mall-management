# Deep sweep — 2026-09-01

> **The evidence file for the sweep whose worklist is [ROADMAP §9](../ROADMAP.md#9--cycle--the-2026-09-01-deep-sweep-sw).**
> It is a record, not a second live list: when a row is picked up it moves to the ROADMAP or to
> [POST-STAGING-BACKLOG](POST-STAGING-BACKLOG.md), and when it is declined it gets a one-line row in
> [gap-analysis §6](../gap-analysis/README.md#6-declined--with-reasons-so-they-are-not-re-raised).
> Two lists of one launch is how a stale one survives.

## How this was produced, and how much to trust each row

Twenty-three finder agents read the code to break it — twelve over the money and GL seams, eleven
over the Filament panels, the tenant portal and the contractor portal — and every finding they
raised was then **adjudicated against the code by a second agent that was told to refute it**, with
a third pass on anything that survived. A finding is in this file only if it survived that.

**It is still a claim about code, and this project's own rule applies to it**: *a gap row is a claim
about code — check it against code.* Four rows were fixed the same day and each one was
independently re-verified before it was touched; two were refuted outright and are not here. Read
the row, open the file, and satisfy yourself before you act on it. Several rows will have been
overtaken by the concurrent CAM work.

**Four were fixed on the day and are NOT in the table below** — they were the three criticals and
one high, and each shipped with a mutation-proved regression test:

| What | Commit |
|---|---|
| The invoice status Select offered `written_off`, so live AR left every collection surface with no bad-debt entry, through a one-way door, using only `invoices.edit` | `9c970144` |
| Work-order **evidence was replacing, not appending** — on both doors — so a contractor's second photograph deleted their first, and the operator's attachment deleted theirs | `42e21d0b` |
| A scheduled report emailed **every mall's** rent roll to whatever addresses the schedule named, including the owner's external accountant | `a39a4c78` |
| Nothing anywhere wrote `is_portal_user`, so **no contractor could ever sign in** and the whole `/vendor` panel was inert | `14771766` |

**Status** — this file is the LIVE record and is updated as work lands, not a snapshot. `open` ·
`✅ fixed` (names the commit) · `⏳ in progress` · `↩︎ refuted on re-check` (it survived adjudication
and then did not survive contact — say why, and leave the row) · `⏭️ declined` (with the reason, and
a matching line in [gap-analysis §6](../gap-analysis/README.md#6-declined--with-reasons-so-they-are-not-re-raised)).
**Update the row in the same commit as the fix.** A status column nobody maintains is worse than
none, because it reads as current — **and so are the counts beside it**. Every subtotal below is
DERIVED from the rows by `python3 docs/qa/scripts/sweep-tally.py`; run it after changing a status
rather than editing a number. The first hand-typed set had already drifted by the third fix (the
header said 193 open over a table of 195, and the money section claimed 11 high where 7 were left),
which is the same failure this repo gates for generated doc blocks.

> ### Where this stands — 252 closed, 5 open (updated 2026-09-05)
>
> Plus the four fixed on the day of the sweep, listed above and not in the table.
>
> **What is left is not a smaller version of the list — it is the residue that resisted.** Four are
> DECISIONS rather than code (SW-236/237/238, and SW-237 is the one the other two wait on: *should
> re-pointing a posting role restate history?* — Yardi says a GL mapping change is prospective, and
> honouring that means freezing the resolved account onto the journal line, across all 24
> journalizers). Three are lock-order cycles that **sqlite cannot prove**, because it compiles
> `lockForUpdate()` to nothing — they need `tests/Mysql/` and two connections, and one was already
> found unsafe as designed. The other fourteen are ordinary and unblocked.
>
> **The adversarial review is what this sweep is really evidence for.** A second agent told to read
> the code and break the change found something real in substantially every fix it was pointed at,
> and several of those findings were that the FIX was worse than the defect: a deposit split that
> would have restated closed-period entries on the next sweep; a legacy guard that made its own fix
> inert on any lease that had ever carried a late fee; an arrears restriction that lost 66,666.67 of
> rent; a whole-table lock on `units`; two comments of mine that were simply false. Green tests said
> nothing about any of them, because each was a case the fixture did not contain. Mutation testing
> caught two more the review did not. **Neither is optional, and neither substitutes for the other.**

**Severity** — `critical` wrong money, lost data, or something confidential leaving the building ·
`high` an operator blocked, or data quietly wrong · `medium` real friction · `low` polish.
**Fix** is the adjudicating agent's own estimate: XS under an hour, S a morning, M a day, L longer.

---

### Money · AR · settlement

*3 open — 3 medium.*

| ID | Status | Sev | Fix | What is wrong | Where |
|---|---|---|---|---|---|
| **SW-001** | ✅ **fixed** `2b75278e` | high | S | Credit-note status Select lets 'void' be picked directly, bypassing the void service's applied-amount refusal. Broader than reported: `applied` is DERIVED (picking it makes the tenant's credit invisible to every picker narrowing on `hasBalance()`) and `issued` skipped `PostingDate::assertOpen`, so AR committed while the journal post was silently refused. **The first cut was worse than the bug** — removing the options made an applied or voided note unsaveable on every field (`Rule::in([])`), and the only remaining option was `issued`, so the sole way past the error was to un-void the note. Narrowed on draft, DISABLED after; void is terminal | `Filament/Admin/Resources/CreditNotes/Schemas/CreditNoteForm:144` |
| **SW-002** | ✅ **fixed** `2733f9af` | high | S | A deposit refund/forfeit larger than what the lease holds is accepted by the DepositTransactions create form — negative deposit liability *(reported by 2 independent agents)*. The FIFTH door onto the pot, and the only one with no cap at all: same `deposit_transactions.create`/`.edit` permissions as the lease action, `minValue(0.01)` and nothing else, and freely editable afterwards because the receipt freeze fires only for `type === 'receipt'`. Capped on the MODEL, measured against the pot less the row's own persisted contribution | `Models/DepositTransaction:300` |
| **SW-003** | ✅ **fixed** `cb59bd76` | high | S | CreatePayment's orphan rollback throws on the form's own default status — a captured, allocation-less receipt survives behind the wrong error *(reported by 2 independent agents)*. Measured: `delete()` on a captured payment is refused, on an `initiated` one succeeds, and this form DEFAULTS to captured. Fixed with Filament's OWN transaction (`$hasDatabaseTransactions`, inert until now because no panel opts in) rather than a hand-rolled wrapper — the first cut swallowed the refusal to keep the operator's form and left `$this->record` on a rolled-back row, so the next keystroke 404'd. The receipt e-mail moved to `DB::afterCommit()`: it is not `ShouldQueue`, so inside the transaction it held the invoice's row locks across an SMTP round-trip | `Filament/Admin/Resources/Payments/Pages/CreatePayment:193` |
| **SW-003b** | ✅ **fixed** `f985a07f` | medium | S | *(found by the review of SW-003.)* `PaymentForm` capped each allocation ROW independently while `CreatePayment::afterCreate()` SUMS rows against the same invoice, so 700 + 600 on a 1,000 invoice passed every form gate and was refused only at the model — after the operator pressed Create, with the whole receipt rejected. The row is now capped against what its SIBLINGS already claim, so the refusal lands on the field. Duplicate rows stay supported input (400 + 600 on one invoice still saves) | `Filament/Admin/Resources/Payments/Schemas/PaymentForm:241` |
| **SW-003c** | ✅ **fixed** `0600d612` | high | S | *(found by the review of SW-003.)* `PaymentForm`'s invoice picker chains `->getOptionLabelUsing(fn ($value) => Invoice::find($value))` — a bare UNSCOPED find that overrides `EntitySelect`'s scoped resolver, so *"the label lookup IS the write guard"* does not hold on this field and a crafted payload can submit another tenant's same-property invoice | `Filament/Admin/Resources/Payments/Schemas/PaymentForm:197` |
| **SW-003d** | ✅ **fixed** `eb364100` | medium | M | *(found by the review of SW-003.)* **No panel opts into Filament's database transactions**, so `beginDatabaseTransaction()`/`rollBack` are no-ops app-wide and EVERY Create/Edit page that throws in `afterCreate`/`afterSave` commits its record. **Four money pages opted in** (CreateLease, CreatePayment, EditPayment, EditLease) rather than the whole panel: 21 pages carry these hooks, 3 notify a person, and a panel-wide flip would put every synchronous e-mail inside a transaction — the exact regression SW-003 hit. **`halt()` COMMITS by default**, so enabling the transaction alone is silently useless: all seven halts on those pages now pass `shouldRollbackDatabaseTransaction: true`. `EditLease` was the LIVE case — its `afterSave()` throws after the record is written, so the term, escalation and deposit-months edits committed while `syncUnits()` never ran and the operator was told it was refused | `Providers/Filament/AdminPanelProvider` |
| **SW-004** | ✅ **fixed** `0534e193` | high | M | EditPayment re-allocation re-spends a surplus already drawn down as tenant credit — the same money settles two invoices. The total-vs-amount cap asks *does this add up to the receipt* and cannot see that part of it has already left: a receipt's UNALLOCATED remainder is what funds `Tenant::creditBalance()`. Nothing links an application back to the receipt that funded it (the credit is a POOL), so `Payment::assertCreditNotOverdrawn()` asks whether the pool is still solvent — scoped by property exactly as `VoidPaymentService`'s sibling guard, or another mall's surplus stands in for this one | `Filament/Admin/Resources/Payments/Pages/EditPayment:148` |
| **SW-005** | ✅ **fixed** `ae18fe62` | high | — | Portal invoice View page offers a live Paymob checkout on a WRITTEN-OFF invoice — same file's demo button gets it right. `canPayNow()` tested no status AT ALL while `canPayDemo()` three lines below carried the denylist, so the button that spends real money was the permissive one. Both ask `isPayable()` now | `Filament/Portal/Resources/Invoices/Pages/ViewInvoice:97` |
| **SW-006** | ✅ **fixed** `ae18fe62` | high | — | A DRAFT invoice is publicly readable and publicly payable through `/pay/{token}` — the one invoice surface with no draft filter. **Reproduced over the real route: 200, naming the tenant and the amount, to an unauthenticated visitor.** `isPayable()` was a fourth hand-rolled status denylist beside `InvoiceSettlement`, which had refused `draft` from the day it was written | `Models/Concerns/Invoice/HasPaymentLink:86` |
| **SW-006b** | ✅ **fixed** `ae18fe62` | high | S | *(found while fixing SW-006 — not in the sweep.)* Every tenant payment path charged the RAW `balance`, which a write-off deliberately leaves standing. A 10,000 invoice with 6,000 forgiven asked the tenant for 10,000 on the public page, in the Paymob session, in the pivot allocation and in the demo capture — collecting it drives AR negative for that debt and leaves bad-debt expense standing against cash that arrived. `InvoiceSettlement::settleableAmount()` already capped SEVEN operator-driven call sites and **none of the tenant-driven ones**; `Invoice::payableAmount()` is now the one amount. **The review caught a worse defect the first cut introduced**: moving our RECORD to the netted figure while `PaymobClient` still charged the raw balance would have left the difference as real cardholder money with no row in Atriom at all | `Services/Paymob/PaymobPaymentInitiator:114` |
| **SW-007** | ✅ **fixed** `fe04a0f9` | high | — | A unit-owner assessment can never be charged a late fee — the sweep fatals on a null lease every night *(reported by 3 independent agents)* | `Services/LateFeeService:203` |
| **SW-008** | ✅ **fixed** `67212524` | high | M | PDC clear() settles a written-off invoice — AR relieved twice, bad debt stands, cash double-counted | `Services/PostDatedChequeService:73` |
| **SW-009** | ✅ **fixed** `2733f9af` | high | M | Two concurrent move-out settlements double-disburse the deposit: depositHeld() is a plain read inside one outer transaction, so the second settle spends and refunds a pot the first already emptied *(reported by 3 independent agents)* — the settle took **no lock at all**, and no UNIQUE index can turn the race into a duplicate key — `deposit_transactions.number` is unique, but the two writers get different numbers, so nothing constrains the POT. `Lease::depositHeldForUpdate()` is the locking twin; the LEASE is the contended row because the pot spans three tables, so `ApplyDepositToInvoiceService` locking the invoice was never a guard for it. Order: leases → invoices → deposit_transactions → deposit_applications | `Services/SettleMoveOutService:81` |
| **SW-009b** | ✅ **fixed** `2733f9af` | high | S | *(found while fixing SW-009 — not in the sweep.)* The same pot has FOUR doors and the sweep only saw the one that locked nothing. `BillSecurityDepositService` locked the lease and then read `depositUnbilledShortfall()` — a PLAIN read — beneath a comment asserting the lock made it a check-then-act guard. Two operators each read the same unbilled shortfall and each raise an invoice: the tenant is asked for **twice the security they agreed**, and `deposits_held` is credited twice on payment. The *Record deposit movement* action capped from the display twin **outside any transaction** | `Services/BillSecurityDepositService:56`, `Filament/Admin/Actions/LeaseActions:246` |
| **SW-009c** | open | medium | M | **Lock-order cycle `leases` ⇄ `units`, via an OBSERVER.** Three services take units → leases (`LeaseCreationService:45→50`, `LeaseRenewalService:60→62`, `ConvertLeaseToHoldoverService:108→110`); six paths take leases → units the other way, because `LeaseObserver::updated` → `Unit::recomputeStatus()` issues an implicit X lock on `units` (`ExpireLeasesCommand:118`, `LeaseTerminationService:74`, `LeaseExtensionService:69`, `LeaseRentChangeService:100`, `RentEscalationService:179`, `SettleMoveOutService`). A deadlock is detected and rolled back, so the symptom is an intermittent 500 rather than wrong money — but `ConcurrencyPolicy` cannot see the second edge at all, because it registers EXPLICIT locks and this one is an observer-driven UPDATE | `Observers/LeaseObserver:72` |
| **SW-009d** | open | medium | M | **Lock-order cycle `credit_notes` ⇄ `invoices` ⇄ `credit_note_applications` — three different orders in ONE file.** `applyToInvoice:55→56` is notes→invoices; `reverseAppliedCredit:197→198` runs from `Invoice::updated` while the caller holds an invoices lock, i.e. invoices→applications→notes; `reverseAllApplications:235→241→242` is notes→applications→invoices; `reverseApplication:282→289→295` is applications→invoices→notes. A void racing an apply on the same note/invoice pair deadlocks | `Services/CreditNoteService:51` |
| **SW-009e** | open | medium | S | **Lock-order cycle `payments` ⇄ `invoices`.** The gateway callback locks the payment then its invoices (`CallbackController:157` → `Payment::refitAllocationsToBalance:396`), and so does `VoidPaymentService:33`; the receipt/allocation paths lock the invoice then its payments (`Payment::assertInvoicesNotOverAllocated:298→326`). A capture landing while an operator edits an allocation on the same invoice deadlocks. Also `PostDatedChequeService::settleOpenInvoices:135` takes a RANGE lock over a tenant's open invoices — the widest in the repo, and the likeliest victim | `Models/Payment:291` |
| **SW-010** | ✅ **fixed** `2b4dd73c` | high | XS | VoidPaymentService's spent-surplus guard falls back to the GLOBAL credit balance for a zero-allocation receipt — credit at another mall masks that this receipt's surplus was already spent. A no-allocation receipt is the ORDINARY case, not an exotic one: a cleared SERIES cheque names no invoice, which is the Egyptian norm. `Payment::originatingAssetId()` already held its property — the same fact `Tenant::creditBalance()` reaches for when it attributes that credit | `Services/VoidPaymentService:59` |
| **SW-011** | ✅ **fixed** `77f088a4` | high | M | A PARTIAL write-off is invisible to every collection surface — the tenant is asked for, and can pay, the forgiven part; AR goes negative and even billing:reconcile agrees with the wrong books | `Services/WriteOffInvoiceService:129` |
| **SW-012** | ✅ **fixed** `b31ee637` | medium | S | Tenant hub's Payments tab scopes through invoices.lease.unit — unit-owner assessment payments vanish for property-restricted operators | `Filament/Admin/RelationManagers/TenantPaymentsRelationManager:35` | **Designed by the 2026-09-03 patch fleet, adversarially reviewed, then applied, tested and mutation-checked here.** The tenant hub's Payments tab scopes receipts through `invoices.lease.unit`, a chain from before unit owners existed — an owner's صيانة assessment invoice carries `lease_id` NULL by construction, so every receipt settling one is invisible on that tenant's own Payments tab for any property-restricted operator, while the Payments register beside it lists the same receipt. `app/Filament/Admin/RelationManagers/TenantPaymentsRelationManager.php:35` — `whereHas('invoices.lease.unit', fn ($u) => $u->whereIn('asset_id', $ids))`. Measured on the `mall_management_qa` database 2026-09-03 (`select count(*) from invoices where lease_id is null` → **42**, and `select count(distinct p.id) … join invoices i … where i.lease_id is null` → 0 because no owner has paid there yet): all 42 unit-owner assessments carry a NULL `lease_id`, so that chain matches NONE of them. The correct predicate already exists t |
| **SW-013** | ✅ **fixed** `67212524` | medium | S | Payment allocation picker and auto-suggest offer DRAFT invoices; allocating flips a never-issued draft straight to paid | `Filament/Admin/Resources/Payments/Schemas/PaymentForm:178` |
| **SW-014** | ✅ **fixed** `962090fe` | medium | M | Payment/PDC invoice pickers override getOptionLabelUsing with an unscoped Invoice::find, deleting the validation write-guard the picker's narrowing relies on | `Filament/Admin/Resources/Payments/Schemas/PaymentForm:197` **Fixed 2026-09-05 (`962090fe`) — half was already closed.** The payment form's picker had been scoped in an earlier commit, with a comment carrying this row's own reasoning; the PDC form's `getOptionLabelUsing(fn ($value) => Invoice::find($value)?->number)` was the survivor. Filament validates a Select by asking it to LABEL the submitted value and refuses what it cannot — so the label resolver IS the write guard, and a bare `find()` deletes it: the OPTIONS were scoped, but a crafted Livewire payload naming another mall's invoice was labelled cleanly and accepted, linking a cheque for Mall A to Mall B's invoice — the cross-property AR/GL leak the query beside it names in writing. Scoped by PROPERTY only, never status or balance, because a cleared cheque's invoice is legitimately absent from the options and must still label on the edit page — the reason the resolver exists. 3 cases, mutation-proved |
| **SW-015** | ✅ **fixed** `71227984` | medium | — | Portal invoice View shows a blank unit on every owner assessment — the fix landed in the table beside it and not in the infolist | `Filament/Portal/Resources/Invoices/Schemas/InvoiceInfolist:22` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** An owner's صيانة assessment prints a blank unit on five surfaces — the portal View page the owner himself opens, the top-bar search result, the AR-ageing worklist, that report's CSV and the invoice export — because each still walks `lease.unit.code` and an assessment carries no lease. Row confirmed and WIDER than reported. `Invoice::unitCode()` (app/Models/Invoice.php:237) has answered this since 61bb1dc6 (2026-08-18), whose message says "two surfaces ask it" — and only the two invoice TABLES were routed. Grepping every invoice reader for `lease.unit.code`/`lease?->unit?->code` at HEAD finds five that were not: app/Filament/Portal/Resources/Invoices/Schemas/InvoiceInfolist.php:22 (the row as reported), app/Filament/Admin/Resources/Invoices/ |
| **SW-016** | ✅ **fixed** `741f879d` | medium | — | A portal filter labelled "Overdue Only" returns every unpaid invoice, and the dashboard's headline stat deep-links into it | `Filament/Portal/Resources/Invoices/Tables/InvoicesTable:138` **Fixed 2026-09-05 (`741f879d`), and it was THREE answers to one question.** The filter's key said `unpaid_only`, its query was unpaid, and the label — the only part a tenant reads — said *Overdue Only*: on the QA baseline, **108 rows under a word describing 11**. The dashboard's outstanding stat deep-linked into it, while its own overdue count read the raw `status='overdue'` STAMP (which the nightly sweep lags a day behind and `partially_paid` can never carry) and said **4** — beside a due-date column colouring all 11 red. `Invoice::scopeOverdue()` is now the ONE definition of past-due-and-still-owed; the pair had been hand-written SIX times under comments asking each other to stay identical. Routed through the admin filter, sidebar badge, dashboard card, `Tenant::isDelinquent()`, `TenantBalances`, and TWO portal filters (unpaid and overdue, because the dashboard shows two figures and they are not the same set). `/api/v1/me/balance` already computed it correctly — the portal was brought to the API, not the other way round, and their agreement is asserted. 5 cases, 4 mutations |
| **SW-017** | ✅ **fixed** `db392343` | medium | S | Receipt-in-use freeze checks the NEW type, so flipping a drawn-on receipt's type to forfeit/refund bypasses it | `Models/DepositTransaction:259` |
| **SW-018** | ✅ **fixed** `bb70830a` | medium | M | A partially-written-off invoice is collectable at its FULL pre-write-off balance on every payment path, and the AR tie-out stays green over the resulting negative AR | `Models/Payment:318` | **Confirmed by the 2026-09-03 verification sweep and fixed the same day.** `ApplyDepositToInvoiceService::apply()` capped the draw at `(float) $locked->balance` — the amount BILLED. A write-off is not a settlement channel, so it deliberately leaves `balance` standing, and the forgiven slice of a partially written-off debt therefore came out of the departing tenant's security deposit: measured, 6,000 of a 10,000 invoice forgiven and the full 10,000 still netted off the refund. **It over-charges in CASH at the one moment there is no recovery path** — the tenant has gone — and it did so while the move-out statement beside it, which reads `collectableBalance()`, showed the operator and the tenant a smaller number. Two documents, one settlement, and the one that moved the money was the wrong one. `collectableBalanceForUpdate()` is the LOCKING twin, which matters as much as the netting: a plain read there is answered from the snapshot taken before it waited. `settleOpenAr()`'s selection moved to `stillOwed()` in the same change. `ADepositNeverPaysForgivenDebtTest`, mutation-proved |
| **SW-019** | ✅ **fixed** `67212524` | medium | S | A written-off invoice passes every server-side money gate for receiving cash — only option lists exclude it — so a stale repeater row or a gateway capture relieves AR twice for the same debt | `Models/Payment:371` |
| **SW-020** | ✅ **fixed** `8f98e1bf` | medium | S | Voiding a cheque-clearing payment keyed in error marks the physical cheque BOUNCED — a bank return that never happened, and an NSF fee becomes billable on it | `Models/Payment:444` |
| **SW-021** | ✅ **fixed** `67212524` | medium | S | Tenant credit can be applied to a written-off (or draft) invoice — the one settlement channel missing the status guard its two siblings have | `Services/ApplyTenantCreditService:34` |
| **SW-022** | ✅ **fixed** `77f088a4` | medium | S | Late fees and dunning are charged on the forgiven slice of a partially-written-off debt | `Services/LateFeeService:158` |
| **SW-023** | ✅ **fixed** `52eb2388` | medium | S | Voiding a partially-written-off invoice leaves the bad-debt entry standing — AR negative and the loss double-counted in P&L | `Services/VoidInvoiceService:52` |
| **SW-024** | ✅ **fixed** `76b93d5c` | low | XS | Deposit register cannot be searched or filtered by tenant | `Filament/Admin/Resources/DepositTransactions/Tables/DepositTransactionsTable:42` **Fixed 2026-09-05 (`76b93d5c`).** The only searchable column was the deposit NUMBER, which nobody remembers, so *"what is held for Cilantro"* meant scrolling. Searches the tenant's own FOLDED blob now — never a raw `tenant.name` path, which compares a folded query against an unfolded value and silently misses exactly the Arabic spellings the fold exists to reconcile while still working for the plain-ASCII spot check. A tenant `EntitySelectFilter` and a `transaction_date` range came with it. 5 cases, 4 mutations |
| **SW-025** | ✅ **fixed** `71227984` | low | — | A payment rail with both directions off saves as an active row that no screen will ever offer | `Filament/Admin/Resources/PaymentMethods/Schemas/PaymentMethodForm:56` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** Un-ticking both direction toggles saves an ACTIVE payment rail that no picker will ever offer and no document can ever name — inert configuration that reads on the register as switched on. Row confirmed. `app/Filament/Admin/Resources/PaymentMethods/Schemas/PaymentMethodForm.php:56-64` has `Toggle::make('for_inbound')` and `Toggle::make('for_outbound')`, each `->default(true)` and neither carrying any rule; `app/Models/PaymentMethod.php:127-130` is the whole of `booted()` and only coerces `settlement_days`. Traced the consequence in code: `inboundCodes()`/`outboundCodes()` (lines 277-286) each filter on their own flag, `optionsFor()` (line 305) picks one of them per column from `ValueSets::catalogueWidenedColumns()`, and `ValueSets` widens a |
| **SW-026** | ✅ **fixed** `b31ee637` | low | S | Payment gateway/cheque fields have no maxLength against their string(255) columns | `Filament/Admin/Resources/Payments/Schemas/PaymentForm:305` | **Designed by the 2026-09-03 patch fleet, adversarially reviewed, then applied, tested and mutation-checked here.** The payment form's three free-text gateway/cheque fields have no `maxLength` against their `varchar(255)` columns, so pasting a long bank reference is a `SQLSTATE[22001] Data too long` 500 on Create that loses everything the operator typed — and the test suite structurally cannot see it, because SQLite does not enforce a varchar width. `app/Filament/Admin/Resources/Payments/Schemas/PaymentForm.php:378-383` — `TextInput::make('gateway')`, `('gateway_transaction_id')` and `('cheque_number')`, each with a `->label()` and nothing else; grepping every `TextInput::make(` in that file shows these are the only three unbounded string inputs on the form (`reference` is disabled+non-dehydrated, `amount` and `allocated_amount` are numeric). Column widths read off `mall_management_qa` 2026-09-03 via `show columns from payments`: `gateway varchar(255)`, `gateway_transaction_id varchar(255)`, `cheque_n |
| **SW-027** | ✅ **fixed** `c00e0143` | low | S | Payments and Invoices status filters offer a hand-kept subset — 'voided' (the new reversal status), bounced, settled, disputed, written_off are unfilterable | `Filament/Admin/Resources/Payments/Tables/PaymentsTable:96` **Fixed 2026-09-05.** Confirmed at HEAD and closed for all THREE registers, not the two the row named. Measured against `ValueSets::allowed()`: invoices offered **5 of 9**, payments **4 of 9**, leases **6 of 7** — and every missing value has its own coloured arm in the `status` column a few lines above its filter, so an operator could read the word and not select it. `voided` is the sharpest: it shipped 2026-08-28 to say money was NOT returned, and `ListPayments::getTabs()` does not carry it either, so nothing on the register could name it. `cancelled`/`credited`/`written_off` are likewise in no invoice tab. **The lease case is the weakest and is stated as such** — the adversarial review corrected my first wording, which claimed a cancelled lease could be *created and never found*: `ListLeases::getTabs()` carries `cancelled` in the **Ended** tab, so it was reachable through the tab and not through the filter. The `->except('cancelled')` arrived in `bcca5b17` (May 2026) with no comment and no reason in the message — incidental, not a decision. All three are now one line through `StatusOptions::for()`, the seam SW-029 built and no admin screen read. A TAB set is a curated worklist and legitimately not exhaustive; the FILTER is the exhaustive tool, so the tabs are untouched. Lang parity probed under `Lang::has(…, 'ar', false)` for all 25 values; swept all 44 status filters, no `->only()`/`->except()` survivor. `ARegisterFilterFindsEveryStatusItHoldsTest`, 6 cases, mutation-proved |
| **SW-028** | ✅ **fixed** `c00e0143` | low | S | PDC lodge-series preview renders English month names inside an Arabic sentence | `Filament/Admin/Resources/PostDatedCheques/Pages/ListPostDatedCheques:101` **Fixed 2026-09-05, and the fix was three times bigger than the row.** `format('M Y')` emits English month names whatever the locale is; measured for 2026-10-01, `->locale('ar')->isoFormat('MMM YYYY')` gives the Arabic month with the year in Latin digits (what `LatinNumeralsTest` pins app-wide) and the English output is byte-identical, so the English panel cannot move. **The adversarial review caught that the first pass fixed 1 of 4 panel-facing sites** — the exact enumerate-from-your-own-diff defect this file names as the most repeated one — and the gate then added found **two more my manual enumeration had also missed**. The highest-leverage was never the cheque preview: `OptionDisplay::dateRange()` is ONE seam behind a lease's commencement→expiry, a vendor contract's start→end and an announcement's window, i.e. the second line of every such `EntitySelect` in BOTH panels, and `dayDate()` covers an invoice's due date and a vendor bill's date on the allocation and cheque-linking pickers. Also `ListOwnerStatementRuns` (the period picker on the modal that GENERATES an owner statement — the same commit-an-act shape) and `RentIndex::label()`. Service-level `format('F Y')` calls are deliberately untouched: they write `invoice_items.description`, stored English prose whose reason is written out in `MonthlyBillingService` — localising it would freeze the billing RUN's locale into the row. `ALodgeSeriesPreviewNamesItsMonthsInTheReadersLanguageTest`, 7 cases including a from-disk sweep that asserts its own premise, mutation-proved |
| **SW-029** | ✅ **fixed** `71227984` | low | — | Portal invoice and payment status filters are hand-written `->only()` lists that cannot find four visible statuses each | `Filament/Portal/Resources/Invoices/Tables/InvoicesTable:97` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** The tenant portal's invoice and payment status filters were hand-written `->only()` lists that could not name four statuses each — statuses the very column beside them renders in colour — so a retailer could read "Written off" or "Voided" on their own list and had no way to filter to it. Read at HEAD e3154f27 (2026-09-04) and exercised in tinker. `app/Filament/Portal/Resources/Invoices/Tables/InvoicesTable.php:102` offers `['issued','partially_paid','paid','overdue']` = 4, while `TenantVisibility::visibleFor('invoices')` returns 8 (`ValueSets::allowed('invoices','status')` is 9, minus the hidden `draft`): `disputed`, `cancelled`, `credited`, `written_off` are unreachable — and every one of them is rendered by the `status` column's own `form |
| **SW-030** | ✅ **fixed** `71227984` | low | S | No positive-fee guard: a 0% / 0-minimum configuration mints EGP 0.00 late-fee invoices (with tenant notifications), and each zero fee permanently blocks a real fee | `Services/LateFeeService:167` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** With a 0% rate and a 0 minimum — the only way an operator can say "this lease carries no late-fee clause" — the nightly sweep minted an EGP 0.00 invoice against every overdue debt, notified the tenant of a penalty of nothing, and stamped that zero fee onto the invoice so a REAL fee could never be charged on it afterwards. Verified at HEAD 79ede334. `app/Services/LateFeeService.php:188` computes `$fee = max($min, round($chargeable * $percent / 100, 2))`; probed 2026-09-04 in a booted app: with `$percent = 0`, `$min = 0` and a 12,000 chargeable balance the fee is **0.00**, `App\Support\Vat::rateForType('late_fee')` is **0** so the VAT and the total are 0.00 too, and `IssueInvoiceService::issue()` refuses only `$items === []` (line 70) — never |
| **SW-031** | ✅ **fixed** `c82e4d20` | medium | S | Disputed invoices are invisible to the final account — refund paid out while contested AR stands, with nothing on the statement | `Services/MoveOutStatementService:59` | **The row's premise was wrong and the review measured it.** `SettleMoveOutService` nets arrears off the deposit through `ApplyDepositToInvoiceService`, whose scope is `->acceptingSettlement()` — the `InvoiceSettlement` register, in which `disputed` is classified LIVE — so the deposit was NEVER refunded in full: measured at `d1a4ee0e^`, 490,000 of 540,000. The real defect is the opposite and worse-hidden: the STATEMENT understated the deduction by 50,000, so the forecast disagreed with its own Settle button, on figures FROZEN onto an immutable lease event. **And my first fix was worse than the bug** — it printed *"under dispute (claimed, not deducted)"* beside an amount the next button deducts, on a document the tenant signs. The statement now reads `->acceptingSettlement()`, the same register, so the two cannot disagree; a test asserts the forecast equals the refund actually written. **What is ARGUED about is a separate figure, from the ITEM flag, beside the total** — the position MF-07 shipped for AR aging in 2026-08-09 (*"the disputed figure sits BESIDE the aged one … deducting it would understate what the mall is owed"*), and reading `invoices.status` instead labelled a whole 50,000 document when only its 20,000 line was contested. `AFinalAccountNamesEveryPotTest` |
| **SW-032** | ✅ **fixed** `c82e4d20` | low | S | The final account omits on-account tenant credit — a document that promises 'everything owed in both directions' misses one pot | `Services/MoveOutStatementService:69` | `Tenant::creditBalance()`, scoped to the LEASE's property (`$lease->unit?->asset_id` — a lease carries no `asset_id`), **STATED and never netted**. Netting it was the second way this change was worse than the bug: `settle()` does not call `ApplyTenantCreditService`, and these figures are frozen onto an immutable lease event — measured with a 100,000 deposit, 250,000 of arrears and 60,000 on account, the signed document said the departing tenant owed **90,000** while the ledger said **150,000**. Understating a debt on the document that closes a tenancy is the worse direction. A receipt naming no property at all (`payments` has no `asset_id` column, so an unallocated bank transfer has neither an allocation nor a cheque to take one from) is honestly 0 here and still visible in the tenant's whole-company balance. **The modal body was extracted to `LeaseActions::finalAccountSummary()` so it could be tested at all** — a `Placeholder`'s `content()` closure is evaluated nowhere a test can reach, and a mutation deleting both new rows left the suite fully green. There is no final-account PDF, so that modal is the entire operator-facing surface |

### Billing · leases

*0 open.*

| ID | Status | Sev | Fix | What is wrong | Where |
|---|---|---|---|---|---|
| **SW-033** | ✅ **fixed** `e6d9f699` | high | M | Nightly leases:expire strands every unconverted holdover: no offered path to convert, renew, or terminate an 'expired' lease | `Filament/Admin/Actions/LeaseActions:795` |
| **SW-034** | ✅ **fixed** `541315ce` | high | M | 'End charge' / 'End assessment' with a future stop date silently stops billing immediately — the intervening months are never invoiced | `Filament/Admin/RelationManagers/ChargeScheduleRelationManager:508` |
| **SW-035** | ✅ **fixed** `e6d9f699` | high | — | `leases:expire` empties the holdover queue every morning, making the whole LE-04 holdover conversion permanently unreachable | `Console/Commands/ExpireLeasesCommand:84` |
| **SW-036** | ✅ **fixed** `e6d9f699` | high | M | leases:expire makes holdover conversion unreachable — the entire LE-04 workflow is dead after the first night | `Console/Commands/ExpireLeasesCommand:87` |
| **SW-037** | ✅ **fixed** `aa624ab1` | high | — | Clicking a report filter's clear (×) 500s the page — the bound property is non-nullable and Livewire unsets it Thirteen screens had it, not two — every financial statement through the shared ledger bar, plus the ageing bucket, month-end close, the reports index, tax depreciation, the VAT and withholding returns and the revenue forecast. `KeepsFilterAnswered` restores a filter whose blank is not an answer; `selectablePlaceholder(false)` stops it being offered. `AClearableFilterNeverBreaksItsPageTest` DRIVES every filter-bearing admin page, because the control and the property live in different files and no source sweep can pair them. | `Filament/Admin/Pages/BillingRunPreview:62` |
| **SW-038** | ✅ **fixed** `5e159676` | high | — | Portal lease list shows DRAFT and PENDING_APPROVAL leases — the tenant reads terms nobody has approved | `Filament/Portal/Resources/Leases/LeaseResource:100` |
| **SW-039** | ✅ **fixed** `c7838487` | high | S | Writing off a partially-paid billed deposit erases the PAID portion from depositHeld — tenant's deposit silently kept at move-out | `Models/Lease:674` |
| **SW-040** | ✅ **fixed** `6713433c` | high | — | An assessment invoice records the calendar month it did not bill, so a resale credits the seller too little and the mall over-collects | `Services/BillUnitOwnershipsService:268` |
| **SW-041** | ✅ **fixed** `e37f23f9` | high | M | Re-running Terminate on a lease under notice stacks duplicate unearned credit notes — no idempotency on the MF-02 credit `CreditUnearnedBillingService` now refuses when a live credit note already stands against the invoice — keyed on the INVOICE, not the termination date, since re-terminating with a different date is the same mistake wearing a different figure. A VOIDED note deliberately does not block: voiding one is how an operator says *do that again properly*. | `Services/LeaseTerminationService:176` |
| **SW-042** | ✅ **fixed** `b31ee637` | medium | XS | Quick-new-lease wizard prefills hard-coded 7-day payment terms and 36-month term, bypassing the property/portfolio conventions the main form was already fixed to honour | `Filament/Admin/Resources/Leases/Tables/LeasesTable:393` | **Designed by the 2026-09-03 patch fleet, adversarially reviewed, then applied, tested and mutation-checked here.** The quick-new-lease wizard prefills a hard-coded 7-day payment term and 36-month term, so a mall that has configured either convention gets neither — and because the wizard's fields always SEND a value, LeaseCreationService's own fallback to the configured default can never fire on this path. At HEAD, `app/Filament/Admin/Resources/Leases/Tables/LeasesTable.php:388-395` is a STATIC `->fillForm([...])` array containing `'term_months' => 36` and `'payment_terms_days' => 7`. Both have configured homes the MAIN form already reads: `LeaseForm.php:350` → `max(1, (int) app(AccountingSettings::class)->default_lease_term_months)` and `LeaseForm.php:699` → `PropertySettings::paymentTermsDays(TenantScope::currentAssetId())` (property → portfolio, registered in `PropertySettings::OVERRIDABLE`). `LeaseCreationService.php:90` has `?? PropertySettings::paymentTermsDays($unit->asset_id)`, but the wizard' |
| **SW-043** | ✅ **fixed** `71227984` | medium | S | Re-entering an existing index month on the rent-index form hits the (code, period) DB unique with a raw 500 — no unique rule, no pointer to the edit path | `Filament/Admin/Resources/RentIndices/Schemas/RentIndexForm:42` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** Re-entering a month that already has a reading for an index — which is exactly what an operator does when a statistical agency revises a figure — hits the (code, period) database unique and renders as an untranslated 500, with nothing telling them that a revision is an EDIT to the reading that already exists. Read at HEAD, not run (repo frozen; the new test is what measures it). `database/migrations/2026_08_19_200000_create_rent_indices.php:56` declares `$table->unique(['code','period'])` and its own docblock (line 30-35) states the intent: "A revision — statistical agencies do revise — is an EDIT to that row, not a second row, because a lease that escalated on the old figure must be able to show which figure it used and when it changed." ` |
| **SW-044** | ✅ **fixed** `76b93d5c` | medium | S | Rentable-items register's holder column reads the unfiltered leases relation: shows former/terminated holders, never shows unit-owner holders | `Filament/Admin/Resources/RentableItems/Tables/RentableItemsTable:54` **Fixed 2026-09-05 (`76b93d5c`).** The holder column read `leases.tenant.name` — the whole morph HISTORY, unfiltered by lease status or by whether the holding is still open — so it named a tenant who gave the bay back last year, and showed NOTHING for a bay held by a UNIT OWNER (whose holding is an `ownerships` row). `RentableItem::currentHolderLabel()` already answered correctly, and its own docblock says it exists so the map and the register cannot disagree; the register was not asking. **The third instance in one day of a model answering a question correctly while a screen asks differently** (SW-076's dropped column, SW-165's missing relation). The table now eager-loads what that method's stated contract requires, or every row costs two queries. Mutation-proved |
| **SW-045** | ✅ **fixed** `b31ee637` | medium | — | An imported quarterly/annual owner assessment is silently never billed and counted as an ordinary `skipped` | `Filament/Imports/ChargeImporter:94` | **Designed by the 2026-09-03 patch fleet, adversarially reviewed, then applied, tested and mutation-checked here.** The charge importer accepts a quarterly or annual assessment frequency that the run which bills unit owners has never understood, so the owner's صيانة is silently never invoiced and the ownership is counted in the ordinary `skipped` bucket every month. Read, not run (repo frozen; the new test is what measures it). `app/Filament/Imports/ChargeImporter.php:94` validates `frequency` with `Rule::in(ValueSets::allowed('charges','frequency'))`, and `app/Support/ValueSets.php:254` sets that to the four values `['monthly','quarterly','annually','one_time']`. `app/Services/BillUnitOwnershipsService.php:345-363` (`appliesToPeriod`) handles `one_time` and then ends `return $charge->frequency === 'monthly';` — so `quarterly`/`annually` answer false, `billOne()` line 158 hits `$charges->isEmpty()` and returns null, `isUnconfigured()` (line 314) is false because rows DO exist, and line 103 does `$stat |
| **SW-046** | ✅ **fixed** `b31ee637` | medium | M | A catch-up billing run silently skips every lease that expired between the failed night and the re-run — today's status answers a period question | `Models/Concerns/Lease/HasLeaseTermState:255` | **Designed by the 2026-09-03 patch fleet, adversarially reviewed, then applied, tested and mutation-checked here.** Both halves of the billing-eligibility pair ask `status === 'active'`, which is a fact about TODAY, inside a question about a PERIOD — so once `leases:expire` has retired a lease the catch-up run for the month it was actually trading skips it, and the final month of that tenancy is never invoiced. Read, not run (repo frozen; the new test is what measures it). `app/Models/Concerns/Lease/HasLeaseTermState.php:172` (`isBillableForPeriod`) opens `if ($this->status !== 'active') return false;` and line 312 (`scopeBillableForPeriod`) opens `->where('status', 'active')`. Every other clause in both is a PERIOD test (`commencement_date <= periodEnd`, `expiry_date >= periodStart`, the holdover branch). `ExpireLeasesCommand` (scheduled 05:15) moves an active lease past its term to `expired`; `RunMonthlyBillingCommand` (`app/Console/Commands/RunMonthlyBillingCommand.php:12`) explicitly supports `--p |
| **SW-047** | ✅ **fixed** `b31ee637` | medium | M | "End charge" on an arrears-billed charge silently forfeits the last consumed month — close() deactivates the row the next invoice still needs | `Services/ChargeScheduleService:479` | **Designed by the 2026-09-03 patch fleet, adversarially reviewed, then applied, tested and mutation-checked here.** "End charge" on an arrears-billed row deactivates it the moment the stop date has arrived, and the planner drops an inactive row before it computes the window that row covers — so the last month the tenant actually consumed is never invoiced. Read, not run (repo frozen; the new test is what measures it). `app/Services/ChargeScheduleService.php:479-491`: `$stopsInTheFuture = $from->isFuture()`, and the `default` arm writes `['end_date' => $from->subDay(), 'is_active' => false]`. `billingBoundary()` (line 641) snaps `$from` to `startOfMonth`, so ending a charge "from September" on 2 September takes the default arm. `MonthlyBillingService.php:559` bars an inactive row absolutely — `if (! $c->is_active) return false;` — BEFORE line 563 computes `coveredWindow()`, which for an arrears row (`Charge::billsInArrears()`, `app/Models/Charge.php:300`) shifts back one cycle: for the September period |
| **SW-048** | ✅ **fixed** `b31ee637` | medium | — | `leases.expiry_reminder_notified_at` is set and never cleared, so extending a lease's term silently kills its renewal reminder for the rest of the tenancy | `Services/LeaseExtensionService:70` | **Designed by the 2026-09-03 patch fleet, adversarially reviewed, then applied, tested and mutation-checked here.** `leases.expiry_reminder_notified_at` is the idempotency stamp the renewal-reminder sweep keys on, and nothing has ever cleared it — so extending a lease's term leaves the stamp describing a date in the middle of the tenancy and the tenant is never reminded again. Read, not run (repo frozen; the new test is what measures it). `grep -rn 'expiry_reminder_notified_at' app/ database/` returns eight hits and exactly ONE writer: `app/Console/Commands/RemindExpiringLeasesCommand.php:81` sets it; line 30 filters `->whereNull('expiry_reminder_notified_at')` and line 70 re-checks it under the lock. No code path anywhere sets it back to null. `app/Services/LeaseExtensionService.php:70` updates `expiry_date` (forwards only, refused otherwise at line 57) and touches nothing else on the reminder side, and `app/Filament/Admin/Resources/Leases/Schemas/LeaseForm.php:358-363` still renders `expiry_date` as |
| **SW-049** | ✅ **fixed** `d6285fca` | medium | S | LeaseRentChangeService's inline rate↔rent derivations ignore the holdover premium — EG-40's fix does not cover the sibling copy, and the round trip compounds it | `Services/LeaseRentChangeService:70` | **Confirmed and fixed 2026-09-03.** EG-40's third door: `LeaseSpaceChangeService` was taught the premium in August, `LeaseRentChangeService` was not — it did the arithmetic inline and the word `holdover` appears nowhere in the file, and `Lease::saving` deliberately skips re-derivation when this service states a rent. **Both directions, opposite errors.** Rate→rent UNDER-CHARGES: the modal prefills the contractual rate and requires it, so a save that only meant to change the service charge wrote 150,000 → **100,000**, silently. Rent→rate INFLATES the contract: the escalation sweep passes the uplifted rent, so 157,500 produced **7,560/m²/yr where 5,040 is contracted** — ×1.5 exactly, compounding on every later derivation. `deriveContractedRateFromEffectiveRent()` is a SECOND method rather than an edit to the shared one, because `LeaseRenewalService` passes a NEGOTIATED rent that is already contractual even off a holdover — stripping a premium there would be EG-39 re-created one column along, and a test pins it. `AHoldoverKeepsItsUpliftThroughARentChangeTest`, 6 cases, 4 mutations |
| **SW-050** | ✅ **fixed** `81d628df` | high | M | Immediate termination deactivates the whole schedule, so a terminated arrears lease's final consumed month is never billed | `Services/LeaseTerminationService:106` **VERIFIED AND ESCALATED 2026-09-05 — this is OUTBOUND money, and the row's own stated cause is WRONG.** Measured end to end through the real services on a fixture of rent 100,000 in advance + service charge 20,000 **in arrears**, deposit 300,000, terminated 20 Sep: the September service charge for the 20 days consumed — **13,333.33** — is invoiced by NOTHING, and because `MoveOutStatementService` computes `net = depositHeld + tenantCredit − openAr` from EXISTING invoices only, an invoice nobody raised is not open AR, so the refund cheque is **300,000.00 where it should be 286,666.67**. Money the tenant OWED leaves the building as a larger refund, the tenant has gone, and there is no recovery path. `pendingTrueUps()` covers CAM and percentage rent and has **no unbilled-period term**, so nothing on the statement says anything is missing. **(1) The stated cause is not the blocker.** `is_active = false` at `LeaseTerminationService:106` is guarded by the same `$underNotice` as the status write, so it can never fire without `status` becoming `terminated` — and `HasLeaseTermState:206` refuses on the STATUS before the planner ever reads a charge row. Proven by re-running with `is_active` forced back to true: nothing changed, refund identical. Widening `BILLABLE_STATUSES` does not reach it either, because termination sets `expiry_date` to the termination date and `:227`/`:361` require `expiry_date >= periodStart`. **The gate cannot be widened into a fix.** **(2) It is narrower AND broader than the row says.** Narrower: an ADVANCE row is billed and correctly credited, so the hole is confined to `charges.billing_timing = 'arrears'`. Broader: the discriminator is not *immediate* termination but whether the end date was recorded **before or after** the final period's invoice was raised — `$isFinalCycle` is computed at plan time and nothing re-plans, so notice given on the 5th for the 30th of the same month loses the month too, with the schedule fully live. It also falsifies the docblock at `HasLeaseTermState:200-203`, which asserts as settled fact that *a terminated lease has had its final account and its unearned credit note*. **(3) Exposure is zero TODAY and arms on configuration**: the QA baseline has 33 leases, all `active`, zero terminations, and `billing_timing` NULL on all 342 charge rows — while EG-30's own migration calls rent-in-advance/service-charge-in-arrears *"the ordinary Egyptian arrangement"* and ships the column on the form and the importer. **(4) The fix needs FOUR owner decisions and one hazard**, which is why it is not built: whether the final bill is auto-issued or a draft (Yardi reviews a batch before it commits); what the posting date is when that month is CLOSED — the guard THROWS, and inside `terminate()`'s transaction that aborts the whole termination; whether Settle blocks until the final invoice exists; and the hazard — **legacy `covered_end` is NULL** (SW-051 deliberately refused to backfill, and the QA database does not even have the column), so a naive re-plan double-bills **86,666.67** by re-raising rent already invoiced AND already credited. Proration needs no decision: re-planning the termination month already returns exactly the right single line, honouring both `ProrationMethod` and `charges.prorate`, because SW-051's clamp drops the already-billed rent. **Do not build any of it before the owner rules on Q1/Q2/Q4** — a wrong choice here moves money outbound in the other direction **FIXED 2026-09-05 (`81d628df`), and the build changed four of the design's own decisions.** `App\Services\BillFinalPeriodService` reuses `planInvoiceForLease()` — no new arithmetic, and `invoice_items.covered_end` is the idempotency stamp, so no new column. **Two doors**, because a future-dated termination is NOTICE: the service bills an immediate termination and `leases:expire` bills the lease that ends by notice or by running out, wrapped per lease so one throw cannot abort the sweep. **The stated cause was half wrong** — `is_active = false` does not stop the SCHEDULED run (the status refusal fires first) but is exactly what stopped the new act, which is already past the status; deactivation now runs once nothing else reads the schedule. **Four changes the mutations and the adversarial review forced, each worth more than the original fix:** the bill is NOT restricted to arrears rows (that under-bills — 66,666.67 of prorated rent lost on any mid-month termination, since the run fires on the 1st; `covered_end` is right for both timings, which is Yardi's rule that charges prorate to the move-out date); the legacy probe is SCOPED BY `charge_id`, because unscoped it made the service INERT — `covered_end` is written by the recurring run alone, so one late fee, deposit bill, fine, utility recharge, CAM recovery, % rent overage or NSF fee anywhere in the lease's life refused it for ever, hardest on exactly the leases it exists for; it bills the CYCLE not the calendar month (a quarterly lease answered `off_cycle` two months in three and raised nothing); and `forceFinalCycle` covers the HOLDOVER, whose expiry is deliberately in the past and whose only door this is, since the sweep excludes them. **Stated deviation**: the posting date falls FORWARD where `CreditUnearnedBillingService` refuses — a credit note can be re-taken, this is the last chance to ask for the money — and if today is closed too it SKIPS loudly rather than taking the whole termination down with it. Every skip is an `OpsLog` warning and the final invoice's number goes into the `lease_terminated` event payload, because a terminated lease never reaches the expiry sweep and has no second chance. **It bills ONE period, not the unbilled backlog** — recorded because the document looks like a complete final bill. The opt-out toggle is reachable and bilingual. `ATerminatedLeaseStillBillsTheMonthItConsumedTest`, 9 cases, 6 mutations |
| **SW-051** | ✅ **fixed** `d41fd0ac` | medium | M | Converting to holdover after the final-settle run double-bills the settled arrears month | `Services/MonthlyBillingService:376` | **Confirmed by the 2026-09-03 verification sweep and fixed the same day, and the row understated its own reach** — the EXTENSION path needs no holdover, no expired lease and no card, just an operator moving `expiry_date` forward, so it is the likelier of the two. Measured: a lease expiring 31 Aug with a 20,000/month arrears service charge bills Jul–Aug on the August invoice and **August again** on September's — 22,800 gross, outbound, plausible on both documents. The cause is that `isFinalCycle` is a PREDICTION the lease can falsify, and the covered window was **stored nowhere**: `alreadyBilledForMonth()` compares the INVOICES' periods (no overlap), while the window survived only as English in `description`. `invoice_items.covered_start`/`covered_end` make it a fact and the planner clamps against it. Null = NOT RECORDED (backfill refused: guessing from the invoice period would be right for advance rows and wrong for exactly the arrears rows this protects, which could SUPPRESS a real bill). `AMonthIsBilledOnceTest`, 6 cases, 2 of 3 mutations red — the third is defensive and says so |
| **SW-052** | ✅ **fixed** `71227984` | medium | S | One overlapping charge schedule takes down the whole Billing Run Preview — the preview does not contain the per-lease refusal the real run contains | `Services/MonthlyBillingService:864` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** One lease whose charge schedule overlaps throws a DomainException straight out of `planInvoiceForLease()` — a READ — so it takes down not just the Billing run preview but every screen built on that method: month-end close readiness, the lease's own Billing forecast tab and the portfolio Revenue forecast, all of which an operator opens precisely because something is wrong. Read at HEAD, not run (repo frozen; the new test is what measures it). `app/Services/MonthlyBillingService.php:589` calls `assertScheduleUnambiguous()` from inside `planInvoiceForLease()`, and that method (line 1012-1031) throws `\DomainException(__('admin.refusals.overlapping_charge_schedule', …))`. Its own docblock claims the blast radius is contained — "`runForPeriod()` |
| **SW-053** | ✅ **fixed** `71227984` | low | XS | Lease-clause form ships hardcoded English 'days' and 'km' suffixes that the Arabic-chrome gate cannot see | `Filament/Admin/RelationManagers/LeaseClausesRelationManager:93` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** The lease-clause form printed the English word `days` after the notice-period box and Settings → SLA printed the English abbreviation `hrs` after all eight deadline boxes, so the Arabic panel carried English words inside Arabic forms — and the Arabic-chrome gate is structurally unable to see an affix. The row's other half is refused: `km` is an SI symbol, not a word, and stays verbatim like the eight `m²` already shipped. Read and RUN at HEAD e3154f27 (2026-09-04). Built the clause schema in tinker — `(new LeaseClausesRelationManager)->form(Schema::make())->getComponents(withActions: false, withHidden: true)` — and read `getSuffixLabel()` off each `TextInput`: `threshold_pct => '%'`, `radius_km => 'km'`, `notice_days => 'days'`. With the pa |
| **SW-054** | ✅ **fixed** `b31ee637` | low | XS | Dead private option-builders in LeasesTable still carry the pre-2026-08-28 self-holding exemption that RentableItemOptions was fixed to remove | `Filament/Admin/Resources/Leases/Tables/LeasesTable:53` | **Designed by the 2026-09-03 patch fleet, adversarially reviewed, then applied, tested and mutation-checked here.** `LeasesTable` still carries a dead private copy of the two rentable-item option builders, and that copy keeps the self-holding exemption that `RentableItemOptions` was fixed to remove on 2026-08-28 — the mechanism (`RentableItem::isHeldOn()`'s `$ignore` parameter) survives with no live caller, so the reversed rule can be reintroduced by copy-paste. `grep -rn 'lettableItemOptions\/heldItemOptions' app/ tests/` at HEAD returns six lines: the two DECLARATIONS on `LeasesTable` (lines 53 and 77), two calls in `LeaseActions` (480, 530) that resolve to `LeaseActions`' OWN private copies (1123, 1129), and those copies — which delegate to `App\Support\RentableItemOptions`. `LeasesTable`'s pair is `private static` and is called from nowhere inside `LeasesTable`: dead. It carries `->reject(fn (RentableItem $i) => $i->isHeldOn(null, ignore: ['type' => 'lease', 'id' => (int) $record->id]))` — the exe |
| **SW-055** | ✅ **fixed** `b31ee637` | low | S | 'Deposit outstanding' filter re-implements depositHeld() in SQL minus the billed-and-settled channel, so it flags leases whose deposit is fully collected | `Filament/Admin/Resources/Leases/Tables/LeasesTable:247` | **Designed by the 2026-09-03 patch fleet, adversarially reviewed, then applied, tested and mutation-checked here.** The leases list's "Deposit outstanding" filter re-implements the deposit pot as one `whereRaw` and omits the billed-and-settled term, so a lease whose deposit arrived on a paid deposit invoice is listed as still owing all of it — on the same page as a `deposit_shortfall` column reading 0.00 for that row. At `app/Filament/Admin/Resources/Leases/Tables/LeasesTable.php:247-259` the filter is `security_deposit > (receipts − refunds/forfeits from deposit_transactions − deposit_applications)`. That is THREE of the four terms `Lease::depositHeld()` (Lease.php:828-851) sums; the fourth, `+ $this->settledDepositBillings()` (Lease.php:917-947), is absent. The `deposit_shortfall` COLUMN on the same table (LeasesTable:170-172) calls `$record->depositShortfall()`, which does include it — so the two answers sit side by side. `BillSecurityDepositService` raises the deposit on its own invoice and writes |
| **SW-056** | ✅ **fixed** `71227984` | low | XS | Unit-ownership form and model default payment_terms_days to a literal 7, ignoring the mall's configured payment-terms convention | `Filament/Admin/Resources/UnitOwnerships/Schemas/UnitOwnershipForm:136` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** The unit-ownership form pre-filled a hard-coded 7-day payment term, so a mall that had configured 30-day terms got 7 on every unit sale it recorded — while the model docblock beside it already described the form pre-filling from the property's convention as the mechanism. The row's second half is refused: the model's own default of 7 mirrors the NOT NULL column and must stay. Read at HEAD e3154f27 (2026-09-04); the create page cannot be built outside Livewire (`PropertyField` needs a container, verified — `Schema::getLivewire()` TypeErrors), so this half is read, not run, and the new test is what measures it. `app/Filament/Admin/Resources/UnitOwnerships/Schemas/UnitOwnershipForm.php:132-137` is `TextInput::make('payment_terms_days')->label( |
| **SW-057** | ❌ **refuted** | low | XS | CreditUnearnedBillingService issues real GL-posted credit notes against DRAFT invoices | `Services/CreditUnearnedBillingService:86` | **Measured against HEAD by the 2026-09-03 verification sweep** (69 agents, one per group of related rows; the adversarial challenge phase did NOT run — it died on the session limit — so this verdict is single-source). The row's LOCATION is right and its EFFECT is not. `CreditUnearnedBillingService.php:87` really does include `'draft'` in `->whereIn('status', ['draft','issued','partially_paid','overdue','paid'])`, so a draft invoice IS selected. But no credit note is ever produced from one, because of a second gate the row did not read: `isTimeApportioned()` at CreditUnearnedBillingService.php:322-323 returns false on `blank($item->charge_id)`, every line is skipped, `$total <= 0` and the method returns null BEFORE `CreditNote::create()`. No invoice in `draft` status can carry a `charge_id` line. Measured three ways: (a) `InvoiceItem::create()` appears in exactly ONE place in app/ — `IssueInvoiceService.ph |

### Facility · vendors · procurement

*0 open.*

| ID | Status | Sev | Fix | What is wrong | Where |
|---|---|---|---|---|---|
| **SW-058** | ✅ **fixed** `c7c00202` | high | M | No screen can assign or reassign a work order's technician after creation — the model's own reassignment-notification hook is unreachable | `Filament/Admin/Resources/FacilityWorkOrders/Schemas/FacilityWorkOrderForm:146` |
| **SW-059** | ✅ **fixed** `5e159676` | high | XS | Service Plans 'Generate due' header action calls a protected method cross-class — guaranteed PHP Error after generation runs | `Filament/Admin/Resources/ServicePlans/Tables/ServicePlansTable:146` |
| **SW-060** | ✅ **fixed** `c7c00202` | high | S | Draft purchase request is a panel dead-end: submit() has no caller and every edit surface is locked to 'requested' | `Filament/Admin/RelationManagers/PurchaseRequestLinesRelationManager:53` |
| **SW-061** | ✅ **fixed** `790ba6a1` | high | M | Editing day_of_month (or frequency) on a live schedule re-walks the calendar and double-books the current period | `Filament/Admin/Resources/RecurringExpenses/Schemas/RecurringExpenseForm:104` |
| **SW-062** | ✅ **fixed** `47fb5df5` | high | — | The contractor can post to the job thread but can never read it — the operator's "Share with the contractor" reaches nobody | `Filament/Vendor/Resources/WorkOrders/Pages/ListWorkOrders:142` |
| **SW-063** | ✅ **fixed** `47fb5df5` | high | — | The quote loop is one-way: the NTE that is supposed to trigger a quote is invisible, and the decision never comes back | `Filament/Vendor/Resources/WorkOrders/Pages/ListWorkOrders:176` |
| **SW-064** | ✅ **fixed** `e37f23f9` | high | — | Every recurring EXPENSE credits the CASH account — the schedule has no paid_from and the generator omits it, so the column default wins The schedule now carries `paid_from` and `bank_account_id`, both nullable so an existing schedule behaves exactly as before, and the generator passes them through. `bank_account_id` comes with the rail because `MoneyAccount` resolves the document's own account first — without it a mall banking in two places cannot say which. | `Services/GenerateRecurringExpensesService:180` |
| **SW-065** | ✅ **fixed** `71227984` | medium | M | Picking a vendor on an internal corrective work order 500s: the edit form offers a field the model refuses with InvalidArgumentException, and execution_type has no screen to change it | `Filament/Admin/Resources/FacilityWorkOrders/Schemas/FacilityWorkOrderForm:152` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** An in-house corrective job could never be handed to a contractor: the edit form offered both assignee pickers, had no control at all for the classification that makes them mutually exclusive, and picking a vendor threw an InvalidArgumentException — the 500 page, with the operator's form lost. Reproduced 2026-09-04 with a throwaway probe on a fresh sqlite schema (since deleted). Creating a `cm` with `execution_type = 'internal'` carrying a technician and then `$cm->update(['vendor_id' => $vendor->id])` — exactly what `EditFacilityWorkOrder` does — throws `InvalidArgumentException: An internal corrective work order is handled in-house; it cannot also name a vendor.` (`app/Models/FacilityWorkOrder.php:863-864`). Probe output: `is DomainExcepti |
| **SW-066** | ✅ **fixed** `c84606ab` | medium | S | An ISSUED (or CLOSED) work permit is fully editable on its record page — the stated 'a live authorisation is not a draft' rule is enforced only by hiding the list's Edit shortcut | `Filament/Admin/Resources/WorkPermits/Pages/EditWorkPermit:26` | `WorkPermit::updating` refuses a change to what the permit AUTHORISES — the work, the place, the window, the conditions, the contractor — once the ORIGINAL status is anything but `draft`. A **denylist of substance**, never an allowlist of what the acts write: `getDirty()` is read after every `saving` hook, the trap the lease-holdover carve-out records. **`canEdit()` is deliberately not the lever** — `WorkPermitActions` live on the record page and gate on `canIssue()`, so refusing the page would strand *close* and *cancel* for exactly the permits that need them (`RowActionPolicy`'s reachability trap). `closed_*`/`closure_notes` stay writable and `status` may move forward but never back to `draft`. **The review found five more, and the worst was the fix's own:** a disabled Filament field is still DEHYDRATED, and `DateTimePicker->seconds(false)` truncates the window to the minute — so filling from an untouched form made `valid_from`/`valid_to` dirty against a stored value carrying seconds and **Save without touching anything was refused**, which is the ordinary state of every permit `DemoSeeder` writes (the fixtures that missed it all parse a zero-second literal). The page drops a live permit's payload and the form disables itself, because a model refusing under a screen that still offers thirteen editable fields is an affordance with no right behind it. `issued_at`/`issued_by_user_id` (WHO authorised hazardous work and WHEN) and `reference` (the number quoted at the gate) were freely rewritable and are frozen; closed → draft → rewrite → issue again was a second authorisation on one reference. And a per-column mutation run found **seven of thirteen denylist entries unproved** by the first test — `vendor_id` among them, the only door around the `isDispatchable()` gate a blacklisted contractor is refused at. All ten teeth mutation-proved; `cancel` and close-on-lapsed now asserted too. `AnIssuedPermitAuthorisesWhatItSaidTest` |
| **SW-067** | ✅ **fixed** `71227984` | medium | S | A rejected purchase request shows its 'rejected' badge but the required rejection reason is rendered nowhere | `Filament/Admin/Resources/PurchaseRequests/Tables/PurchaseRequestsTable:39` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** A purchase request refused by a manager records the reason the refusal form REQUIRES — and no screen in the panel ever printed it, so the buyer could not find out why and the operator left no answer anyone could point at. At HEAD e3154f27. `PurchaseRequestService::reject()` (app/Services/PurchaseRequestService.php:183) and `::cancel()` (:311) both take a `string $reason` — the action modals make that Textarea `->required()` (app/Filament/Admin/Actions/PurchaseRequestActions.php:96, :188) — and `::approve()` (:142) takes an optional note. All three write `purchase_requests.decision_notes`. Probe: `grep -rn 'decision_notes' app/` returns exactly those three writes plus one registration in `app/Support/ActivityLogging.php:210`, and ZERO reads. |
| **SW-068** | ✅ **fixed** `71227984` | medium | M | A recurring schedule can be created in a state that never books, and the screen never says so | `Filament/Admin/Resources/RecurringExpenses/Schemas/RecurringExpenseForm:113` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** A recurring cost schedule can be saved with a window that contains no scheduled day at all — the nightly run then skips it for ever, and the register goes on showing an amount and a frequency as though a statutory levy were being booked. At HEAD e3154f27, by instantiating `App\Models\RecurringExpense` and calling `nextDueOn(CarbonImmutable::parse('2036-01-01'))`. Four windows, all saveable through `/admin/recurring-expenses` (the form has no cross-field rule and the model no guard), all answering null for ever: (a) `ends_on` 2026-09-01 before `starts_on` 2026-10-01 => NULL; (b) monthly, `day_of_month` 1, starts 2026-09-20, ends 2026-09-30 => NULL; (c) quarterly, day 1, starts 2026-09-20, ends 2026-11-30 => NULL; (d) annually, day 1, starts  |
| **SW-069** | ✅ **fixed** `b31ee637` | medium | — | The contractor's only screen shows status and priority as raw database codes, in both languages | `Filament/Vendor/Resources/WorkOrders/Pages/ListWorkOrders:51` | **Designed by the 2026-09-03 patch fleet, adversarially reviewed, then applied, tested and mutation-checked here.** The contractor's only screen printed the raw database codes `in_progress` and `urgent` as its status and priority badges, in both languages, because Filament renders a state untouched when no formatter is set. `app/Filament/Vendor/Resources/WorkOrders/Pages/ListWorkOrders.php:52` and `:67` are `TextColumn::make(...)->badge()` with no `formatStateUsing`. `vendor/filament/tables/src/Columns/Concerns/CanFormatState.php:376-382` — `formatState()` evaluates `$this->formatStateUsing ?? $state`, i.e. it passes the state through untouched; there is no humanising default. Probe: I swept every `TextColumn::make('<col>')` in `app/Filament` whose leaf column is one `ValueSets::SETS` governs, skipping array-fed tables (`->records(`), and classified each by whether its chain carries `->badge()` and a formatter. Result: **150 badge columns over classification columns, 2 with no formatter — both of them |
| **SW-070** | ✅ **fixed** `b31ee637` | medium | — | The jobs list has no filters and opens oldest-first, so most of a contractor's screen is closed work | `Filament/Vendor/Resources/WorkOrders/Pages/ListWorkOrders:233` | **Designed by the 2026-09-03 patch fleet, adversarially reviewed, then applied, tested and mutation-checked here.** The contractor's job list had no filters and, because finished jobs are shown deliberately and never age out, opened on ten rows of closed work with the three live jobs below them. `app/Filament/Vendor/Resources/WorkOrders/Pages/ListWorkOrders.php:253-255` — `->toolbarActions([])->defaultSort('scheduled_for','asc')` and no `->filters(...)` call anywhere in the file. `VendorScope::VISIBLE_STATUSES` (`app/Support/Filament/VendorScope.php:54`) includes `done` and `cancelled`, with a docblock saying so on purpose. Measured read-only against `mall_management_qa`: `select vendor_id, status, count(*) … group by vendor_id` gives vendor 1 **10 `done` + 3 `open`**; replaying the page's own order (`order by scheduled_for asc, id asc where vendor_id = 1`) puts the ten finished jobs in rows 1–10, the top one scheduled **2024-09-02**, and the three live jobs at rows 11, 12 and 13. The list is register |
| **SW-071** | ✅ **fixed** `b31ee637` | medium | — | act_material_cost ignores the stock ledger while partsCost() and the module doc follow it — two definitions of what a part cost the job | `Models/Concerns/FacilityWorkOrder/HasWorkOrderCost:92` | **Designed by the 2026-09-03 patch fleet, adversarially reviewed, then applied, tested and mutation-checked here.** ONE defect, ONE patch: `recomputeCosts()` wrote out its own opinion of which child rows count instead of asking the child model, and two of its three clauses had drifted — a part draw whose stock went back on the shelf kept being charged, and a DRAFT contractor invoice counted as actual cost and could price a percent-of-value SLA penalty. Both rows land in the same block, `app/Models/Concerns/FacilityWorkOrder/HasWorkOrderCost.php:116-129`. SW-071: the material clause is `$this->parts()->whereIn('status', [STATUS_APPROVED, STATUS_RECORDED])` — the STATUS alone. `FacilityWorkOrder::partsCost()` (`app/Models/FacilityWorkOrder.php:360`) reads `FacilityWorkOrderPart::scopeCounted()` (`:136`), which is `(status = approved AND whereHas('movement')) OR status = recorded` — the STOCK LEDGER — and `docs/modules/26-facility.md:766` states that reading as the system's behaviour ("a voided draw's mo |
| **SW-072** | ✅ **fixed** `b31ee637` | medium | — | A DRAFT vendor bill counts as actual cost on the work order, and can price a contractor's SLA penalty | `Models/Concerns/FacilityWorkOrder/HasWorkOrderCost:96` | **Designed by the 2026-09-03 patch fleet, adversarially reviewed, then applied, tested and mutation-checked here.** ONE defect, ONE patch: `recomputeCosts()` wrote out its own opinion of which child rows count instead of asking the child model, and two of its three clauses had drifted — a part draw whose stock went back on the shelf kept being charged, and a DRAFT contractor invoice counted as actual cost and could price a percent-of-value SLA penalty. Both rows land in the same block, `app/Models/Concerns/FacilityWorkOrder/HasWorkOrderCost.php:116-129`. SW-071: the material clause is `$this->parts()->whereIn('status', [STATUS_APPROVED, STATUS_RECORDED])` — the STATUS alone. `FacilityWorkOrder::partsCost()` (`app/Models/FacilityWorkOrder.php:360`) reads `FacilityWorkOrderPart::scopeCounted()` (`:136`), which is `(status = approved AND whereHas('movement')) OR status = recorded` — the STOCK LEDGER — and `docs/modules/26-facility.md:766` states that reading as the system's behaviour ("a voided draw's mo |
| **SW-073** | ✅ **verified fixed** | medium | — | Editing day_of_month forward on a live recurring schedule books the same period twice — the UNIQUE index keys on the DATE, not the period | `Models/RecurringExpense:193` | **Measured against HEAD by the 2026-09-03 verification sweep** (69 agents, one per group of related rows; the adversarial challenge phase did NOT run — it died on the session limit — so this verdict is single-source). The premise was TRUE on 2026-09-01 and was fixed on 2026-09-02 by commit 790ba6a1 ("fix(expenses): moving the booking day re-booked the period it had already booked"). I reconstructed the pre-fix walk verbatim from `git show 790ba6a1^:app/Models/RecurringExpense.php:209` (`$alreadyDone = last_generated_on !== null && ! $cursor->greaterThan($last_generated_on)`) and ran it: monthly schedule, starts_on 2026-09-01, last_generated_on 2026-09-01, day_of_month edited 1 -> 15, asked on 2026-09-20 => returned **2026-09-15**, i.e. September due a second time. Quarterly, day 1 -> 20, Q booked 2026-07-01, asked 2026-07-31 => **2026-07-20**, same double-book. At HEAD (`app/Models/RecurringExpense.php:23 |
| **SW-074** | ✅ **verified fixed** | medium | — | Editing a recurring cost's `day_of_month` forward re-arms the period it has already generated — a second statutory expense for the same month, posted to the GL, that the UNIQUE index cannot catch | `Models/RecurringExpense:209` | **Measured against HEAD by the 2026-09-03 verification sweep** (69 agents, one per group of related rows; the adversarial challenge phase did NOT run — it died on the session limit — so this verdict is single-source). **This is SW-073, reported twice.** Both rows cite `app/Models/RecurringExpense.php` and both land inside `nextDueOn()`: in the pre-fix file (`790ba6a1^`) line 193 is that method's docblock ("The day is CLAMPED to the month's length...") and line 209 is the defective comparison itself (`$alreadyDone = ... ! $cursor->greaterThan(CarbonImmutable::instance($this->last_generated_on))`). Same method, same walk, same edit, same consequence. SW-074's claim adds only the detail that the duplicate is "posted to the GL", which is true of the expense branch (`GenerateRecurringExpensesService::recordExpense()` mints it as `recorded`) and quieter on the vendor-bill branch (a duplicate `VendorBill` is `dr |
| **SW-075** | ✅ **fixed** `71227984` | medium | — | The profile page enforces an email uniqueness the domain deliberately rejects, and can lock a contractor out of their own password change | `Providers/Filament/VendorPanelProvider:51` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** The contractor portal's profile page enforces email uniqueness across the whole of `vendor_contacts`, a rule the domain deliberately rejects — and because the page saves name, email and password in one submit, a contractor whose address also sits on an ordinary non-login contact row can never change their own password. HEAD e3154f27. `app/Providers/Filament/VendorPanelProvider.php:51` is `->profile(isSimple: false)`, i.e. Filament's stock `Filament\Auth\Pages\EditProfile`. `vendor/filament/filament/src/Auth/Pages/EditProfile.php:357-366` builds `TextInput::make('email')->…->unique(ignoreRecord: true)`, and `:416` sets `->model($this->getUser())`, so the rule's table is resolved from `VendorContact`. Probe (booted app, no writes): `(string)  |
| **SW-076** | ✅ **fixed** `1261ad57` | low | XS | Global search details on work orders and equipment read the dropped `category` column — a permanently blank 'Category' row where the trade should be, plus an untranslated raw priority code | `Filament/Admin/Resources/FacilityWorkOrders/FacilityWorkOrderResource:166` **Fixed 2026-09-05.** Verified in a booted app: `category` is absent from BOTH `facility_work_orders` and `equipment` and `trade_id` is present on both — the Trade catalogue replaced it, because two columns answering *"what kind of work is this"* would be two truths about one question. **A missing attribute resolves NULL rather than throwing**, which is the whole reason it survived: the top-bar result rendered a permanently blank *Category* row where the routing spine belongs, and a blank row reads as *"this record has no trade"* rather than as a bug. The tables beside them have read `$record->trade?->label()` all along — two doors onto one fact allowed to disagree, the same shape as SW-165's `->tenant`, found in the same sweep. The priority was the second half, printed as the raw stored code so an Arabic operator read `high` among otherwise translated details, while that resource's own table resolves it through `FacilityVocabulary`. **`SearchPolicy` structurally could not see it** — that gate governs which models are indexed and that no resource searches a raw column, and says nothing about what a result DISPLAYS. Both resources now eager-load `trade`: these details fire per ROW of a live-search dropdown, so without it the fix would trade a blank row for an N+1 on every keystroke. `GlobalSearchDetailsNameTheTradeNotADroppedColumnTest`, 5 cases, asserts the columns really are gone before reporting on them, 4 teeth mutation-proved |
| **SW-077** | ✅ **fixed** `71227984` | low | XS | Estimated-hours fields have no maxValue against decimal(8,2) — a fat-fingered figure is an SQL out-of-range 500, not a validation message | `Filament/Admin/Resources/FacilityWorkOrders/Schemas/FacilityWorkOrderForm:103` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** Both forms that collect a work order's estimated labour hours carry a floor and no ceiling against a `decimal(8,2)` column, so a fat-fingered figure is an out-of-range QueryException — a 500 page after Save, with the form lost — rather than a validation message, and the sqlite suite structurally cannot see it. HEAD e3154f27. `show columns from facility_work_orders like 'est_labour_hours'` → `decimal(8,2) null=YES`, i.e. a ceiling of 999,999.99; `service_plans.est_labour_hours` is the same width. `app/Filament/Admin/Resources/FacilityWorkOrders/Schemas/FacilityWorkOrderForm.php:104-109` and `app/Filament/Admin/Resources/FacilityWorkOrders/Schemas/CorrectiveWorkOrderForm.php:91-95` are both `TextInput::make('est_labour_hours')->numeric()->min |
| **SW-078** | ✅ **fixed** `b31ee637` | low | S | The work-orders list shows neither the assigned technician nor the vendor — the dispatch board cannot answer 'who is on this job?' | `Filament/Admin/Resources/FacilityWorkOrders/Tables/FacilityWorkOrdersTable:49` | **Designed by the 2026-09-03 patch fleet, adversarially reviewed, then applied, tested and mutation-checked here.** The work-orders list is the dispatch board and it renders neither the contractor nor the technician, so "who is on this job?" needs a record page opened per row. Read `app/Filament/Admin/Resources/FacilityWorkOrders/Tables/FacilityWorkOrdersTable.php:49-274` at HEAD and enumerated the columns: reference, work_order_type, title, asset.name, area.name, equipment.code, scheduled_for, progress, priority, target_response_at, target_resolution_at, penalty.amount, cost_bearer, status, act_total_cost, cost_variance, repeat_visit, over_nte, pm_compliance — 19 columns, and NEITHER `vendor_id` nor `assigned_to_user_id`. Both have been on the row since the module shipped (`app/Models/FacilityWorkOrder.php:178-179` fillable, relations `vendor()` at :316 and `assignee()` at :329). The filter list (:275-325) carries no assignee or vendor filter either. `grep -c` confirms one `->modifyQueryUsing(` eager |
| **SW-079** | ✅ **fixed** `b31ee637` | low | XS | Hardcoded Latin 'h' hour-suffix composed into SLA overrun strings inside column closures — Arabic panel reads '٦٧h' | `Filament/Admin/Resources/FacilityWorkOrders/Tables/FacilityWorkOrdersTable:132` | **Designed by the 2026-09-03 patch fleet, adversarially reviewed, then applied, tested and mutation-checked here.** Three column descriptions compose an hour count and glue a Latin `h` onto it, so the Arabic panel prints a Latin letter as the unit inside a right-to-left sentence. Tokenised every one of the 629 files under `app/Filament` (comments stripped) and matched `/\.\s*'h'/`: exactly two files, three sites — `FacilityWorkOrdersTable.php:132` (`__('admin.facility.sla.unanswered').' · '.$record->hoursOverResponseSla().'h'`), `:150` (the resolution twin) and `SlaPoliciesTable.php:68` (`__('admin.facility.sla.global_default').': '.SlaResolver::globalHoursFor($record->priority).'h'`). `Lang::has('admin.facility.sla.hours_count')` is false at HEAD. The unit is already written as a WORD everywhere it goes through a lang key: `lang/ar/admin/system.php:760,762,764` render the three SLA breach notification bodies as `:hours ساعة`, and `lang/ar/admin/facility.php` renders `labour.vs_estimate` as `:booked س |
| **SW-080** | ✅ **fixed** `7f678b56` | low | S | Unscoped find() in contract helper-text and purchase-match placeholder reads other malls' commitment figures | `Filament/Admin/Resources/VendorBills/Schemas/VendorBillForm:86` | **Designed by the fleet; its test did not run, repaired and proven here (2026-09-04).** The vendor-bill form resolves the chosen contract and the chosen purchase order with an unscoped `find()`, so a `vendor_contract_id` or `purchase_request_id` supplied in the Livewire payload prints another mall's contract value, billed-to-date, remaining commitment and three-way-match figures to an operator who cannot open that mall. `app/Filament/Admin/Resources/VendorBills/Schemas/VendorBillForm.php:86` is `VendorContract::find($get('vendor_contract_id'))` and `:169` is `PurchaseRequest::find($get('purchase_request_id'))` — neither carries a property clause. Both models are `#[PropertyOwned]` (VendorContract.php:18, PurchaseRequest.php:33) with no global scope: their `booted()` methods (Ve |
| **SW-081** | ✅ **fixed** `b31ee637` | low | XS | Vendor bill due_date can precede bill_date — no ordering rule on the date pair | `Filament/Admin/Resources/VendorBills/Schemas/VendorBillForm:207` | **Designed by the 2026-09-03 patch fleet, adversarially reviewed, then applied, tested and mutation-checked here.** A vendor bill's due date can be typed earlier than its bill date — nothing pairs them — and the bill then reads as permanently OVERDUE in the AP navigation badge, which counts `balance > 0 AND due_date < today`. `app/Filament/Admin/Resources/VendorBills/Schemas/VendorBillForm.php:207-210` is a bare `DatePicker::make('due_date')->label(...)->native(false)->disabled($locked)` — no `after`, `afterOrEqual`, `minDate` or model guard. `database/migrations/2026_07_01_000001_create_vendor_bills_table.php` has `due_date` nullable with no CHECK. The consequence is `VendorBillResource::getNavigationBadge()` (line 136-142): `whereNotIn(status, NON_POSTABLE)->where('balance','>',0)->whereDate('due_date','<',now())` — so a mistyped year turns the red AP badge on with a count the list cannot explain. The AR twin already has the rule: `InvoiceForm.php:202` is `->after('issue_date')` with `admin.validati |
| **SW-082** | ✅ **fixed** `c00e0143` | low | S | Money inputs validate a floor but no ceiling against decimal(14,2) — MySQL-only 500 the suite cannot see | `Filament/Admin/Resources/VendorBills/Schemas/VendorBillForm:227` **Fixed 2026-09-05.** Reproduced on MySQL 8.0.33 in a session-local `TEMPORARY` table: `999999999999.99` inserts, `1000000000000.00` gives `SQLSTATE[22003] … Out of range value` — a `QueryException`, so the 500 page with the form lost, not a message on the field. **The sqlite suite structurally cannot see it**: `Schema::getColumns()` reports a `decimal(14,2)` as `numeric` with no precision at all, so sqlite stores the overflowing figure silently — the same green-here/fatal-there split already recorded for `GREATEST` and the dropped CHECK constraints. **The ceiling is a TENTH of the column's** because `VendorBill::saving` re-derives `total = subtotal + vat_amount` into a `decimal(14,2)` of its own on every write path, so capping each half at the column's own ceiling would still overflow the total on a field the operator cannot type into; two halves at 99,999,999,999.99 sum to 199,999,999,999.98, which the column holds. That same recompute is why capping the two inputs closes the document: a crafted `total` in the Livewire payload is overwritten before the insert. Review verified no legitimate bill is refused and no second operator-typed door exists. `AMoneyFieldRefusesWhatItsColumnCannotHoldTest`, 5 cases, mutation-proved |
| **SW-083** | ✅ **fixed** `76b93d5c` | low | XS | The AP register and the expense register have no date-range filter, unlike every AR money list | `Filament/Admin/Resources/VendorBills/Tables/VendorBillsTable:82` **Fixed 2026-09-05 (`76b93d5c`).** *"Which bills did we take in March"* — the payables clerk's first question — had no answer on the AP register or the expense register except sort-and-scroll, while every AR money list has a range. `App\Support\Filament\DateRangeFilter` is the ONE definition: the nine-line idiom was hand-written on five registers and the two money lists that most need it had none. Extracted rather than copied a seventh time, which is cheap only while the copies have not drifted. `whereDate` deliberately — these columns are `date` on some tables and `datetime` on others, and a plain `>=` against a datetime silently drops everything after midnight on the closing day. **The date mutation needed BOTH clauses changed to bite** — one alone left the other rendering the date function, a false mutation caught in the run. Mutation-proved 4 ways |
| **SW-084** | ✅ **fixed** `71227984` | low | S | Portfolio-wide (NULL asset_id) vendor contracts vanish from the notice-due chase filter and the dashboard count | `Filament/Admin/Resources/Vendors/Tables/VendorsTable:136` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** A vendor contract with no property covers EVERY mall, and four of the five readers narrowed it with `whereIn('asset_id', $ids)` — which never matches NULL — so a portfolio-wide contract at its notice deadline was on no chase list, in no dashboard count and on no badge, while the nightly scan e-mailed about it anyway. At HEAD 45bffd58. The intent is written down three times: `ContractsRelationManager::form()` (app/Filament/.../ContractsRelationManager.php:70-73) — *"a null here is a PORTFOLIO-WIDE contract covering every mall (hence the '—' placeholder and no ->required())"*; `ActionRequired.php:142-143` — *"A contract carries its own asset_id (null = portfolio-wide), so it scopes directly"*; and `database/migrations/2026_08_21_200000_create |
| **SW-085** | ✅ **fixed** `1acd158f` | low | — | FacilityWorkOrderLabour is the one cost channel with no 'moved between jobs' recompute of the job it left | `Models/FacilityWorkOrderLabour:84` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-04).** Three of the four channels that feed a work order's cost recompute the job a re-homed document LEFT; the timesheet channel does not, and it also resolves the job through a cacheable display relation rather than the current foreign key — so the rule that keeps a job's cost honest was written out three times and missed once. At HEAD (83624504): `grep -rn "getOriginal('facility_work_order_id')" app/` matches exactly three files — `app/Models/VendorBill.php:420`, `app/Models/Expense.php:213`, `app/Models/FacilityWorkOrderPart.php:234` — each carrying a byte-identical twelve-line `saved` block plus an identical `workOrderForCosting()` helper (verified byte-identical: one `substr_count` of the same 33-line needle returns 1 in each of the three files). `app/Models/FacilityWorkOrderLabour.php:84-8 |
| **SW-086** | ✅ **fixed** `1acd158f` | low | — | A recurring schedule's first period is derived from the MONTH of starts_on, so it can book a cost dated before the schedule begins — and wedge itself permanently | `Models/RecurringExpense:200` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-04).** A recurring cost schedule derives its first period from the MONTH of `starts_on`, so a schedule that begins mid-month while its booking day is the 1st — the form's default and the column's — books its first document dated BEFORE the schedule begins, and an annual one then runs early for the rest of its life. Probed `RecurringExpense::nextDueOn()` at HEAD (83624504) through a booted app, no DB writes. `starts_on 2026-09-20`, `day_of_month 1`, monthly, asked on 2026-09-25 → **2026-09-01**, nineteen days before the operator said the cost begins. `day_of_month 15` → 2026-09-15, still before it. Quarterly, same shape → 2026-09-01. **Annually → 2026-09-01, and the next is 2027-09-01**, so the whole series stays nineteen days early. `database/migrations/2026_08_23_840000_...php:73` is `unsignedTi |
| **SW-087** | ✅ **fixed** `1acd158f` | low | XS | Withholding tax at bill payment is resolved at today's rate instead of the payment date, contradicting the documented dated-catalogue invariant | `Services/VendorBillService:81` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-04).** Withholding tax on a supplier payment is resolved at TODAY's rung of the dated tax catalogue instead of the payment's own date — in the service that records the deduction and in the preview that tells the operator what the bank will pay — so a back-dated payment withholds the wrong amount and certifies the wrong amount to the supplier. At HEAD (83624504), `app/Services/VendorBillService.php:81` is `$withheld = WithholdingTax::onBillPayment($pay, $bill);` — no date, while `$postingDate` (the normalised payment date, returned by `PostingDate::assertOpen()` at `:57`) is in scope in the same closure. `WithholdingTax::onBillPayment()` passes `$on` straight to `rateFor()`, whose docblock says "Resolved for `$on` — the payment's date — because a withholding rate has a validity period like every o |

### HR · payroll · treasury

*0 open.*

| ID | Status | Sev | Fix | What is wrong | Where |
|---|---|---|---|---|---|
| **SW-088** | ✅ **fixed** `fb85e218` | high | — | The per-property proration override renders as a NUMERIC box — the setting is unsettable, and anything typed is silently discarded | `Filament/Admin/Pages/PropertyOverrides:149` |
| **SW-089** | ✅ **fixed** `5041571a` | high | — | Two unvalidated free-text time fields feed cron expressions — "24:00" stops the entire scheduler | `Filament/Admin/Pages/Settings:381` |
| **SW-090** | ✅ **fixed** `494c7fbd` | high | S | Payroll add-line modal renders allowances / other deductions / deduction note / employer SI and silently discards all four on save | `Filament/Admin/RelationManagers/PayrollLinesRelationManager:232` |
| **SW-091** | ✅ **fixed** `5e159676` | high | S | Un-settled custody's custody_date can be back-dated into a closed period through the Edit form — row saves, GL re-post silently refused | `Filament/Admin/Resources/Custodies/Pages/EditCustody:11` |
| **SW-092** | ✅ **fixed** `6b278e01` + review | high | — | Two payroll runs for the same month can both be approved concurrently — the double-pay guard takes no lock, and the advance re-check decides from a pre-lock snapshot. **The first fix's locking twin was itself a no-op** and the review caught it: `whereHas('payroll', …)->lockForUpdate()` locks the OUTER table only, so it locked `payroll_lines` (whose `advance_deduction` never moves) and read `payrolls.status` — the only value that does — from the snapshot; measured on real MySQL, 0.00 where the truth was 3,000, byte-identical to the plain twin it replaced. Now a join, with the run row locked as the transaction's FIRST statement so the read view is fixed after the cache lock is granted | `Services/PayrollService:48` |
| **SW-093** | ✅ **fixed** `1acd158f` | medium | — | The audit trail cannot be filtered or searched by WHO acted — its most-asked question is unanswerable on screen and in the scheduled CSV | `Filament/Admin/Pages/ActivityLog:207` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-04).** The portfolio-wide audit trail — the screen an auditor is sent to, and the only one that delivers a scheduled CSV — has no control for WHO acted, while the per-record Activities tab beside it has had one all along. `app/Filament/Admin/Pages/ActivityLog.php:207-271` declares exactly four filters — `log_name`, `event`, `period`, `created_range` — and no causer control; `grep -c searchable app/Filament/Admin/Pages/ActivityLog.php` = 0, so no search box renders either (correct: every column is derived at read time, which is why the tab says `->searchable(false)` out loud). The tab already answers it: `app/Filament/Admin/RelationManagers/ActivitiesRelationManager.php:81-85` carries `EntitySelectFilter::make('causer_id')->entity(User::class)` with a query pinning BOTH `causer_id` and `causer_typ |
| **SW-094** | ✅ **fixed** `757cb46f` | medium | — | PropertyOverrides' Save button is ungated in the Blade — the gated getFormActions() is never rendered, so manager/viewer get a raw 403 | `Filament/Admin/Pages/PropertyOverrides:168` | **Designed by the fleet; its test needed repair, proven here (2026-09-05).** Property Overrides shows a Save button to the three roles that may read the screen and may not write it, because the Blade renders a raw submit button and the gated getFormActions() beside it in the PHP is called by nothing — so pressing Save is a bare 403. At HEAD 83624504. `resources/views/filament/pages/property-overrides.blade.php:5-9` is an unconditional `<div class="mt-6"><x-filament::button type="submit">`. `app/Filament/Admin/Pages/PropertyOverrides.php:196-206` declares `getFormActions()` carrying `->authorize(fn () => Auth::user()?->can('settings.manage'))` — and it is DEAD: `PropertyOverrides extends Filament\Pages\Page`, and `grep -n 'use ' vendor/filament/filament/src/Pages/Page.php` shows Page uses CanAuthorizeAccess/HasErrorNotifications/HasRoutes/HasSubNavigation/InteractsW |
| **SW-095** | ✅ **fixed** `7f678b56` | medium | — | Deactivating the configured withholding tax code makes the WHOLE Settings screen unsaveable | `Filament/Admin/Pages/Settings:522` | **Designed by the fleet; its test did not run, repaired and proven here (2026-09-04).** Retiring the withholding tax code that Settings names makes the entire eight-tab Settings screen refuse to save, on a field nobody touched. Probed the real mechanism in tinker at HEAD on 2026-09-03. A `Filament\Forms\Components\Select` on a mounted schema with state `WH_3_P` and options not containing it returns `getOptionLabel(withDefault: false) === null`, `getInValidationRuleValues() === []`, and its validation rules are `['nullable', Rule::in([])]` — i.e. `in:` with an EMPTY list, which rejects every value including the stored one. With the code present in the options the same probe returns `null` from `getInValidationRuleValues()` and emits no `in` rule at all. `app/Models/TaxCode.php:3 |
| **SW-096** | ✅ **fixed** `b31ee637` | medium | — | The Workflows page prints every state name in raw English — Arabic operators read English cells despite three existing status catalogues | `Filament/Admin/Pages/Workflows:92` | **Designed by the 2026-09-03 patch fleet, adversarially reviewed, then applied, tested and mutation-checked here.** The Workflows page renders every state name as raw English typography of the database value even on the Arabic panel, and it gates the whole page on `approvals` — a module key that owns the value-threshold approval ladder and none of the three state machines the page actually draws. BOTH rows confirmed live at HEAD (04f19175). SW-096: `app/Filament/Admin/Pages/Workflows.php:92` is `private function humanize(string $state) { return ucwords(str_replace('_', ' ', $state)); }`, called for `state` and for every member of `to` at lines 105-106 — so on the Arabic panel an operator reads `Awaiting Tenant`, `In Progress`, `Ordered`. Probe: `php -r '$a=require "lang/{en,ar}/admin.php"; ...'` printed `admin.statuses.tenant_request` (7 states), `admin.facility.statuses` (4) and `admin.procurement.statuses` (7) in BOTH locales, and every state key in `TenantRequestService::TRANSITIONS`, `FacilityWork |
| **SW-097** | ✅ **fixed** `7e83d7ed` | medium | S | A terminated employee is a dead-end status: no reactivate/undo action exists on any screen, and the form has no status field | `Filament/Admin/Actions/EmployeeActions:34` |
| **SW-098** | ✅ **fixed** `1acd158f` | medium | M | The Roles & Permissions vocabulary renders in English on the Arabic panel: ~190 checkbox labels, the role-description column, and two role pickers bypass the existing roles_list catalogue | `Filament/Admin/Resources/Roles/Schemas/RoleForm:58` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-04).** The Roles & Permissions screen renders 232 permission labels, 14 role descriptions and both role pickers in the registry's raw English, on the Arabic panel — while the badge column three lines away renders the same role as «مدير عام». At HEAD 83624504. (a) `app/Filament/Admin/Resources/Roles/Schemas/RoleForm.php:58` is `->options($perms)` where `$perms` is `RolesPermissionsSeeder::PERMISSIONS[$module]`, i.e. `key => English sentence` ('Edit a payment rail, including the account its money lands in'). Probe: `php artisan tinker` over the registry counted **52 modules / 232 permissions**, and `trans('admin.permissions', [], 'ar')` returned the literal key — no such catalogue exists in either locale (`grep -rn 'View settings' lang/` → nothing). The SECTION headings are fine: `trans('admin.perm |
| **SW-099** | ✅ **fixed** `e5f1a1ed` | medium | S | canSuspend has a self-guard but no protected-role guard — an hr user can suspend a super_admin | `Filament/Admin/Resources/Users/UserResource:91` |
| **SW-100** | ✅ **fixed** `84ac49cd` | medium | — | A lump-sum payroll run and a payslip run for the same month both approve, double-posting the salary expense | `Models/Payroll:250` | **Confirmed and fixed 2026-09-03.** The 2026-08-20 double-pay guard is on the EMPLOYEE (so a supplementary run stays possible) and a LUMP-SUM run names nobody — a supported shape, since `PayrollForm` unlocks the header money fields exactly when a run has no lines. So it skipped the guard on its own approval AND left nothing for another run's guard to find. Measured: a 3-line run of 12,000 and a lineless run of 12,000, same mall and month, **both approved in either order**, and two lineless runs likewise. `PayrollJournalizer` reads `gross_salaries` from the HEADER, so the ledger carried **24,000 of salaries expense for a 12,000 month** — overstating the wage bill, understating NOI, and crediting the bank for cash that never left. `billing:reconcile` does not look at payroll at all. Fixed with a RUN-level bar when either side has no lines, keeping the per-employee bar (and the supplementary run) when both do. **The property clause was the same hole through a second door** — `where('asset_id', null)` matches nothing, so a consolidated run and a mall run were both approvable; both fixed in one pass. `ConfigurationHealthTest`'s corrective-run fixture approved two lineless runs in one month, i.e. it encoded this defect; it cancels the stale run first now, which is the escape the refusal names. `ANoMonthIsPaidTwiceTest`, 8 cases, 3 mutations |
| **SW-101** | ❌ **refuted** | medium | — | `tenant_users.locale` is read by three places and written by none — the portal ignores a retailer's stated language | `Models/TenantUser:32` | **Measured against HEAD by the 2026-09-03 verification sweep** (69 agents, one per group of related rows; the adversarial challenge phase did NOT run — it died on the session limit — so this verdict is single-source). The row's central claim — "written by none" — is false. `routes/web.php:58-64` writes it: the `/locale/{locale}` switcher loops `foreach (['web','portal'] as $guard)` and calls `$user->forceFill(['locale' => $locale])->saveQuietly()`. Measured, not reasoned: (1) `forceFill` bypasses `$fillable` — probed a fresh `TenantUser`, `forceFill(['locale'=>'ar'])` → `preferredLocale()` returns 'ar', while `fill(['locale'=>'ar'])` → NULL, so the missing fillable entry is irrelevant to this path; (2) the portal guard resolves to this model — `config('auth.guards.portal')` = `['driver'=>'session','provider'=>'tenant_users']`, provider model `App\Models\TenantUser`; (3) the switcher really renders on the  |
| **SW-102** | ✅ **fixed** `1acd158f` | medium | S | hr cannot open Payrolls at all — contradicting the seeder's own stated premise — and the payslip PDF on the employee page is withheld from hr while the figures render | `database/seeders/RolesPermissionsSeeder:882` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-04).** The hr role cannot open Payrolls at all — contradicting the sentence written directly above its own grant — and reads every figure on an employee's payslip while the payslip PDF beside those figures is withheld. At HEAD 83624504. `database/seeders/RolesPermissionsSeeder.php:875-884` grants hr exactly `users.*`, `roles.view`, `departments.view`, `employees.*`, both advance actions and `payroll_rates.view` — nothing from `payrolls.*` — under a comment reading *"HR generates the run and must be able to see what it will compute on."* Probe against the dev database: `hr` holds `payroll_rates.view` and nothing else beginning `payroll`; `accounting` and `manager` each hold `payrolls.view/create/edit/approve`. `PayrollResource` uses `RoleGatedActions`, so `canViewAny()` asks `payrolls.view` — hr's |
| **SW-103** | ✅ **fixed** `b31ee637` | low | — | The portfolio holdover rate is floored at 0 while the action it prefills is floored at 100 — the default can be a value the modal then refuses | `Filament/Admin/Pages/Settings:356` | **Designed by the 2026-09-03 patch fleet, adversarially reviewed, then applied, tested and mutation-checked here.** The portfolio holdover default may be saved below 100% while the conversion modal it prefills refuses anything under 100, so the operator is refused on a field they never touched. Three different floors for one number at HEAD on 2026-09-03. `app/Filament/Admin/Pages/Settings.php:356-362` — `TextInput::make('billing.holdover_default_rate_pct')…->minValue(0)->required()`. `app/Filament/Admin/Actions/LeaseActions.php:830-849` — the conversion modal's `rate_pct` field is `->minValue(100)` under a nine-line comment stating why 100 is the floor ("a rate below it would price overstaying BELOW renewing"), and `:825` prefills that field from `app(BillingSettings::class)->holdover_default_rate_pct`. `app/Services/ConvertLeaseToHoldoverService.php:75-77` — `if ($rate <= 0) throw new InvalidArgumentException('The holdover rate must be greater than zero.')`, raw English, surfaced to the operator beca |
| **SW-104** | ✅ **fixed** `b31ee637` | low | — | Turning off the approval ladder hides the tenant-request and work-order state machines too | `Filament/Admin/Pages/Workflows:49` | **Designed by the 2026-09-03 patch fleet, adversarially reviewed, then applied, tested and mutation-checked here.** The Workflows page renders every state name as raw English typography of the database value even on the Arabic panel, and it gates the whole page on `approvals` — a module key that owns the value-threshold approval ladder and none of the three state machines the page actually draws. BOTH rows confirmed live at HEAD (04f19175). SW-096: `app/Filament/Admin/Pages/Workflows.php:92` is `private function humanize(string $state) { return ucwords(str_replace('_', ' ', $state)); }`, called for `state` and for every member of `to` at lines 105-106 — so on the Arabic panel an operator reads `Awaiting Tenant`, `In Progress`, `Ordered`. Probe: `php -r '$a=require "lang/{en,ar}/admin.php"; ...'` printed `admin.statuses.tenant_request` (7 states), `admin.facility.statuses` (4) and `admin.procurement.statuses` (7) in BOTH locales, and every state key in `TenantRequestService::TRANSITIONS`, `FacilityWork |
| **SW-105** | ✅ **fixed** `1acd158f` | low | — | The contractor panel has no durable language — SetLocale and the switcher route still know only two of the three auth surfaces | `Http/Middleware/SetLocale:39` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-04).** A contractor's language choice is written to the session and nowhere else, because both copies of the guard list still say ['web','portal'] and vendor_contacts has no locale column at all — so it is gone at the next sign-in and no queue or scheduler can ever know it. At HEAD 83624504. `app/Http/Middleware/SetLocale.php:41` is `foreach (['web', 'portal'] as $guard)` and `routes/web.php:57` is the same literal, under a comment saying *"Both panels"*. There are THREE panels: `grep -n 'authGuard' app/Providers/Filament/*.php` gives portal → `portal` and vendor → `vendor`, with admin taking Filament's default (`vendor/filament/filament/src/Panel/Concerns/HasAuth.php:94` → `'web'`), and `config/auth.php:55-58` maps guard `vendor` to provider `vendor_contacts`. `php artisan tinker` → `Schema::get |
| **SW-106** | ❌ **refuted** | low | XS | Terminate modal accepts a termination date before the hire date, silently zeroing gratuity accrual and printing negative tenure | `Filament/Admin/Actions/EmployeeActions:42` | **Measured against HEAD by the 2026-09-03 verification sweep** (69 agents, one per group of related rows; the adversarial challenge phase did NOT run — it died on the session limit — so this verdict is single-source). The CAUSE is real: app/Filament/Admin/Actions/EmployeeActions.php:42-46 is `DatePicker::make('terminated_on')->default(now())->required()->native(false)` — no `minDate`, no `->rule()`, and line 51 writes it with a bare `$record->update()` through no service. Employee::booted() (Employee.php:122-130) has only a base_salary NOT-NULL coercion, no date guard. So a termination date years before the hire date is stored. BOTH stated harms are false. (1) 'silently zeroing gratuity accrual': GratuityService.php:60-62 does return 0.0 when `$end->lte($start)`, but `grep -rn accruedFor app` shows its ONLY caller is `exposure()` at GratuityService.php:97, and exposure queries `Employee::query()->where('s |
| **SW-107** | ✅ **fixed** `1acd158f` | low | XS | Custody View/Edit shows a blank custodian when the employee is no longer active — the options list drops the stored value | `Filament/Admin/Resources/Custodies/Schemas/CustodyForm:28` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-04).** Terminating the person holding a custody does not merely blank the custodian on the custody's edit page — it makes the whole record unsavable, including the purpose and reference that `Custody::saving()` deliberately leaves editable, which is exactly the moment an outstanding custody has to be chased. HEAD = 83624504, filament/filament v4.11.8. `app/Filament/Admin/Resources/Custodies/Schemas/CustodyForm.php:28` is `->options(fn () => self::employeeOptions())`, and `employeeOptions()` (:64-75) is `Employee::query()->active()` narrowed to `TenantScope::currentAssetId()`. `Employee::scopeActive()` (app/Models/Employee.php:117-120) is `where('status','active')`, and `employees.status` defaults to `active` with `terminated` the other value (2026_07_04_140001_create_employees_table.php:29). So a |
| **SW-108** | ✅ **fixed** `1acd158f` | low | S | Creating a department whose name slugs to an existing functional role silently binds membership to that role's full permission set | `Models/Department:235` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-04).** A department whose name slugs onto an existing role does not mint an access role — it adopts that role, so attaching a member hands out the role's whole permission set (measured: `manager` = 225 permissions, a role a non-super_admin may not grant on the user form at all), and detaching strips it from an account that held it in its own right. HEAD = 83624504. `Department::roleName()` returns the slug verbatim (app/Models/Department.php:135-140), `booted()`'s `creating` hook derives the slug and dedupes it against DEPARTMENTS only — `while (static::withTrashed()->where('slug', $slug)->exists())` at :215 — and the `created` hook at :240 does `Role::findOrCreate($department->slug, 'web')` under a comment stating "Created with NO permissions on purpose". `findOrCreate` on a name that already ex |
| **SW-109** | ✅ **closed — stale** | low | XS | The 'nobody is paid twice for the same month' guard is scoped to one asset_id, so a consolidated run and a mall run can both pay the same employee | `Models/Payroll:257` **Already fixed, verified 2026-09-05.** The consolidated-run hole in the payroll double-pay guard was closed on 2026-09-03 in `84ac49cd` with its own regression case and control. Verified rather than taken on trust: the clause is live at `Models/Payroll:278-280` as `->when($payroll->asset_id !== null, …)`, which is exactly the fix — the old bare `where('asset_id', $id)` compiled to `asset_id = null` and matched nothing when the run was the consolidated one |
| **SW-110** | ✅ **fixed** `1acd158f` | low | — | An employee advance can be granted with a future date, posting cash out of a period that has not happened | `Services/GrantEmployeeAdvanceService:34` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-04).** Cash handed to an employee — an advance, or a عهدة — can be dated a month into the future, so the journal entry that pays it out lands in a period that has not happened, while the settlement half of the very same document has refused a future date since F-93. `app/Services/GrantEmployeeAdvanceService.php:34` and `app/Services/GrantCustodyService.php:35` both call `PostingDate::assertOpen(...)`; their settlement twins `app/Services/RecordAdvanceRepaymentService.php:40` and `app/Services/SettleCustodyService.php:40` call `PostingDate::assertNotFuture(...)`. Probe (`php artisan tinker`, HEAD, 2026-09-04): `PostingDate::assertOpen('2027-03-04','advance_date')` returned the Carbon 2027-03-04 (ACCEPTED); `PostingDate::assertNotFuture('2027-03-04','advance_date')` threw `The date cannot be in the |

### Cross-cutting

*0 open.*

| ID | Status | Sev | Fix | What is wrong | Where |
|---|---|---|---|---|---|
| **SW-111** | ✅ **fixed** `08b7a14f` | high | — | A general (no-property) owner request is invisible to every property-restricted operator — and it is the form's DEFAULT | `Filament/Admin/Resources/OwnerRequests/OwnerRequestResource:97` |
| **SW-112** | ✅ **fixed** `08b7a14f` | high | — | The owner can neither read nor answer the conversation thread module 15 built for them | `Filament/Admin/Resources/OwnerRequests/Tables/OwnerRequestsTable:120` |
| **SW-113** | ✅ **fixed** `2b4dd73c` | high | XS | Paymob callback treats only 'captured' as terminal, not the whole received set — a late or replayed decline callback flips a 'reconciled'/'settled' payment to 'failed', silently un-paying the invoice and voiding its GL leg. The REVERSED half of the very same condition was already derived from the model, under a comment reading *"enumerate a set like this by asking the model, not by grepping the diff"*; the RECEIVED half was a literal. Both paths (the fast check and the locked re-check) now ask `Payment::isReceived()` | `Http/Controllers/Paymob/CallbackController:128` |
| **SW-114** | ✅ **fixed** `cbff66d6` | high | — | Applying an SLA penalty never re-derives the work order's cost — recompute() is saveQuietly, so the only hook that calls recomputeCosts() never fires | `Services/ApplySlaPenaltyService:45` |
| **SW-115** | ✅ **fixed** `fb85e218` | high | — | `sales:estimate-missing` runs on the 8th and the chase it is supposed to follow runs on the 10th — the reminder can never fire for any tenant the estimate covers | `routes/console:246` |
| **SW-116** | ✅ **fixed** `1acd158f` | medium | — | Schedule-payout modal defaults to a rail the catalogue may no longer offer | `Filament/Admin/Actions/OwnerStatementRunActions:104` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-04).** Seven money forms state their payment-rail DEFAULT as a hard-coded literal beside options that come from the operator-editable rail catalogue, so retiring a rail leaves the form pre-filled with a code its own picker no longer offers — Filament then cannot label it, renders the field blank, and refuses the submit as invalid on a field nobody touched. HEAD 2026-09-04. Sweep of `app/Filament` (comment-stripped via `token_get_all`) for `PaymentMethod::optionsFor(` / `DepositTransaction::methodOptions(` found **9 rail pickers**, of which **7 carry a literal `->default(`**: LeaseActions.php:208 `'bank'`, OwnerStatementRunActions.php:104 `Disbursement::METHOD_BANK_TRANSFER`, EmployeeAdvancesRelationManager.php:120 and :185 `'cash'`, DepositTransactionForm.php:74 `'bank'`, ExpenseForm.php:85 `'cas |
| **SW-117** | ✅ **fixed** `1acd158f` | medium | — | The Income Statement restores the remembered property AFTER the pin, desyncing its scope caption from the mall it is reporting | `Filament/Admin/Pages/IncomeStatement:70` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-04).** The Income Statement restores the operator's remembered property AFTER the shared scope bar has pinned it to the mall they are standing in, so the pinned, disabled property picker names yesterday's mall over figures that were clamped back to today's. HEAD 2026-09-04. `app/Filament/Admin/Pages/Concerns/ScopesLedgerReport.php:138` calls `ReportPreferences::restore($this)` and `:141` then pins — `$this->assetId = TenantScope::currentAssetId() ?? $this->assetId;` — under a comment stating the pin must be the last word. `app/Filament/Admin/Pages/IncomeStatement.php:84` calls `hydrateLedgerScopeFromQuery()` and `:95` calls `ReportPreferences::restore($this)` a SECOND time, after the pin. Probe (`php artisan tinker`): `ReportParameters::parametersOf(IncomeStatement::class)` = comparison, spread,  |
| **SW-118** | ✅ **fixed** `1acd158f` | medium | — | A duplicate custom-field key is a raw duplicate-key 500 — the DB has UNIQUE(model,key) and no layer above it validates | `Filament/Admin/Resources/CustomFields/Schemas/CustomFieldForm:47` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-04).** Adding a second custom field on a key a record type already carries is a raw duplicate-key 500 — the table has UNIQUE(model, key) and neither the form nor the model asks — and if the existing field has been switched off, the operator cannot even see why. HEAD 2026-09-04. `database/migrations/2026_08_23_820000_a_custom_field_is_a_row_the_operator_defines.php` declares `$table->unique(['model', 'key'])`. `app/Filament/Admin/Resources/CustomFields/Schemas/CustomFieldForm.php:47-58` — the `key` TextInput carries `required()`, `maxLength(64)`, `rule('regex:/^[a-z][a-z0-9_]*$/')`, `helperText`, `disabled(fn (?CustomField $record) => $record !== null)`, `dehydrated()`, `default(null)` and **no uniqueness rule**; `grep -c 'unique' app/Filament/Admin/Resources/CustomFields/Schemas/CustomFieldForm.p |
| **SW-119** | ✅ **fixed** `1acd158f` | medium | — | Document-wording uniqueness never fires for the house row: the scope Radio's blank state is '' not null, and MySQL's unique index does not cover NULL | `Filament/Admin/Resources/DocumentTemplates/Schemas/DocumentTemplateForm:38` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-04).** Two house-default wording rows can exist for one invoice block, so which footer a tenant's invoice carries is decided by whichever row the database happens to return first — the form's uniqueness rule compares `asset_id = ''` (which never matches NULL) and the unique index cannot see NULLs either. HEAD 83624504. (1) `app/Filament/Admin/Resources/DocumentTemplates/Schemas/DocumentTemplateForm.php:38-41` scopes its `Rule::unique` with `$get('asset_id')`; that field is `PropertyField::scope()` — `app/Support/Filament/PropertyField.php`, a `Radio` with `->default('')` and `formatStateUsing(fn ($state) => filled($state) ? (string) $state : '')` — so the house row submits the STRING `''`. Probe (tinker, dev MySQL `mall_management`): `DB::table('document_templates')->where('key','invoice.footer') |
| **SW-120** | ✅ **fixed** `c00e0143` | medium | — | `format('M Y')` puts English months inside the Arabic panel — in a required picker's options and inside a translated sentence *(reported by 2 independent agents)* | `Filament/Admin/Resources/OwnerStatementRuns/Pages/ListOwnerStatementRuns:42` **Closed 2026-09-05 by `c00e0143` — it is the same line as SW-028.** Both rows point at `ListOwnerStatementRuns:42`, and the SW-028 fix routed it through `->locale(app()->getLocale())->isoFormat('MMM YYYY')`; `grep -c "format('M Y')"` on that file is now **0**. Two independent agents reporting one line as two rows is the duplicate this sweep's numbering cannot see, and it is worth noting that the SW-028 pass found *four* sites of this defect where these two rows between them named one — the gate added there (`ALodgeSeriesPreviewNamesItsMonthsInTheReadersLanguageTest`, a from-disk sweep of everything the panel renders) is what closes the class rather than either row |
| **SW-121** | ✅ **fixed** `741f879d` | medium | — | "View working" renders the itemised owner P&L as one run-on line | `Filament/Admin/Resources/OwnerStatementRuns/Tables/OwnerStatementRunsTable:36` **Fixed 2026-09-05 (`741f879d`).** *View working* joined the owner's itemised P&L with `\n` into one `TextEntry` — and a newline is HTML whitespace, so the document an owner checks their payment against rendered as one run-on line, the only P&L in the app shown as a paragraph. Array state + `listWithLineBreaks()` (the contract Filament's `<ul>` branch is selected by), each side now under its own FROZEN total (the subtotal rows the PDF always carried — the panel showed accounts and a net with nothing to add up in between), expenses PARENTHESISED as the PDF prints them, and the locale ladder over `income_breakdown` reduced from two copies to `OwnerStatementRun::breakdownRows()` — *which language an account name reads in* had two spellings on the one document a dispute would be settled on. 7 cases, 3 mutations |
| **SW-122** | ✅ **fixed** `b31ee637` | medium | S | A retired catalogue code bricks the record's edit form: Filament's In rule refuses every save of a violation whose category (or charge code whose tax code) was deactivated | `Filament/Admin/Resources/Violations/Schemas/ViolationForm:48` | **Designed by the 2026-09-03 patch fleet, adversarially reviewed, then applied, tested and mutation-checked here.** Half of this row is already fixed and the other half is live: retiring a TAX code makes every record naming it unsavable, because `TaxCode` is a retire-able catalogue that `CatalogueAwareSelect` cannot see — it derives only from `ValueSets::catalogueWidenedColumns()`, and `tax_code` is deliberately not a `ValueSets` set. Confirmed against HEAD (04f19175), and the row is HALF STALE — say so when closing it. The VIOLATION half named in the row is already fixed: `git log app/Support/Filament/CatalogueAwareSelect.php` shows `bae3a170` ("retiring a code made every record already carrying it unsavable") plus review commit `50a4b704`; `AppServiceProvider.php:133` binds `Filament\Forms\Components\Select` to `CatalogueAwareSelect`, `violations.category` is in `ValueSets::CATALOGUE_WIDENED` (ValueSets.php:590), and `ViolationForm.php:48` is a plain `Select::make('category')` which therefore resolv |
| **SW-123** | ✅ **fixed** `1acd158f` | medium | — | CustomField::model is not immutable — only `key` is refused at the model, while the docblock and module doc say both are | `Models/CustomField:105` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-04).** A custom field's `model` can be changed after the row exists, which strands every answer already recorded under that key AND empties the definition's deletion blockers — so the field then becomes freely deletable and the orphaning becomes permanent. HEAD 83624504. `app/Models/CustomField.php:105-107` refuses `isDirty('key')` and says nothing about `model`. Three places state that both are frozen: the class docblock ("The key is immutable" section, which names `key` only — the weakest of the three), `app/Filament/Admin/Resources/CustomFields/Schemas/CustomFieldForm.php:18-23` ("Two fields are disabled once the row exists ... `CustomField::saving()` refuses the change too; this is the UI half, and the model is the gate"), and `docs/modules/38-custom-fields.md:62-64` verbatim: "`custom_fields |
| **SW-124** | ✅ **fixed** `1acd158f` | medium | — | Un-applying an SLA penalty does not lock the bill, unlike applying one — concurrent detach/waive leaves the payable understated | `Services/ApplySlaPenaltyService:57` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-04).** Un-applying an SLA penalty (detach, and its twin waive) rewrites the vendor bill's whole derived balance from every applied penalty on it, but reaches the bill through the plain relation instead of a locking read — so a release racing an apply on the same bill writes a balance with the other penalty's deduction erased, overstating the payable by exactly that penalty. CONFIRMED at HEAD 83624504. `app/Services/ApplySlaPenaltyService.php:67` — `$bill = $locked->bill;` is a PLAIN read, and `:75` then calls `$bill?->refresh()->recompute()`. `VendorBill::recompute()` (app/Models/VendorBill.php:248-260) re-derives `penalty_applied_amount` as `SUM(amount)` over EVERY `applied` penalty on the bill, then `balance` and `status` from it — a whole-bill write. The SAME hole is in `app/Services/AssessSla |
| **SW-125** | ✅ **fixed** `1acd158f` | medium | S | Recorded refunds and forfeits stay fully editable after the final account is frozen — the ChangeImpact registry's 'already had the freeze' claim covers receipts only | `Support/ChangeImpact:236` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-04).** A recorded refund or forfeit stays fully editable after the move-out has been settled, so the figure on the immutable statement the tenant signed can be retyped afterwards — shrinking a refund re-inflates `depositHeld()` by the difference and lets that phantom pot fund a second payout. CONFIRMED at HEAD 83624504, reproduced end to end on an in-memory sqlite replay (probe: migrate → asset/unit/tenant/lease → 100,000 receipt → terminate → `SettleMoveOutService::settle()` with no arrears and no deductions). Result: refund row written for **100,000**, `Lease::depositHeld()` **0**, one settlement `LeaseEvent` (`payload->settlement`). Then `$refund->update(['amount' => 10000])` was **ACCEPTED**, `depositHeld()` climbed back to **90,000**, and a second 90,000 refund against that phantom pot was a |
| **SW-126** | ✅ **fixed** `741f879d` | low | — | The shared evidence field accepts images only and caps nothing, while the admin form on the same collection accepts PDFs and caps size and count | `Filament/Actions/EvidenceUpload:45` **Fixed 2026-09-05 (`741f879d`).** One `evidence` collection, three doors, two answers: the work-order form took `image/* + application/pdf` capped at 10 MB × 10, while `EvidenceUpload` — the factory behind the operator's *Attach evidence* button AND the contractor's portal verb — said `->image()` and capped **nothing**: the only uncapped upload in the app, on a private disk written to by an external contractor, with Livewire's 12 MB temporary default as the entire bound. So the signed hot-work permit was takeable on the edit form and refused at the button named for it and at the contractor's own door — the door the vendor portal exists for. `EvidenceUpload::accepting()` is the one answer, composed by all three; removal stays per-door because *absent means removed* is true on a form hydrated from the record and false in a modal that opens empty. Driven through the real modals; the shared field does not `preserveFilenames()`, so the probe is the MIME. 5 cases, 3 mutations |
| **SW-127** | ✅ **fixed** `1acd158f` | low | XS | Property form accepts leasable area greater than gross area — the pair has no cross-field rule | `Filament/Admin/Resources/Assets/Schemas/AssetForm:80` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-04).** The property form takes gross building area and leasable area as two unrelated numbers, so a mall can be recorded as letting more space than it contains — the load factor then prints over 100% and CAM under-recovers from every tenant, because the inflated figure is the recovery's own denominator. CONFIRMED at HEAD 83624504. `app/Filament/Admin/Resources/Assets/Schemas/AssetForm.php:72-82` — both `total_area_sqm` and `leasable_area_sqm` carry only `->numeric()->minValue(0)->suffix('m²')`; neither carries `->rules(`, `->lte(` or `->gte(`, and there is no model-level guard (`grep -rn leasable_area_sqm app/` finds fillable/cast, the form, an export column, `Asset::leasableEfficiencyPct()` and `CamReconciliationService:656`). Reproduced on an in-memory replay: `Asset::create([... 'total_area_sq |
| **SW-128** | ✅ **fixed** `1acd158f` | low | XS | violation_date and category stay editable after the fine is billed, diverging the record from its issued invoice | `Filament/Admin/Resources/Violations/Schemas/ViolationForm:103` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-04).** Once a violation's fine has been billed, its category and its date are still editable — and both are baked into the issued invoice (the line text quotes the category label and the date; the invoice's period is that date's own month), so an edit leaves the register and the document the tenant is holding describing two different breaches with nothing on either to say so. `app/Services/BillViolationFineService.php:100-121` — the invoice's ONLY line is `__('admin.violations.fine_line', ['reference' =>…, 'category' => ViolationCategory::labelFor($locked->category), 'date' => $locked->violation_date->isoFormat('D MMM YYYY')])`, and `$periodStart`/`$periodEnd` are `$locked->violation_date->startOfMonth()/endOfMonth()`. `app/Filament/Admin/Resources/Violations/Schemas/ViolationForm.php` freezes ex |
| **SW-129** | ✅ **fixed** `1acd158f` | low | S | Global search details print the raw violation category code instead of its label | `Filament/Admin/Resources/Violations/ViolationResource:147` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-04).** The global-search result for a violation prints the raw catalogue code ("safety") where the list beside it prints the operator's own label — and the identical line exists on the expense register, so the same defect ships twice: an operator who renames a house rule or a cost type sees the new name everywhere except in the search results. `app/Filament/Admin/Resources/Violations/ViolationResource.php:146` is `__('admin.fields.category') => $record->category,` while `app/Filament/Admin/Resources/Violations/Tables/ViolationTable.php:41` renders the same column as `->formatStateUsing(fn (?string $state) => ViolationCategory::labelFor($state))`. Swept all 23 `getGlobalSearchResultDetails()` overrides under `app/Filament`: exactly TWO print a live `IsCodeCatalogue`-governed code raw — `ViolationR |
| **SW-130** | ✅ **fixed** `1acd158f` | low | — | Two registries discover screens from a hardcoded Admin+Portal list, so the vendor panel is swept by neither | `Support/ScreenGuides:393` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-04).** Both registries that force a decision about a new screen discover from a hardcoded Admin+Portal directory list, so the contractor panel added on 2026-08-28 is swept by neither — and what that blindness is hiding is real: the vendor panel ships a global search box its own docblock says it does not have, over the ONLY raw-column global search left in the application. `app/Support/ScreenGuides.php:393` iterates `['Admin/Resources', 'Admin/Pages', 'Portal/Resources', 'Portal/Pages']`; `tests/Feature/Scenarios/SearchPolicyConformanceTest.php:43` iterates `[app_path('Filament/Admin/Resources'), app_path('Filament/Portal/Resources')]`. `app/Providers/Filament/VendorPanelProvider.php:56` discovers a third namespace. Measured at HEAD by iterating `Filament::getPanels()` (admin 66 resources, portal  |

### General ledger · period · banking

*0 open.*

| ID | Status | Sev | Fix | What is wrong | Where |
|---|---|---|---|---|---|
| **SW-131** | ✅ **fixed** `d1db794f` + review | high | — | A Form 41 quarter cannot survive a link or a saved view — the screen shows the full year while the emailed CSV shows the quarter | `Filament/Admin/Pages/Concerns/ScopesLedgerReport:67` |
| **SW-132** | ✅ **fixed** `aa624ab1` | high | — | Clearing any report Select unsets a non-nullable Livewire property and 500s the page Same root cause as SW-037 and fixed with it. | `Filament/Admin/Pages/Concerns/ScopesLedgerReport:130` |
| **SW-133** | ✅ **fixed** `18d19704` | high | — | The unallocated-entries warning exists only on screen — the PDF, CSV, XLSX, scheduled email and owner pack omit money silently | `Filament/Admin/Pages/Concerns/ScopesLedgerReport:270` |
| **SW-134** | ⚠️ **piece (a) fixed** `acd3ae89` | high | L | Re-pointing a posting-role mapping (or a charge code's posting_role) retroactively re-derives the entire ledger history: the weekly --all sweep mass-voids/re-posts every open-period document and permanently str | `Services/Accounting/LedgerPoster:506` | **Mechanism confirmed at HEAD; the WARNING is in, the policy is not.** Accounts resolve at PAYLOAD time and are never frozen onto the entry, and `LedgerPoster::matches()` includes `ledger_account_id` in its line signature — so re-pointing a mapping makes the next `accounting:sync-ledger` sweep void and re-post every historical document that used it, up to a week later, unconfirmed. Nothing gated it: the model guards duplicates and the deletion of a global default, `SealedPeriod` returns early because `AccountMapping` is not a GL source, and `ChangeImpact` classifies the columns of SOURCES rather than of a configuration table. Measured on the QA baseline: **487 posted lines on `accounts_receivable`**. **(a) DONE** — `App\Support\PostingRoleExposure` counts what is posted and splits it OPEN vs CLOSED on the mapping form. That split is the substance: an open-period entry re-derives and the books stay coherent; a closed-period one CANNOT, so it keeps the old account while the mapping says otherwise and `billing:reconcile --deep` reports drift for ever — which turns `atriom:preflight` permanently red and blocks the next deploy for an unrelated reason. Counts `REPORTABLE_STATUSES`, not `posted` alone (a void and its replacement are both history). Silent when nothing is posted, because a warning on a fresh install is one trained away before the one that matters. `ARepointedPostingRoleSaysWhatItWillDoTest`, 3 mutations. **(b) and (c) STAY OPEN and are recorded as SW-236/237** — it warns, it does not refuse, deliberately: whether a mapping change should be PROSPECTIVE is an accounting decision nobody has taken, and refusing on the operator's behalf would be taking it |
| **SW-135** | ✅ **fixed** `d7ff909d` | high | S | On estimate_basis='billed', a unit owner's estimated_paid is always 0 — the owner is billed his full annual share again despite a year of paid assessments. The participant filter was `whereIn('invoices.lease_id', …)` and an assessment invoice carries a NULL `lease_id`. The ownership branch is now grouped beside it, off ONE participant definition (`CamExpensePool::participantOwnershipQuery()`) that the allocator reads too, so the two cannot drift again | `Services/SyncCamPoolFromLedgerService:278` |
| **SW-136** | ✅ **fixed** `1acd158f` | medium | S | Reopen-period row action bypasses the year-end close: postings land under a standing closing entry | `Filament/Admin/Resources/AccountingPeriods/Tables/AccountingPeriodsTable:104` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-04).** Reopening a single month from the periods row action does not consult the year-end close, so an entry posted into that month lands under a closing entry that has already been computed — and because `YearEndCloseService::close()` is idempotent, that P&L movement can never be rolled into retained earnings by re-closing: equity is understated for that year permanently, with the books still balancing. `PeriodService::reopenPeriod()` (app/Services/Accounting/PeriodService.php:34-39) is a bare `update(['status' => 'open'])` with no gate; the row action at `app/Filament/Admin/Resources/AccountingPeriods/Tables/AccountingPeriodsTable.php:113` calls it unguarded, unlike `close_period` three lines above which catches the close gate's refusal. `JournalPostingService::assertOpenPeriodFor()` (:369-378) |
| **SW-137** | ✅ **verified fixed** | medium | S | Two bank accounts can point at the same ledger account — and with the shipped chart's single bank leaf, that is the default outcome | `Filament/Admin/Resources/BankAccounts/Schemas/BankAccountForm:46` | **Measured against HEAD by the 2026-09-03 verification sweep** (69 agents, one per group of related rows; the adversarial challenge phase did NOT run — it died on the session limit — so this verdict is single-source). Fixed by `78ad4550` (2026-09-02) "each bank account gets a chart account of its own" — one day after the sweep was written, plus `39cae155` extracting the minting service. - `BankAccount::booted()` (BankAccount.php:106-108) → `assertLedgerAccountIsItsOwn()` (BankAccount.php:166-189) refuses, on a dirty write, both (a) a chart account already held by another bank (`withTrashed()->whereKeyNot()`) and (b) any account carrying an `AccountMapping` posting role — which is the "shipped chart's single bank leaf" half of the claim. - `BankAccountForm:91-93` now `->suggest()`s only accounts taken by neither, and `createOptionForm`/`createOptionUsing` (:99-114) mint a dedicated leaf via `MintBankLedger |
| **SW-138** | ✅ **fixed** `1acd158f` | medium | S | A matched bank statement can be re-homed to another bank account, stranding matches against the wrong bank | `Filament/Admin/Resources/BankStatements/Schemas/BankStatementForm:27` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-04).** A bank statement whose lines are already matched can be re-pointed at a different bank account, which leaves the old bank's postings looking explained by a statement that no longer belongs to it and the new bank's statement looking explained by postings that never touched it — both reconciliations wrong, both still balancing. `app/Filament/Admin/Resources/BankStatements/Schemas/BankStatementForm.php:27` — the resource's ONE form serves Create and Edit (`BankStatementResource::form()`), so `bank_account_id` is an ordinary `EntitySelect` on a statement that has already been reconciled. `app/Models/BankStatement.php` has NO `booted()` at all (grepped: zero `static::updating` / `static::saving`), no `ChangeImpact` entry (BankStatement is not a GL source) and no `SealedPeriod` reach — so `$stat |
| **SW-139** | ✅ **fixed** `1acd158f` | medium | XS | Opening-balances preview reads $account?->name — a column ledger_accounts does not have — so account names never render and every draft line's description is null | `Services/Accounting/ImportOpeningBalancesService:96` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-04).** Every row of an opening-balance preview came back with no account name, and every line of the draft journal entry it writes was stored with a null description — because the service read a column the chart does not have. `app/Services/Accounting/ImportOpeningBalancesService.php:96` builds each preview row with `'name' => $account?->name`. `ledger_accounts` has NO `name` column — `database/migrations/2026_06_30_000001_create_ledger_accounts_table.php:20-21` creates `name_en` and `name_ar`, and `app/Models/LedgerAccount.php` declares no `name` accessor, no `$appends` and no `getNameAttribute()` (grepped). Eloquent answers null for an attribute that is not present rather than throwing, so every parsed row carries `name => null`. Two silent consequences, both confirmed by reading the consumers: |
| **SW-140** | ✅ **fixed** `1acd158f` | medium | — | The trial-balance PDF ignores "Include zero balances" — the printed copy cannot answer the question the toggle exists for | `Services/Accounting/LedgerReportPdfService:22` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-04).** Ticking "Show accounts with no movement" on the trial balance changes the screen and the CSV and does nothing to the PDF, so the printed copy — the one an accountant ticks off against — silently answers a different question from the one on screen. `app/Services/Accounting/LedgerReportService.php:47` takes `bool $includeZeroBalances = false` and appends `accountsWithNoMovement()` when it is true. `app/Filament/Admin/Pages/TrialBalance.php:143` declares the public `$includeZeroBalances`, :152 renders the toggle, and :167 passes it into `report()` — which `reportCsv()` (:120) also goes through, so the CSV and the scheduled delivery honour it. The PDF does not: `app/Filament/Admin/Pages/TrialBalance.php:88-95` calls `LedgerReportPdfService::trialBalance()` with five positional arguments, and ` |
| **SW-141** | ✅ **fixed** `1acd158f` | medium | XS | Period-close gate part (b) is blind to SLA penalties applied on the period's last day: whereBetween compares the datetime applied_at against date bounds that bind as midnight | `Services/Accounting/PeriodService:102` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-04).** An SLA penalty applied at any time of day on the last day of an accounting period was invisible to the period-close gate, so the period could be closed underneath it and its journal entry stranded for ever — the vendor keeps the deduction on their bill and the ledger never records it. `app/Services/Accounting/PeriodService.php:102` scans each source with `whereBetween($dateColumn, [$period->starts_on, $period->ends_on])`. `AccountingPeriod` casts both bounds `'date'` (`app/Models/AccountingPeriod.php:34-35`), so they bind as datetimes at midnight — compiled the query at HEAD through tinker and got, verbatim: `select * from \`sla_penalties\` where \`applied_at\` between '2026-08-01 00:00:00' and '2026-08-31 00:00:00'`. I then checked the column type of all 24 entries in `LedgerRealtimeSync: |
| **SW-142** | ✅ **fixed** `71227984` | medium | S | Bank statement CSV amount parser turns thousands-separated integer amounts into fractions: '1,234,567' imports as 1.234 and '12,500' as 12.50 | `Services/Banking/ImportBankStatementService:189` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** A bank statement line reading '1,234,567' imported as 1.234 — and the same defect, pointing the other way, lives in the two byte-identical amount parsers behind the opening-balance and budget imports, so one seam (App\Support\CsvAmount) closes all three. HEAD, 2026-09-04. `ImportBankStatementService::toFloat()` (app/Services/Banking/ImportBankStatementService.php:173-196) reads a lone comma as a decimal point ALWAYS. Probe (php -r, re-implementing the method verbatim): '1,234,567' => 1.234 · '12,500' => 12.5 · '(1,250)' => -1.25 · '1.234,56' => 1.23456 (the European form its own docblock at :173 claims to handle). `ImportOpeningBalancesService::amount()` (:230-236) and `BudgetService::amount()` (:255-260) are byte-identical (`preg_replace(' |
| **SW-143** | ✅ **fixed** `71227984` | medium | M | GL tie-out and deposits tie-out read only the GLOBAL control-account mapping, so one mall-scoped accounts_receivable/accounts_payable/deposits_held override row makes books_tie_out permanently red (or masks rea | `Services/Reconciliation/BooksReconciliationService:383` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** Both all-time tie-outs read only the GLOBAL control-account mapping while every journalizer resolves the role per property, so one per-mall accounts_receivable / accounts_payable / deposits_held override makes books_tie_out permanently red and blocks the next atriom:preflight. HEAD, 2026-09-04. `BooksReconciliationService::glTieOut()` (app/Services/Reconciliation/BooksReconciliationService.php:398-399) calls `$this->accounts->account('accounts_receivable')` with NO asset id, and `AccountResolver::account()` (app/Services/Accounting/AccountResolver.php:18-31) then reads the `whereNull('asset_id')` row only. `DepositHoldings::glBalance()` (app/Support/DepositHoldings.php:176) does the same via `$assetIds ?? [null]`. Meanwhile all six AR journ |
| **SW-144** | ✅ **fixed** `71227984` | medium | M | Deactivating a chart account still referenced by a bank account or payment rail makes the weekly full-history ledger sweep void-and-repost every historical entry to the generic floor account, stranding bank mat | `Support/MoneyAccount:104` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** Deactivating (or un-posting) a chart account that a bank account, payment rail or expense category still points at silently re-homes every entry ever posted through it onto the generic floor account on the next weekly sweep — and deactivating is exactly what the deletion guard tells the operator to do instead of deleting. HEAD, 2026-09-04 — traced by reading, not reproduced (the sweep was not run). Three tiers re-ask `is_postable && is_active` at posting time and fall through to the floor: `MoneyAccount::ledgerAccountOf()` (app/Support/MoneyAccount.php:100-110, `->where('ledger_accounts.is_postable', true)->where('ledger_accounts.is_active', true)`), `PaymentMethod::accountIdOrFloor()` (app/Models/PaymentMethod.php:246-262) and `ExpenseCate |
| **SW-145** | ✅ **fixed** `741f879d` | low | XS | Posting-map role helper reads $get('key') but the key Select is not live — the 'expects a {group} account' hint never appears while choosing | `Filament/Admin/Resources/AccountMappings/Schemas/AccountMappingForm:34` **Fixed 2026-09-05 (`741f879d`).** Measured by reflecting on the BUILT schema: `isLive` null on both posting-role pickers, falling through the Section to the root Schema's false — so the helper naming the expected account group (*"Normally points at a Revenue account"*) reached the server only after a SAVE, about a decision already taken. The sweep's code-and-test-disagree note resolved to a false alarm: nothing anywhere asserted the hint. Both the posting map's `key` and `ChargeCodeForm`'s `posting_role` fixed — the identical defect one file away, and fixing one is how two halves of one screen drift. Testing note recorded: Filament v4.11 has NO `getHelperText()` reader (`helperText()` composes into `belowContent()`), so the rendered page is the only honest probe. 3 cases, 2 mutations |
| **SW-146** | ✅ **fixed** `1acd158f` | low | XS | Posting-map list shows the chart in Arabic to every locale — the same defect already fixed in this screen's own picker | `Filament/Admin/Resources/AccountMappings/Tables/AccountMappingsTable:37` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-04).** The posting map's account column prints `account.name_ar` to every reader, so an operator working the panel in English reads the chart of accounts in Arabic on the one screen that decides where money posts. app/Filament/Admin/Resources/AccountMappings/Tables/AccountMappingsTable.php:37 is `TextColumn::make('account.name_ar')` with no locale handling — confirmed at HEAD, `grep -c` = 1. The fix the row refers to IS already in this screen's picker: AccountMappingForm:48-52 carries the comment "the account name came from `name_ar` regardless of the reader's language… `LedgerAccount::displayName()` answers for the active locale", and the table was left behind. `LedgerAccount::displayName()` (app/Models/LedgerAccount.php:158) is the existing one seam. Measured on the dev database (`mall_manageme |
| **SW-147** | ✅ **fixed** `1acd158f` | low | XS | Accounting-periods View action opens an empty modal — the resource form it renders has no fields | `Filament/Admin/Resources/AccountingPeriods/AccountingPeriodResource:57` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-04).** The accounting-period register's View button opens a modal with a heading, a Close button and nothing between them — the resource declares a `form()` that returns the schema untouched, and Filament renders a ViewAction from that same empty schema. app/Filament/Admin/Resources/AccountingPeriods/AccountingPeriodResource.php:57-61 is `public static function form(Schema $schema): Schema { // Periods are seeded, not editable — no form. return $schema; }`. Filament resolves a resource ViewAction's schema at vendor/filament/filament/src/Resources/Pages/ListRecords.php:95 — `$this->infolist($this->form($schema))` — and `Resource::infolist()` (vendor/filament/filament/src/Resources/Resource.php:63) also returns the schema untouched, so the modal has zero components. Swept every `*Resource.php` unde |
| **SW-148** | ✅ **fixed** `1acd158f` | low | XS | Bank-line matching workspace re-queries coverage 3-4 times per row, ignoring the eager load it sets up | `Filament/Admin/Resources/BankStatements/RelationManagers/LinesRelationManager:82` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-04).** `MatchBankStatementLineService::coverage()` re-queries a line's matches every time it is asked, and the bank-reconciliation workspace asks it four times per row on top of an eager load that already fetched the answer. app/Services/Banking/MatchBankStatementLineService.php:147 is `foreach ($line->matches()->with('journalLine')->get() as $match)` — an unconditional relation query, two statements per call (one for `bank_matches`, one for the `journalLine` eager load). Counted from the call sites rather than from data, because the dev database holds zero `bank_statement_lines` and zero `bank_matches` (`select count(*)` on `mall_management`, 2026-09-04): `LinesRelationManager` calls `coverage()` at lines 83 (the age cell), 95 (the match-state cell), 109 (that cell's colour) and 186 (the Match a |
| **SW-149** | ✅ **fixed** `1acd158f` | low | S | Chart-of-accounts import completion notification is hardcoded English | `Filament/Imports/LedgerAccountImporter:131` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-04).** Every bulk import and export tells the operator it finished in hardcoded English, under a heading Filament renders in Arabic — one notification in two languages, on the screen that says whether a cut-over worked. app/Filament/Imports/LedgerAccountImporter.php:146 builds the body as an English literal. It is not one call site: `grep -rn getCompletedNotificationBody app/` returns 19 — all ten importers (Charge, Employee, FixedAsset, Lease, LedgerAccount, MeterReading, OpeningInvoice, Tenant, Unit, Vendor) and all nine exporters (Asset, CreditNote, Invoice, Lease, Payment, Tenant, TenantRequest, Unit, Vendor) — each holding its own copy of the same three clauses, and not one of them calls `__()`. The other half of the SAME notification IS translated: Filament's `Importer::getCompletedNotifica |
| **SW-150** | ✅ **fixed** `71227984` | low | S | Auto-reversal entries store untranslatable mixed-language prose with no narrative key: the Arabic ledger renders 'قيد عكسي للقيد JE-0519 — Superseded by an updated document.' | `Services/Accounting/JournalPostingService:211` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** Every reversal the ledger posts itself carries an English sentence in its Arabic column, so an Arabic accountant reads 'قيد عكسي للقيد JE-0519 — Superseded by an updated document.' on the general ledger, and no wording fix can ever reach a row already posted. CONFIRMED at HEAD. `app/Services/Accounting/JournalPostingService.php:211-212` builds the narrative by concatenation — `'قيد عكسي للقيد '.$entry->number.($reason ? ' — '.$reason : '')` — and writes NO `description_key`/`description_data`, although `JournalPostingService::post()` at line 106-108 does (EG-36). Grepped the callers: exactly four reach it, three of them machine — `LedgerPoster.php:233` ('Document no longer has a ledger effect.'), `LedgerPoster.php:244` ('Superseded by an up |

### Portals · mobile API

*0 open.*

| ID | Status | Sev | Fix | What is wrong | Where |
|---|---|---|---|---|---|
| **SW-151** | ✅ **fixed** `b2958da3` | high | — | An announcement's expires_at is unvalidated, so a broadcast can deep-link every tenant to a 404 — and the record is immutable the moment it sends | `Filament/Admin/Resources/Announcements/Schemas/AnnouncementForm:124` |
| **SW-152** | ✅ **fixed** `bae3a170` | high | — | Retiring a catalogue code makes every record already carrying it permanently unsavable — the half of the deposit bug that was never fixed | `Filament/Admin/Resources/TenantRequests/Schemas/TenantRequestForm:139` |
| **SW-153** | ✅ **fixed** `92681f33` | high | — | A unit owner cannot raise a tenant request: the screen is offered, the required Unit picker has zero options | `Filament/Portal/Resources/TenantRequests/Schemas/TenantRequestForm:59` |
| **SW-154** | ✅ **fixed** `ce038ed8` | high | — | `GET /me/statement?to=` prints the window it was asked for and ignores it — the statement lists rows after its own stated end date | `Services/TenantStatementPdfService:98` |
| **SW-155** | ✅ **fixed** `71227984` | medium | — | A tenant request's zone silently goes stale when its unit is corrected on the Edit page, under a field labelled "auto" | `Filament/Admin/Resources/TenantRequests/Schemas/TenantRequestForm:227` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** Correcting a tenant request's unit on the Edit page leaves its facility zone pointing at the shop it used to be about — silently, under a field the form renders disabled and labelled "auto" — and the identical defect is on the work order that dispatches the technician. CONFIRMED at HEAD, and it is TWO instances, not one. `app/Filament/Admin/Resources/TenantRequests/Schemas/TenantRequestForm.php:227` renders `EntitySelect::make('area_id')->disabled()->dehydrated(false)->placeholder(__('admin.fields.area_auto'))` under a comment saying "the derivation owns the value, the form only surfaces it". The derivation is `TenantRequest::booted()` line 233-237 — **`static::creating` only**. `unit_id` on the same form (line 99) is freely editable on edi |
| **SW-156** | ✅ **fixed** | low | — | The two overdue scans re-check their stamp under the lock but not the balance, so a payment landing mid-run produces a dunning notice on a settled invoice | `Console/Commands/RemindOverdueTenantsCommand:99` |
| **SW-157** | ✅ **fixed** `71227984` | low | — | Two table columns named read_at: the read/unread tick on the announcement recipient list is silently dropped | `Filament/Admin/Resources/Announcements/RelationManagers/RecipientsRelationManager:58` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** The announcement recipient list declares two columns called read_at, so Filament silently drops the read/unread tick — on the one screen whose purpose is that "we told you" is a record rather than an assertion. CONFIRMED at HEAD. `RecipientsRelationManager.php:58` declares `IconColumn::make('read_at')` and line 67 declares `TextColumn::make('read_at')`. `vendor/filament/tables/src/Table/Concerns/HasColumns.php:84` is `$this->columns[$component->getName()] = $component;` and `getVisibleColumns()` (line 121) derives from that map, which `index.blade.php:32` renders — so the second declaration replaces the first and the tick never reaches the page. No error, and both declarations read correctly in isolation. I swept the whole class with a toke |
| **SW-158** | ✅ **fixed** `71227984` | low | — | Announcements ship a TrashedFilter with no Restore or ForceDelete anywhere — a soft-deleted notice is a dead end | `Filament/Admin/Resources/Announcements/Tables/AnnouncementsTable:123` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** Deleting an announcement soft-deletes it — which retracts the notice from every tenant's portal and mobile feed — and no screen anywhere could restore it, so an accidental deletion was permanent from the panel. Measured at HEAD. (1) `grep -rl TrashedFilter app/Filament` → 33 files, including `app/Filament/Admin/Resources/Announcements/Tables/AnnouncementsTable.php:123`; `grep -rl RestoreAction app/Filament` → 15 files, all Edit pages, and Announcements is not among them. So a trashed notice is findable (the filter's "only trashed" branch) and not recoverable. (2) `app/Models/Announcement.php:51-56` — `#[DeletionAllowed]` + `SoftDeletes`, and `AnnouncementResource::canDelete()` → `DeletionPolicy::resourceMayDelete()` → super_admin, with no s |

### CAM · recoveries · sales

*0 open.*

| ID | Status | Sev | Fix | What is wrong | Where |
|---|---|---|---|---|---|
| **SW-159** | ✅ **fixed** `8d11faff` | high | XS | A disputed sales declaration cannot be re-locked from any screen — the void/correct/re-bill loop is a dead end | `Filament/Admin/Actions/SalesDeclarationActions:191` |
| **SW-160** | ✅ **fixed** `f6619e53` | high | S | Re-generating a pool with billed allocations corrupts landlord_unrecovered_amount (billed rows are skipped out of the sum) | `Services/CamReconciliationService:381` |
| **SW-161** | ✅ **fixed** `71227984` | medium | S | period_year stays editable after a CAM pool is billed/reconciled — the one identity field the freeze does not cover | `Filament/Admin/Resources/CamExpensePools/Schemas/CamExpensePoolForm:110` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** On a CAM pool that has already invoiced its tenants, an operator could retype the YEAR (or the mall, or the pool code) and could type `status = reconciled` straight into the form — closing a year with tenants' shares still unbilled and skipping both the `cam.mark_reconciled` permission and the readiness check. Read at HEAD, not inferred. (1) `app/Models/CamExpensePool.php:279-291` — the `updating` guard names exactly five columns (`total_actual_expense`, `total_estimated_collected`, `admin_fee_pct`, `admin_fee_on_net`, `recovery_vat_rate`); `period_year`, `asset_id`, `pool_code` and `status` are all `$fillable` (model lines 87-115) and none is guarded. (2) `app/Filament/Admin/Resources/CamExpensePools/Schemas/CamExpensePoolForm.php:110-131` |
| **SW-162** | ✅ **fixed** `71227984` | medium | S | CAM pool status is a free Select, bypassing the markReconciled gate and permission | `Filament/Admin/Resources/CamExpensePools/Schemas/CamExpensePoolForm:186` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** On a CAM pool that has already invoiced its tenants, an operator could retype the YEAR (or the mall, or the pool code) and could type `status = reconciled` straight into the form — closing a year with tenants' shares still unbilled and skipping both the `cam.mark_reconciled` permission and the readiness check. Read at HEAD, not inferred. (1) `app/Models/CamExpensePool.php:279-291` — the `updating` guard names exactly five columns (`total_actual_expense`, `total_estimated_collected`, `admin_fee_pct`, `admin_fee_on_net`, `recovery_vat_rate`); `period_year`, `asset_id`, `pool_code` and `status` are all `$fillable` (model lines 87-115) and none is guarded. (2) `app/Filament/Admin/Resources/CamExpensePools/Schemas/CamExpensePoolForm.php:110-131` |
| **SW-163** | ✅ **fixed** `83b12b75` | medium | S | Correcting gross_sales after the VAT toggle leaves a stale VAT deduction that flows into the billed charge | `Filament/Admin/Resources/TenantSalesDeclarations/Schemas/TenantSalesDeclarationForm:236` **Fixed 2026-09-05.** The `vat` deduction is computed once, when the toggle is flipped, from the gross at that instant — so CORRECTING the gross afterwards left it taken from a figure that no longer existed, and it flowed straight into the charge basis. Measured: 1,368,000 corrected to 1,860,000 kept a VAT row of 168,000 where 228,378.95 was within the new figure, and because the breakpoint is subtracted FIRST a small error in sales becomes a large one in the overage. Re-derived only when a `vat` row already EXISTS, so it re-runs what the operator asked for and never invents a deduction on a declaration stated net. Driven through the real form, because the staleness lives in a `->live()` callback no service-level test can see |
| **SW-164** | ✅ **fixed** `83b12b75` | medium | S | sales_exclusions KeyValue takes free-typed keys and values; unknown keys and formatted numbers are silently dropped from the charge basis | `Filament/Admin/Resources/TenantSalesDeclarations/Schemas/TenantSalesDeclarationForm:251` **Fixed 2026-09-05, and it was two defects.** (a) `SalesExclusions::total()` skips any key outside the catalogue and the field is a free `KeyValue`, so `VAT`, `refunds` or `sales returns` typed by hand deducted **0.00** and the tenant paid percentage rent on turnover the operator had agreed to exclude — silent, because the screen shows the derived total and never the parse. REFUSED now and the refusal NAMES the keys; refusing costs nothing because `other` exists precisely so a real negotiated clause never has to be invented as a new key. On the MODEL rather than the form, since the portal, the importer and the API reach the same column. (b) **`(float) '1,200.00'` is 1.0 in PHP** — it stops at the comma, and a thousands separator is how money is written and what a POS report prints, so a 120,000 returns deduction was worth one pound: measured, `declared_sales` 999,999.00 where 880,000.00 was right. `SalesExclusions::amount()` reads it as written, Arabic-Indic digits and separators included. **Testing trap recorded**: a `KeyValue` holds `['vat' => x]` after `fillForm()` and `[['key' => 'vat', 'value' => x]]` after any Livewire UPDATE, so a test reading one shape sees 0.0 after the interaction it is testing and reports a working fix as broken. 10 cases, 4 mutations |
| **SW-165** | ✅ **fixed** `c00e0143` | medium | — | Portal CAM allocation View shows a blank unit for a unit owner — the same relation the resource's own docblock says is null there | `Filament/Portal/Resources/CamAllocations/Schemas/CamAllocationInfolist:24` **Fixed 2026-09-05, and the row's own subject was the least of it.** `UnitOwnership` declares `unit()` and `owner()` and NO `tenant()` — its docblock says so in writing — and an undefined relation is a missing attribute, so it resolved **null instead of throwing**. Probed live on `mall_management_qa`: `unit_ownerships#1` has `owner->name` = "Ashraf El-Gindy", `->tenant` = NULL, `method_exists($o,'tenant')` = false. **Four readers.** The worst was `CamStatementPdfService`, sitting *under a seven-line comment written to stop an ownership statement rendering a blank party* — the unit, reference and area halves were genuinely fixed and the party half has been inert since it shipped, so the one document RC-06 exists for printed an empty party block. It also cost the document its LANGUAGE: `DocumentLocale::preferenceOf(null)` returns null, so the recipient tier was skipped and an owner's statement rendered in the operator's language rather than his own. Two more were the admin and portal `PdfDownloadAction` recipients — the admin one 250 lines below that file's own docblock warning about this trap. Review confirmed zero `unitOwnership->tenant` readers remain. `ACamShareNamesItsOwnerAndHisUnitTest`, 6 cases, mutation-proved |
| **SW-166** | ❌ **refuted** | medium | — | Portal sales-declaration create re-checks tenant ownership but not percentage rent — the mobile API checks both | `Filament/Portal/Resources/TenantSalesDeclarations/Pages/CreateTenantSalesDeclaration:27` | **Measured against HEAD by the 2026-09-03 verification sweep** (69 agents, one per group of related rows; the adversarial challenge phase did NOT run — it died on the session limit — so this verdict is single-source). The portal DOES enforce percentage rent — through a layer the row did not look at. The chain, read line by line and then measured: TenantSalesDeclarationForm.php (portal) declares `EntitySelect::make('lease_id')->entity(Lease::class)->modifyOptionsQuery(fn ($q) => $q->where('tenant_id', Portal::tenantId())->where('status','active')->where('has_percentage_rent', true))`. `EntitySelect::modifyOptionsQuery()` (app/Support/Filament/EntitySelect.php:125-130) stores the closure and re-runs `applyEntityBehaviour()`, which at :470-475 wires `getOptionLabelUsing(fn ($value) => ... OptionDisplay::labels($model, [$value], $modifier, ...))`. `OptionDisplay::labels()` (:1051-1060) goes through `pickable( |
| **SW-167** | ✅ **fixed** `71227984` | medium | S | The F-08 over-recovery guard checks Σ shares ≤ 100% while allocation multiplies the grossed-up basis — a stated share plus gross-up over-recovers past actual cost with the guard green | `Services/CamReconciliationService:162` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** The F-08 over-recovery guard asks whether the shares sum to 100%, while the allocation multiplies each share by the GROSSED-UP expense — so a stated share that fills a `gla` denominator back up recovers more than the landlord spent, with the guard green and the over-recovery recorded only as a negative `landlord_unrecovered_amount`. `app/Services/CamReconciliationService.php:136` computed `$basis = $pool->apportionmentBasis($occupancyPct)` (the grossed-up figure), `:197` refused only `$projectedShare > 1.000001`, and `:286` allocates `round(max(0.0, $basis - $excluded) * $share, 2)`. `app/Models/CamExpensePool.php:342-364` already refuses to gross up when `denominator_basis === occupied` under a comment saying 'there the shares already sum  |
| **SW-168** | ✅ **verified fixed** | medium | M | A mid-year resold unit's whole annual true-up lands on the Dec-31 owner; an ownership ended before year-end is recovered from nobody | `Services/CamReconciliationService:481` | **Measured against HEAD by the 2026-09-03 verification sweep** (69 agents, one per group of related rows; the adversarial challenge phase did NOT run — it died on the session limit — so this verdict is single-source). Pre-fix source at `git show 5e8374bb^:app/Services/CamReconciliationService.php` lines 561-563 read `$pool->participantOwnershipQuery()->where('status', UnitOwnershipStatus::HandedOver->value)->covering(CarbonImmutable::create($pool->period_year, 12, 31))` — exactly the point-in-time Dec-31 membership test the row describes. At HEAD (CamReconciliationService.php:577-585) it reads `->whereIn('status', UnitOwnershipStatus::everHadPossession())->overlapping($periodStart, $periodEnd)->with('unit.areas')`. `UnitOwnershipStatus::everHadPossession()` (app/Enums/UnitOwnershipStatus.php:81-84) returns `[HandedOver, Transferred]`, so the mid-year SELLER (whom `TransferUnitOwnershipService` sets to `Tr |
| **SW-169** | ✅ **fixed** `71227984` | medium | S | CAM statement resolves the cap term without the pool code — two callers missed by the cap-belongs-to-a-pool change (03133a13) | `Services/CamStatementPdfService:150` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** The CAM statement resolves the cap scope and the stated-share flag without the pool code, so a tenant's audit document reports the portfolio-wide fallback term instead of the one the reconciliation beside it actually used. `app/Services/CamStatementPdfService.php:144` calls `$lease?->statedCamSharePct((int) ($pool?->period_year ?? 0))` and `:150` calls `$lease?->camCapScope((int) ($pool?->period_year ?? 0))` — both omitting the second argument. `app/Models/Concerns/Lease/HasCamTerms.php:46,175` take `?string $poolCode = null`, and `camTermFor()` (`:108-124`) falls back to `whereNull('pool_code')` when none is given, so the document resolves the portfolio-wide term. `git show --stat 03133a13` ('a cap belongs to a recovery pool, not to a year |
| **SW-170** | ✅ **fixed** `71227984` | medium | S | The tenant statement's 'total due' omits the recovery VAT the invoice actually charges | `Services/CamStatementPdfService:159` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** The reconciliation statement's bottom line omits the recovery VAT that the invoice (or credit note) beside it actually charges, so the document a tenant audits quotes a smaller figure than the one they are billed. Read at HEAD. `app/Services/CamStatementPdfService.php:159` computes `'total_due' => round(max($trueUp, 0) + admin_fee + admin_fee_vat, 2)` — no recovery-VAT term — and `resources/views/cam/statement.blade.php:227-229` computes the credit side inline as `abs(true_up) - admin_fee - admin_fee_vat`. Both documents DO charge it: `app/Services/CamReconciliationService.php:1300-1302` (`$vatRate = (float) ($allocation->pool->recovery_vat_rate ?? 0); $vat = round($amount * $vatRate / 100, 2);`) and `:1464-1466` (the credit note, same shap |
| **SW-171** | ✅ **fixed** `741f879d` | low | XS | View modal of a LOCKED declaration recomputes the frozen percentage-rent figure as a live estimate | `Filament/Admin/Resources/TenantSalesDeclarations/Schemas/TenantSalesDeclarationForm:293` **Fixed 2026-09-05 (`741f879d`).** The preview field's comment claimed a locked declaration never reaches the hydration hook because `canEdit()` refuses it — true of the EDIT page, false of the `ViewAction` beside it, which declares no schema of its own, so Filament renders the SAME form and runs every `afterStateHydrated` on it. With no lock passed, `refreshDerived()` recomputed and OVERWROTE the billed figure with a live estimate: an overage invoiced at 2,500 read 5,000 the day the rate was renegotiated, and read **BLANK** if the lease stopped carrying percentage rent — beside an invoice still owed at 2,500. The hook now reads the RECORD's own status (`mixed`, because a schema resolves `$record` by NAME and it is null on a create page); the unlocked preview is pinned by its own control, so the guard cannot be widened into deleting the feature. 3 cases, 2 mutations |
| **SW-172** | ✅ **fixed** `71227984` | low | — | A tenant's own uploaded sales report and request attachments are write-only — no portal screen shows them back | `Filament/Portal/Resources/TenantSalesDeclarations/Schemas/TenantSalesDeclarationInfolist:30` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** Both things a tenant can upload from the portal — request attachments and the sales report — are write-only: no portal screen shows either back, so a retailer cannot check what they sent us. Read at HEAD. `app/Filament/Portal/Resources/TenantRequests/Schemas/TenantRequestForm.php:83-97` collects up to five images/PDFs into the `attachments` collection, and `app/Filament/Portal/Resources/TenantRequests/Schemas/TenantRequestInfolist.php` (the whole file, 84 lines) never mentions media — the portal resource has no Edit page either (`TenantRequestResource::canEdit()` returns false, getPages() is index/create/view), so there is no other surface. `app/Filament/Portal/Resources/TenantSalesDeclarations/Schemas/TenantSalesDeclarationInfolist.php:30- |
| **SW-173** | ✅ **fixed** `71227984` | low | S | voidLocked stores frozen English prose in audit_notes | `Services/PercentageRentCalculationService:629` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** voidLocked() composes an English sentence naming the date and the operator and stores it in audit_notes, so the record of why a locked declaration was reversed is frozen in one language and sits in a column the same operator can edit. Read at HEAD. `app/Services/PercentageRentCalculationService.php:629-636`: `$existing = …; $stamp = now()->format('Y-m-d'); $note = "Voided on {$stamp} by {$voidedBy->name}: {$reason}"; $declaration->update([... 'audit_notes' => $existing.$note])`. Raw English, composed at write time, with no `__()` anywhere in it — so no lang edit can ever reach a row already written, and an Arabic-reading accountant gets half a sentence. It is the shape CLAUDE.md records twice (`JournalNarrative` EG-36, `LeaseEventNarrative` |
| **SW-174** | ❌ **refuted** | low | S | Ownership allocations have no unique-index backstop — the lease path's last line of defence against concurrent double generation is absent for owners | `database/migrations/2026_08_15_200000_a_cam_allocation_may_belong_to_an_ownership:47` | **Measured against HEAD by the 2026-09-03 verification sweep** (69 agents, one per group of related rows; the adversarial challenge phase did NOT run — it died on the session limit — so this verdict is single-source). The STRUCTURAL half is true; the stated CAUSE is not. True: `SHOW INDEX FROM cam_allocations` on the live `mall_management` DB gives `cam_alloc_pool_lease_unique` UNIQUE (Non_unique=0) on (cam_expense_pool_id, lease_id), and `cam_allocations_unit_ownership_id_index` NON-unique (Non_unique=1) on unit_ownership_id. No unique index covers the ownership pair. False: that the unique index is "the lease path's last line of defence against concurrent double generation". I measured it. Server is MySQL 8.0.33, `@@global.transaction_isolation = REPEATABLE-READ`, no app override in `config/database.php`. I built a scratch DB (`tmp_sw174_probe`, since dropped) replicating both index shapes, populated it |

### Reports · settings · system

*0 open.*

| ID | Status | Sev | Fix | What is wrong | Where |
|---|---|---|---|---|---|
| **SW-175** | ✅ **fixed** `8d11faff` | high | — | "Record payment" on the collections worklist builds /admin/{tenantId}/… — a 404, and the prefill can never fire | `Filament/Admin/Pages/ArCollections:234` |
| **SW-176** | ✅ **fixed** `c6305aec` | high | — | "Send every month" replays the as-at date frozen at save time, for ever | `Services/Reports/DeliverSavedReportService:68` |
| **SW-177** | ✅ **fixed** `74769c3c` | medium | — | Vendor Scorecard: `operations` can read it but not export it — and can get the identical CSV by scheduling it to themselves | `Filament/Admin/Pages/Concerns/ExportsReport:74` **Fixed 2026-09-05 (`74769c3c`).** Confirmed: `operations` holds `vendors.view` and not `reports.view`, so it could READ every scorecard figure (`canAccess()` gates on `vendors.view`, deliberately — the docblock explains the `vendor` role must never read penalty totals) and was refused the CSV of the same figures — while the scheduled-delivery door handed over the identical file, making the refusal pure friction protecting nothing. `mayExport()` now answers `static::canAccess()`: the Exports doctrine, whoever may read a list may take it away, since an export returns the page's own scoped query and can never show a row the screen would not. Behaviour-identical on every page whose read gate IS `reports.view`, and the controls assert both directions so the widening cannot become everyone-exports-everything. 4 cases, mutation-proved |
| **SW-178** | ✅ **fixed** `71227984` | medium | — | The trial balance is the one accounting screen whose figures cannot be opened, though it already carries the account id | `Filament/Admin/Pages/TrialBalance:192` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** The trial balance is the one accounting screen whose figures cannot be opened — its rows have carried the account id since the page was written and nothing linked them into the general ledger. `app/Filament/Admin/Pages/TrialBalance.php:184` builds each row as `'id' => $row['account_id']`, and the `account` column at :191-194 carries `label`/`weight`/`description` and no `->url()`. The three financial statements do carry one: `RendersFinancialStatement::statementTable()` renders `->url(fn (array $record) => $this->ledgerUrlFor($record))` at :176-177, and `ledgerUrlFor()` at :143-156 builds `GeneralLedger::getUrl(['accountId','year','period','assetId'])`. `LedgerReportService::trialBalance()` emits `account_id` on every row, including the zer |
| **SW-179** | ✅ **fixed** `71227984` | medium | — | The marketing-post store picker is a HARD filter, so the Edit page refuses to save once the attributed retailer's lease ends | `Filament/Admin/Resources/MarketingPosts/Schemas/MarketingPostForm:48` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** The marketing-post store picker is a hard filter, so once the attributed retailer's lease ends every save of that post is refused as "invalid" on a field nobody touched — with no way out but to clear the attribution. `app/Filament/Admin/Resources/MarketingPosts/Schemas/MarketingPostForm.php:48-50` narrows with `->modifyOptionsQuery(...)` on `activeLeases.units`. `EntitySelect::applyTo()` (app/Support/Filament/EntitySelect.php:470-475) wires `getOptionLabelUsing(fn ($value) => OptionDisplay::labels($model, [$value], $modifier, ...))` with THE SAME `$modifier`, and `OptionDisplay::labels()` (:1051-1060) resolves through `pickable($model, $modifyQuery, ...)`. Filament then turns "cannot label" into a refusal: `Select::getOptionLabel()` (vendor |
| **SW-180** | ✅ **fixed** `71227984` | medium | — | The marketing-post "Live" column re-derives liveFor() and drops the store clause, so the operator's column contradicts the filter beside it | `Filament/Admin/Resources/MarketingPosts/Tables/MarketingPostsTable:79` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** The register's "Live" column composes its own reading and drops the store clause, so a post keeps reading Live after its retailer's lease ends — contradicting the Live tab and the Live filter on the same screen, and the shopper's feed. `app/Filament/Admin/Resources/MarketingPosts/Tables/MarketingPostsTable.php:79-86` computes the state as `isPublished() && ! hasExpired() && (display_from ?? starts_at) <= now`. `MarketingPost::scopeLiveFor()` (app/Models/MarketingPost.php:323-365) adds a fourth clause the column has none of: `tenant_id IS NULL OR EXISTS(tenant WHERE is_listed AND status='active' AND EXISTS(activeLeases.units WHERE units.asset_id = marketing_posts.asset_id))` — added at :347-358 under a comment naming the two failures it clos |
| **SW-181** | ✅ **closed — stale** | medium | — | A contractor has no language: the dispatch bell freezes in the dispatching operator's locale and their EN/AR choice never persists | `Notifications/WorkOrderDispatchedNotification:36` **Already fixed, verified 2026-09-05.** Both halves are closed at HEAD: the contractor's language is a column and a guard as of `1acd158f` (2026-09-04) — verified at `Models/VendorContact:136` returning `$this->locale`, over migration `2026_08_28_100000_add_locale_to_document_counterparties` — and the dispatch bell was never frozen in the sender's locale |
| **SW-182** | ✅ **fixed** `71227984` | medium | — | The balance-sheet export drops the figure the sheet balances on, and no export carries the balance check | `Services/Reports/ReportCsvExporter:60` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** Every financial statement leads its screen and its PDF with a ✓/✗ integrity check, and no CSV export carried it — the balance sheet's export also drops `total_equity_and_liabilities` entirely, so the exported copy ends on "Net income" with nothing to foot against total assets. Read at HEAD, 2026-09-04. `LedgerReportService::balanceSheet()` (line 250) returns `total_equity_and_liabilities` and `balanced`; `BalanceSheet::getSubheading()` (line 86) prints the ✓/✗ and `BalanceSheet::table()` (line 148) prints `total_equity_and_liabilities` as the second section total; `resources/views/accounting/pdf/balance-sheet.blade.php:22-28` prints BOTH as a grand-total row and a coloured paragraph. `ReportCsvExporter::balanceSheet()` (line 74) passes thre |
| **SW-183** | ✅ **fixed** `741f879d` | medium | — | Occupancy-cost and MAT windows include the current month, which has costs but no declarations — ratios overstated and colour bands flipped | `Services/Reports/ReportService:607` **Fixed 2026-09-05 (`741f879d`), and it was worse than filed.** Cost is billed on the FIRST of a month, sales are declared after the LAST, so a window ending inside the running month divided twelve months of cost by eleven of sales: occupancy cost overstated by exactly ×12/11 (**walking a genuine 23.0% tenant across the 25% red line** — the colour band is the whole reading of that report), and MAT reading **−8.3% growth on dead-flat trading, every month of the year**. Measured on the QA books: 250,945.00 of September cost against zero September declarations. `TenantSalesDeclaration::lastDeclarableMonth()` names the rule ONCE — both scheduled commands already encoded it — and both reports clamp to it. **The rule is the CALENDAR, never the data**, and the regression test carries the argument in numbers: skipping undeclared months would drop them from BOTH sides, so a tenant who stops filing would read the same healthy 23.0% as the diligent one — inverting the one signal the report exists to give; under the calendar rule they read 25.09%, which is worse, which is the truth. `mtd`/`ytd` keep the running month — that is what *to date* means. **Found while measuring: `subMonths(11)` OVERFLOWS off a 31st** (31 March lands on 1 May), so the "rolling 12 months" was silently ELEVEN in five months of the year. 6 cases, 4 mutations |
| **SW-184** | ✅ **fixed** `71227984` | medium | — | A `from`/`to` report range can be inverted, and WeeklySpend then reports EGP 0.00 of spend as a fact | `Support/ReportFilters:83` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** A `from`/`to` report range can be stated backwards, and the three range reports then answer with a confident zero — WeeklySpend seeds no weeks at all and its subheading prints "EGP 0.00 · EGP 0.00 · EGP 0.00" as a fact. Read at HEAD, 2026-09-04. `App\Support\ReportFilters::from()` (line 78) and `::to()` (line 88) are plain `DatePicker`s with no bound on each other, and they are the only door: `ReportFilters::from`/`to` is used by exactly three pages — `WeeklySpend`, `OccupancyCost`, `VendorScorecard`. In `ReportService::weeklySpend()` (line 170) an inverted pair makes the seeding loop `for ($cursor = $from; $cursor <= $to; …)` run zero times AND both `whereBetween` clauses match nothing, so `weeks` is `[]` and `totals` is `['fixed' => 0.0,  |
| **SW-185** | ✅ **fixed** `c00e0143` | low | — | The cash-flow statement is the only ledger report with no ledger-freshness line and no "Post to GL" | `Filament/Admin/Pages/CashFlow:33` **Fixed 2026-09-05.** Five admin pages read `LedgerReportService`; four carried `PostsToLedger` and Cash Flow carried neither the freshness subheading nor the Post-to-GL button — on the one page where *"it does not reconcile"* and *"nobody has synced since Tuesday"* are the same sentence, since posting is a daily SWEEP and an unposted receipt moves the bank account and none of the three activity sections. **An omission, not a decision**: `CashFlow` was added 2026-07-02 and the extension commit is 2026-07-10, whose own message claims *"…so no report can silently show stale numbers"* — enumerated from that diff rather than from the code, and it missed the page that had shipped eight days earlier. Nothing in the file stated a reason to differ and its own `getSubheading()` docblock argues the opposite. Review verified the freshness line does not leak into the CSV or PDF (both call `StatementIntegrity` directly) and that the seven traits compose with no method conflict. `EveryLedgerFedReportSaysHowFreshTheBooksAreTest`, 4 cases, mutation-proved |
| **SW-186** | ❌ **refuted** | low | — | OccupancyMap's headline is string-concatenated, so the Arabic panel reorders it — its sibling map does it correctly | `Filament/Admin/Pages/OccupancyMap:126` | **Measured against HEAD by the 2026-09-03 verification sweep** (69 agents, one per group of related rows; the adversarial challenge phase did NOT run — it died on the session limit — so this verdict is single-source). The concatenation is real — `app/Filament/Admin/Pages/OccupancyMap.php:126` is `__('admin.widgets.occupancy_grid.occupancy_rate').": {$rate}% ({$occupied}/{$total})"` while the sibling `RentableItemMap.php:116` passes placeholders. The RENDERING claim is false. I implemented the Unicode Bidi Algorithm (W1–W7, N0 bracket pairing, N1/N2, I1/I2, L2) and VALIDATED it against the two real bugs CLAUDE.md records for `App\Support\Pdf\Bidi`: it reproduced `.Issued in error; voided same day` and `201808046413+` exactly. The admin panel is `dir="rtl"` under Arabic (`vendor/filament/filament/resources/lang/ar/layout.php:5` → `components/layout/base.blade.php:12`). Running the real string through it in  |
| **SW-187** | ✅ **fixed** `71227984` | low | — | The spend↔campaign link is write-only: no column, no filter, and absent from the spend register CSV the owner reviews | `Filament/Admin/Resources/MarketingBudgets/RelationManagers/MarketingSpendsRelationManager:72` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** A marketing spend can name the campaign it paid for and nothing ever reads it back — no column, no filter, and no line in the register CSV the owner reviews — so "what did the Ramadan campaign cost" is a question the data can answer and the panel cannot. At HEAD 2026-09-04. `marketing_spends.marketing_post_id` is collected by `MarketingSpendsRelationManager:72` (an `EntitySelect` scoped to the budget's property) and read back by nothing in `app/Filament`: the spend table (:113-150) declares six columns — spent_on, category, description, amount, paid_from, receipt_reference — and has no `->filters()` block at all, and `MarketingBudgetResource::spendRegisterCsv()` (:113-144) emits those same six and no campaign. `MarketingPost::spends()` (app |
| **SW-188** | ✅ **fixed** `71227984` | low | — | `SetApiLocale::SUPPORTED` is a second hardcoded language list, unreferenced and ungated | `Http/Middleware/SetApiLocale:17` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** The row's "unreferenced" half is wrong — the tenant profile endpoint validates against it — but the drift is real and wider than the row says: which languages this system speaks is written out in five places besides the one list, and one of them reads a config key that does not exist. At HEAD 2026-09-04. `grep -rn 'SetApiLocale::SUPPORTED' app/ tests/` → `UpdateProfileRequest:39` uses it (so REFUTED on 'unreferenced'), under a comment claiming it is "the ONE supported list rather than a copy", which is what it is not. A comment-stripped token sweep of all 1,419 PHP files under `app/` for an adjacent `'en'`/`'ar'` pair returns FIVE files besides `SetLocale`: `Http/Middleware/SetApiLocale.php:17`, `Http/Controllers/PaymentLinkController.php:5 |

### Inventory · fixed assets

*0 open.*

| ID | Status | Sev | Fix | What is wrong | Where |
|---|---|---|---|---|---|
| **SW-189** | ✅ **fixed** `c6305aec` | high | — | weightedAverageCost() averages every receipt ever made, not the stock on hand — Inventory keeps a permanent, compounding residual for stock that is gone | `Services/StockMovementService:307` |
| **SW-190** | ✅ **fixed** `71227984` | medium | XS | Dispose modal: proceeds_account picker can never appear, so bank proceeds always post to cash | `Filament/Admin/Actions/FixedAssetActions:66` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** The dispose modal's "Proceeds to" picker can never appear, so its answer is never submitted and every disposal books its sale proceeds to CASH — an asset sold and banked debits cash on hand. Same defect class as the charge-schedule prorate toggle, so one patch closes both. At HEAD 2026-09-04. `app/Filament/Admin/Actions/FixedAssetActions.php:66` conditions `proceeds_account` on `(float) $get('proceeds') > 0` while the `proceeds` TextInput above it (:53-59) carries no `->live()`, so nothing re-renders the schema after the amount is typed. And a hidden Filament field is not dehydrated: `vendor/filament/schemas/src/Components/Concerns/HasState.php:813 isHiddenAndNotDehydratedWhenHidden()` → `dehydrateState()` at :316-340 FORGETS the state path |
| **SW-191** | ✅ **fixed** `71227984` | medium | S | Item's Stock Movements tab shows every mall's movements to a property-restricted operator, and cannot tie out to the scoped on-hand beside it | `Filament/Admin/RelationManagers/StockMovementsRelationManager:26` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** On a shared catalogue item, the Stock Movements tab lists every mall's receipts and issues, while the on-hand figure printed beside it on the same screen is property-scoped — so the two halves of one screen cannot be reconciled, and a property-restricted operator reads another mall's stock history. `app/Filament/Admin/RelationManagers/StockMovementsRelationManager.php:36` builds its table off `$relationship = 'movements'` with no query modifier at all, while `app/Filament/Admin/Resources/InventoryItems/InventoryItemResource.php:118-137` narrows BOTH `on_hand` and `stock_value` through `TenantScope::reportAssetIds(TenantScope::currentAssetId())`. `StockMovement` carries `#[PropertyOwned(via: 'warehouse')]` (`app/Models/StockMovement.php:33`) |
| **SW-192** | ✅ **fixed** `71227984` | medium | S | Total NBV summarizer includes disposed assets' residual book value while its label claims the balance-sheet tie-out | `Filament/Admin/Resources/FixedAssets/Tables/FixedAssetsTable:87` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** The fixed-asset register's totals include assets that have already been sold, so the footer labelled as the balance-sheet tie-out overstates net fixed assets by every disposed asset's residual book value. Probed `mall_management_qa` on 2026-09-04. GL: `select la.code, sum(jl.debit)-sum(jl.credit) from journal_lines jl join ledger_accounts la … where la.code like '12%' and je.status <> 'void'` → Furniture & Equipment net **2,250,000.00**, Accumulated Depreciation net **−338,166.64**, i.e. **1,911,833.36** of net fixed assets. The register's six rows (`fixed_assets`, `deleted_at is null`) sum to cost **2,325,000.00**, accumulated **362,333.21**, NBV **1,962,666.79** — one `disposed` row (`Floor scrubber`, cost 75,000.00, accumulated 24,166.57 |
| **SW-193** | ✅ **fixed** `71227984` | medium | XS | 'Fully depreciated' filter ignores salvage_value and opening_accumulated_depreciation — the write-off worklist misses exactly the assets it exists for | `Filament/Admin/Resources/FixedAssets/Tables/FixedAssetsTable:131` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** 'Fully depreciated' compares gross cost against the depreciation entries alone, so it ignores salvage value and any pre-Atriom write-off — and because the monthly run clamps accumulated at cost minus salvage, the write-off worklist can never return an asset that carries a salvage value at all. `FixedAssetsTable:129-137` at HEAD: `where('fixed_assets.acquisition_cost', '<=', DepreciationEntry::query()->selectRaw('coalesce(sum(amount),0)')…)`. `DepreciationService::run()` clamps the last charge at `depreciableBase()` = `cost − salvage` (`app/Services/DepreciationService.php:20-23,119`), so `Σ entries ≤ cost − salvage < cost` whenever salvage > 0 — the predicate is unsatisfiable. Probed `mall_management_qa` 2026-09-04: all six register rows ca |
| **SW-194** | ✅ **fixed** `71227984` | medium | XS | inventory_items.reorder_quantity is enterable on no screen and no importer — the draft-reorder service's 'stated reorder quantity' branch is dead in production | `Filament/Admin/Resources/InventoryItems/Schemas/InventoryItemForm:72` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** `inventory_items.reorder_quantity` — the column that says how much to buy — is settable on no screen and no importer, so the low-stock scan's 'stated reorder quantity' branch is unreachable in production and every drafted purchase line carries the shortfall, which lands the item back on its own threshold. `grep -rn reorder_quantity app/` at HEAD returns exactly four lines: `app/Models/InventoryItem.php:37` (fillable), `:44` (cast), and `app/Services/DraftReorderPurchaseService.php:106-107`, both halves of the `$item->reorder_quantity !== null` ternary. `app/Filament/Admin/Resources/InventoryItems/Schemas/InventoryItemForm.php` carries `reorder_level` (line 72) and no `reorder_quantity`; `ls app/Filament/Imports/` has ten importers and none  |
| **SW-195** | ✅ **fixed** `71227984` | low | XS | Low-stock filter and red on-hand disagree with the low-stock scan: reorder_level 0 means 'off' to the alert, 'always low' to the screen | `Filament/Admin/Resources/InventoryItems/Tables/InventoryItemsTable:101` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** An item with no reorder level is treated as permanently short by the list (red on-hand figure, and it fills the reorder worklist) while the low-stock scan deliberately ignores it — three readers, three copies of one rule, and the screen and the bell say opposite things about the same item. CONFIRMED at HEAD. `app/Console/Commands/ScanLowStockCommand.php:56` filters `->where('reorder_level','>',0)` under a comment that says 0 means "we do not track a minimum for this". `app/Filament/Admin/Resources/InventoryItems/Tables/InventoryItemsTable.php:52` colours `(float) $state <= (float) $record->reorder_level` → for an untracked item at 0 on hand that is `0 <= 0` → 'danger'. `InventoryItemsTable.php:106` filters `inventory_items.reorder_level >=  |
| **SW-196** | ✅ **fixed** `71227984` | low | S | Three record pickers (custodian, transfer from/to warehouse) are bare Selects that evade the EntitySelect gate through a helper-method indirection | `Filament/Admin/Resources/StockMovements/Pages/ListStockMovements:156` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** The transfer and consumption warehouse pickers are bare Selects that search one raw column, fold neither side and re-write the property scope by hand — and they were invisible to `EntitySelectConformanceTest` because the tell-tale `pluck('name','id')` sits in a private helper rather than in the chain, which is a hole the gate has for ELEVEN pickers, not the three the row names. CONFIRMED and UNDERSTATED at HEAD. The row names three (custodian, transfer from/to). Re-implementing the gate's own detection with a one-hop expansion into same-file helpers and running it over all of `app/Filament` found **11** offenders where the gate reports 0: `Admin/Actions/InvoiceActions.php:57` and `:96` (`invoice_item_id`), `RelationManagers/PayrollLinesRela |
| **SW-197** | ✅ **fixed** `71227984` | low | S | Stock-movement refusals reach the operator as raw-English toast bodies, outside the refusal-translation gate by exception class | `Filament/Admin/Resources/StockMovements/Pages/ListStockMovements:257` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** Five stock-movement refusals reach the operator's toast as raw English because they are raised as `InvalidArgumentException`, a class the refusal-translation gate deliberately does not sweep — so an Arabic panel prints an English business rule, and every other door into the service renders the same refusal as the 500 page. CONFIRMED at HEAD. `app/Services/StockMovementService.php` throws `InvalidArgumentException` with hardcoded English at lines 37, 42, 108, 223, 227 and 243. `app/Filament/Admin/Resources/StockMovements/Pages/ListStockMovements.php:257` catches `InvalidArgumentException/DomainException` and puts `$e->getMessage()` into the toast body, under a docblock (line 234) that calls these "real, reachable things — not enough stock on |
| **SW-198** | ✅ **fixed** `71227984` | low | S | The stock-movement ledger has no date-range, warehouse or item filter — one type filter on an append-only register | `Filament/Admin/Resources/StockMovements/Tables/StockMovementsTable:65` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** The append-only stock ledger carries one filter — movement type, which the tabs above it already offer — so "what left the Consumables store in March" means scrolling a table that grows for ever; and the CSV export ignores the screen entirely, which becomes a silent wrong answer the moment filters exist. CONFIRMED at HEAD. `app/Filament/Admin/Resources/StockMovements/Tables/StockMovementsTable.php:65-69` declares exactly one filter (`SelectFilter::make('type')`), duplicating the `getTabs()` split at `ListStockMovements.php:345-354`; there is no date, warehouse or item filter. Every other dated register carries a range — `CustodiesTable.php:110-118` is the house pattern (`Filter::make('custody_date')` with `from`/`until` DatePickers on `admi |

### Tax

*2 open — 2 high.*

| ID | Status | Sev | Fix | What is wrong | Where |
|---|---|---|---|---|---|
| **SW-199** | ✅ **fixed** `fe0d8d46` | high | S | VAT return counts VOIDED credit notes on the documents side (credit notes are 'void', never 'cancelled'), understating filed taxable base and permanently breaking the tie-out control. **Excluding `void` outright is the obvious repair and is ALSO wrong** — caught in review before it shipped: voiding posts a REVERSAL and leaves the original `void` (a REPORTABLE status), and `reversalPeriod()` back-dates that reversal only while the period is open — so a note voided after its month closed is netted in the CURRENT month, and dropping it moved `base_standard` on an ALREADY FILED return by the whole credited supply (measured 60,000 → 80,000). The rule is about DATES, in two halves that compose through the accumulator's sign | `Services/Reports/VatReturnService:87` |
| **SW-199b** | ✅ **fixed** `d7ff909d` | high | S | *(found by the review of SW-199 — not in the sweep.)* `SyncCamPoolFromLedgerService` filters billed invoices with `whereNotIn('invoices.status', ['cancelled','written_off'])` and **omits `draft`**, which is the column DEFAULT. A never-issued draft inflates the billed estimate and therefore understates the CAM true-up, or mints a credit note. Its siblings `PercentageRentCalculationService:108` and `ReportService:937` both exclude `draft`, so the asymmetry is verified rather than assumed. **CAM is another session's area this cycle — do not fix without checking HEAD** | `Services/SyncCamPoolFromLedgerService:296` | **Fixed here as `draft` only**; the `credited` / partially-credited / `written_off` treatment is a separate accounting question, recorded as SW-216.
| **SW-199c** | ✅ **fixed** `1130303f` | high | S | *(found by the review of SW-199.)* `AssetStatementPdfService` filters `['cancelled','credited']` and omits **`written_off`** and **`draft`**. A write-off deliberately leaves `balance` standing, so `where('balance','>',0)` two lines later puts already-relieved bad debt on the OWNER's outstanding list. Every sibling AR read excludes it — `TenantLedger:51`, `TenantStatementPdfService:101`, `DepositHoldings:83`, `Lease::DEPOSIT_BILLING_EXCLUDED_STATUSES` | `Services/AssetStatementPdfService:59` |
| **SW-199d** | ✅ **fixed** `ebf0faf1` | medium | S | *(found by the review of SW-199.)* `BooksReconciliationService` matches credit notes with NO status filter, so a CAM allocation still `billed` whose backing note was VOIDED passes `$hasCredit` and the reconciler reports clean — a check that cannot fail, the family CLAUDE.md gates for | `Services/Reconciliation/BooksReconciliationService:183` |
| **SW-199e** | ✅ **fixed** `ebf0faf1` | medium | S | *(found by the review of SW-199.)* A DRAFT credit note downloads as an unwatermarked, numbered tax document: `CreditNotePdfService` watermarks on `status === 'void'` only, and neither download action gates on status. `CreditNote::isOnTheBooks()` is exactly the predicate. The exporter has no on-the-books marker either, so summing its `total` column double-counts drafts and voids | `Services/CreditNotePdfService:57` |
| **SW-199f** | ✅ **fixed** `ebf0faf1` | medium | M | *(found by the review of SW-199.)* `CreditNoteJournalizer` gates on the hand-rolled allowlist `['issued','applied']` — the complement of `CreditNote::NOT_ON_THE_BOOKS` by coincidence, not derivation. A fifth status would be COUNTED by the documents side (exclusion) and SKIPPED by the GL (allowlist), silently. `CreditNote::hasBalance()` is a third copy of the same judgement | `Services/Accounting/Journalizers/CreditNoteJournalizer:27` |
| **SW-200** | ✅ **fixed** `71227984` | medium | — | The VAT and withholding returns show a pinned per-mall "Property scope" while both deliberately compute portfolio-wide | `Filament/Admin/Pages/VatReturn:200` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** The VAT return and Form 41 print "Property: <the mall you are standing in>" above figures that are the whole portfolio's — a statutory filing position under a caption naming one mall. CONFIRMED at HEAD (e3154f27). `VatReturn.php:200` and `WithholdingTaxReturn.php:341` each call their service with a NULL asset, with the reason written beside the call ("a VAT return is filed per registration… A property filter here would invite someone to file a per-mall return"). Neither page overrides `filtersForm()`/`ledgerFilterComponents()` (grep: only TrialBalance:157 and IncomeStatement:128 compose it), so both inherit `ScopesLedgerReport::ledgerFilterComponents()` → `PropertyField::reportScope()`, which is `->default(TenantScope::currentAssetId())->di |
| **SW-039b** | ✅ **fixed** `c7838487` | high | S | *(found by the review of SW-039.)* A PARTIAL credit note inflated the deposit pot: `credit_applied_amount` feeds `paid_amount`, so crediting 40,000 of a 100,000 deposit invoice with 60,000 of cash on it left the invoice `paid` — never `credited` — and a move-out refunded 40,000 that never arrived. Outbound, with no recovery path | `Support/DepositBilling:heldOn` |
| **SW-039c** | ✅ **fixed** `c7838487` | high | S | *(found by the review of SW-039.)* A PARTIAL write-off made the deposit shortfall permanently un-re-billable — `WriteOffInvoiceService` retires an invoice only on a FULL write-off, so no status moves and the forgiven slice went on counting as *already asked for*. The **Bill deposit** button stayed hidden and the service refused quoting money the operator had themselves forgiven | `Support/DepositBilling:claimedOn` |
| **SW-039d** | ✅ **fixed** `c7838487` | high | S | *(found by the review of SW-039.)* `DepositHoldings` carried the same status filter as two LITERALS — a third and fourth copy — so the register header read *"held 0.00"* under a red GL-gap stat while the lease page beside it read 60,000 | `Support/DepositHoldings:83` |
| **SW-210** | ✅ **fixed** `bff5ee36` | high | M | *(found by the review of SW-039 — an ACCOUNTING decision, not a code choice.)* `deposits_tie_out` is red for any written-off deposit invoice, so `atriom:preflight` blocks the next deploy. `InvoiceWriteOffJournalizer` posts `Dr bad_debt_expense / Cr accounts_receivable` whatever the line was — but a `security_deposit` line credited `deposits_held`, a LIABILITY, at issue, not revenue. Writing one off therefore books a bad-debt expense against revenue never recognised and leaves the refund obligation standing at its full billed figure: the balance sheet overstates the obligation and the P&L takes a phantom expense. Either the journalizer relieves `deposits_held` for the deposit portion, or `billedAndOutstanding()` keeps counting written-off deposit lines. **Ask the accountant** | `Services/Accounting/Journalizers/InvoiceWriteOffJournalizer:47` **DECIDED AND FIXED 2026-09-05 — it was not an accountant's question.** A `security_deposit` line credits `deposits_held`, a LIABILITY, at issue, so no revenue is ever recognised against it: no standard permits an expense against unrecognised revenue, and Yardi reverses a written-off deposit charge against the deposit liability. The journalizer now splits its debit. **The split is FROZEN on `invoice_write_offs.deposit_amount`, and the adversarial review is what forced that** — the first version derived it from the invoice's LIVE settlement, and a partial write-off leaves the invoice live (`settled_short` is a shipped reason), so a payment a month later would move the split and `matches()` would void and re-post the entry AT ITS ORIGINAL DATE, into a month that may have closed, where the re-post throws inside a sync that only logs — **SW-236 exactly, through another door**, and reversing one write-off would have restated a sibling's entry too. **PROSPECTIVE**, which is Yardi's rule for a classification change: existing rows carry 0.00 and post what they always posted, so nothing re-posts and no green `gl_in_sync` turns red; backfilling the true split would have blocked `deploy.sh` behind closed periods on precisely the installs this row is about. The role is resolved from the charge code rather than hardcoded (`posting_role` is operator-editable, and a hardcoded `deposits_held` debits a liability never credited); the attribution reuses `DepositBilling`'s existing deposit-reached-last rule, re-ordered to `[entry_date, id]` so the operator's own dates decide which month's P&L moves; capped at the NET figure the issue credited, since `InvoiceItemSettlement` works gross and a taxable deposit line would otherwise drive the liability negative by its VAT. **The row's own alternative** — leave the ledger alone and have `billedAndOutstanding()` keep counting written-off deposit lines — reposts nothing and was considered; the ledger fix is the accounting-correct one and the prospective treatment buys back its migration cost. `AWrittenOffDepositRelievesTheObligationNotThePnlTest`, 7 cases, 4 mutations |
| **SW-211** | ✅ **fixed** `c7838487` | medium | S | *(found while running the gates; renumbered from SW-202, which collided with the tax-form row of that number — commit `c7838487` names the old id.)* `UnresolvedClassReferenceConformanceTest` read `$entry->screen::getUrl()` as a class named `screen` and was RED on main over correct code. Its type-declaration branch already skipped a name behind `->`; its static branch did not | `tests/…/UnresolvedClassReferenceConformanceTest:252` |
| **SW-062b** | ✅ **fixed** `84386f3f` | high | S | *(found by the review of SW-062/063.)* The contractor's job brief showed **other contractors' quotes** on the same job — `proposals()` keys on the work order alone, and a job legitimately carries competing prices — so the losing bidder read the winner's figure and the operator's `decision_reason`, under a heading saying "Your quotes" | `Filament/Vendor/…/JobBrief:ownQuotes` |
| **SW-062c** | ✅ **fixed** `84386f3f` | medium | S | *(found by the review of SW-062/063.)* The brief's thread and quotes rendered as ONE run-on paragraph: a single-item `TextEntry` emits `e($state)` in a bare div and nothing sets `white-space`, so every `\n` collapsed — byline into body, message into message. `listWithLineBreaks()` is the only branch that emits an element per item | `Filament/Vendor/…/JobBrief:108` |
| **SW-212** | ✅ **fixed** `1f9f166e` | high | M | *(found by the review of SW-114; analysed 2026-09-02, NOT fixed — a half-fix here would be theatre.)* `FacilityWorkOrder::recomputeCosts()` is an unlocked read-modify-write: four plain aggregates, then `saveQuietly()`. Two writers on one job — a bill payment and a penalty application — each compute from their own snapshot and the last wins. **A `lockForUpdate()` on the work order does NOT fix it**: under REPEATABLE READ the waiter's snapshot was fixed at its first read, so it re-reads the children from before the wait — the F-09 finding. Making the four child aggregates locking reads DOES fix it and introduces a **deadlock**: `VendorBill::saved` already holds the bill row and would then want the work order, while a labour write holds the work order and wants the bills. The clean answer is to compute and write in ONE `UPDATE … SET … (SELECT …)`, which has no read-modify-write window and takes its locks in a single statement — but that gives up the model attributes the method also sets, and needs verifying on real MySQL (`docs/qa/scripts/race.sh`), not on sqlite | `Models/Concerns/FacilityWorkOrder/HasWorkOrderCost:81` | **Real, reproduced 3/3 on MySQL 8.0.33, and the reproduction took FOUR attempts — three of which measured nothing and reported a pass.** The instructive one: A opened a transaction, read (fixing its snapshot), waited and recomputed — and its SQL trace showed four aggregate SELECTs and **no UPDATE at all**, because A's stale aggregate equalled A's stale in-memory row byte for byte, `getDirty()` was empty, and `performUpdate()` short-circuited. (The others: both workers inserting back to back just serialised; both inserting inside one snapshot window collided on the document number, so the UNIQUE index refused one and the cost race never ran — the unit-double-booking accident again.) **A has to have a cost of its OWN.** Then, old code: `labour 1500 + material 0 + service 3000` stored with a TOTAL of **2500** — the failure is not a bucket losing a bill, it is a row that no longer adds up, on the column `costVariance()` reads. Intermittent (2 in 3) because a child insert takes a shared FK lock on the parent, so whenever B lands last B repairs A's damage. Fixed by computing inside the UPDATE, where MySQL reads like READ COMMITTED (measured: after another transaction committed +2,000 the plain aggregate read 3,000 and the same aggregate inside an UPDATE read 5,000). Sub-selects compiled from the RELATIONS so the soft-delete scopes travel; the total repeats its terms because MySQL lets a later SET assignment read an earlier one and **SQLite reads the original row** (measured both). Byte-identical across all 36 work orders. **The review found two regressions I introduced**: `syncOriginal()` syncs the WHOLE attribute array, so a caller's unsaved edit was silently marked clean and the next `save()` wrote nothing; and writing `est_total_cost` unconditionally reintroduced the very lost update one column across — an operator's typed 1,000 replaced by a 100 read from a snapshot fixed before they typed it. Both fixed, both with controls |
| **SW-213** | ✅ **fixed** `71227984` | medium | S | *(found by the review of SW-114.)* `ScanWorkOrderSlaBreachesCommand::alertBreach()` holds an X lock on `facility_work_orders` across a SYNCHRONOUS mail send — `WorkOrderSlaBreachedNotification` is not `ShouldQueue` and its `via()` is `['mail','database']` — so the hourly scan blocks facility writers for one MailerSend round-trip per recipient | `Console/Commands/ScanWorkOrderSlaBreachesCommand:320` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** Nine scheduled scans send a notification - a synchronous MailerSend round-trip per recipient - while still holding the `lockForUpdate()` row lock, so an hourly job blocks every operator writing that row, and a delivery failure to one recipient rolls back the bell rows of the recipients who already got the e-mail and mails them again next hour, for ever. HEAD, tokenised sweep of every `DB::transaction(` closure under `app/` (158 closures across 105 files): 9 `Notification::send()` calls sit lexically inside one - `ScanWorkOrderSlaBreachesCommand:253` and `:345`, `ScanTenantRequestSlaBreachesCommand:84`, `ScanOverdueInvoicesCommand:76` (which locks `invoices`), `ScanLeaseOptionWindowsCommand:163`, `ScanContractRenewalsCommand:112`, `ScanTenan |
| **SW-205** | ✅ **fixed** `71227984` | medium | M | *(found by the review of SW-152.)* `CatalogueAwareSelect` keys on the RECORD's table, so five pickers that write a CHILD table from an action modal or a relation manager resolve to the wrong registry key — `employee_advance_repayments.method`, `deposit_transactions.method` (LeaseActions), `vendor_bill_payments.method`, `disbursements.method`, `custody_transactions.category`. All create-only today, so nothing is broken; the claim "covers every Select" is not yet true and no gate ties `catalogueWidenedColumns()` to the pickers that write those columns. `Radio`/`ToggleButtons`/`CheckboxList` derive `Rule::in` identically and are unbound | `Support/Filament/CatalogueAwareSelect:120` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** The catalogue carve-out is bound to `Select` alone, so a catalogue column rendered by `Radio`, `ToggleButtons`, `CheckboxList` or any `Select` subclass would silently reinstate the 2026-08-18 defect — today zero do, and nothing keeps it that way. The row's other half, the five child-writing pickers, is real but has no live consequence and no correct code fix; it is recorded rather than patched. CONFIRMED, and HALVED, at HEAD (83624504). (A) The five pickers exist and key on the parent's table: `EmployeeAdvancesRelationManager:186` (`employee_advance_repayments.method`, record = EmployeeAdvance), `LeaseActions:200` (`deposit_transactions.method`, record = Lease), `EditVendorBill:166` (`vendor_bill_payments.method`, record = VendorBill), `Own |
| **SW-206** | ✅ **fixed** `b31ee637` | medium | S | *(found by the review of SW-152.)* `ValueSets::widen()` reads `codes()`, which is `is_active = true` only — so retiring an operator-added code REMOVES it from the accepted set, and any path that rewrites the column (importer, `replicate()`, a service `update()`) is refused at the model layer. It does not bite on a plain edit because `guard()` short-circuits on `! isDirty()`. The widen docblock claims it "only ever WIDENS… must never invalidate the documents that already name it", which is false for an operator-added code | `Support/ValueSets:widen` | **Designed by the 2026-09-03 patch fleet, adversarially reviewed, then applied, tested and mutation-checked here.** `ValueSets` widens a column from the catalogue's ACTIVE codes only, so retiring an operator-added code removes it from the set the saving listener accepts — and every path that re-states the column is then refused at the model layer, most damagingly the nightly recurring-cost run, which stops booking that levy for ever. Confirmed live at HEAD (04f19175). `ValueSets::widen()` (app/Support/ValueSets.php:633) unions the literal floor with `$catalogue()`, and all sixteen `CATALOGUE_WIDENED` entries point at `codes()` / `inboundCodes()` / `outboundCodes()`, every one of which funnels through `IsCodeCatalogue::cachedCodes()` — whose query is `$scope(static::query()->where('is_active', true))` (app/Models/Concerns/IsCodeCatalogue.php:178). So a retired code that is NOT in the literal floor leaves the accepted set entirely. The row's own note is right that a plain edit survives, because `guard() |
| **SW-207** | ↩︎ **refuted on re-check** | high | M | *(found by the review of SW-176.)* Claimed that `ClauseRegister`, `ActivityLog` and `RevenueForecast` define no `mount()` so delivery throws. **Already fixed by `a813a0ac`** — `DeliverSavedReportService` guards with `method_exists($instance, 'mount')`, and `EveryDeliverableReportCanActuallyRenderTest` is green. The second half (the command stamps `last_delivered_on` before `deliver()`) is a deliberate CLAIM-FIRST lock, documented in place: the stamp is what makes a concurrent run a no-op, and a same-day retry would fail identically. The reviewer read a tree that predated the fix | `Services/Reports/DeliverSavedReportService:112` |
| **SW-208** | ❌ **refuted** | medium | S | *(found by the review of SW-176.)* `IncomeStatement::$spread` is kept as "shape" but `spreadGroups()` returns null for `ytd` unless `hasSelectedMonth()` — which reads `$period`. A month-end pack saved with `spread=ytd` + `comparison=prior_period` loses its whole column structure when the period moves to a whole-year shape. A kept parameter that is a function of a moved one | `Filament/Admin/Pages/IncomeStatement:212` | **Measured against HEAD by the 2026-09-03 verification sweep** (69 agents, one per group of related rows; the adversarial challenge phase did NOT run — it died on the session limit — so this verdict is single-source). MEASURED, three ways. (1) The row's failure mode cannot occur at HEAD. `ReportPeriod::advanceLedgerPeriod()` (app/Support/ReportPeriod.php:110-133) rewrites a saved period IN ITS OWN SHAPE; probe: advance(IncomeStatement, ['year'=>2026,'period'=>'2026-08','spread'=>'ytd','comparison'=>'prior_period'], 2026-09-03) returned period='2026-08' — still month-shaped, so spreadGroups() yields the 2 columns ['period','ytd']. Null becomes null (whole year), a month becomes a month; the period NEVER widens from month to year. (2) It was not true on the sweep day either: `git show a39a4c78:app/Services/Reports/DeliverSavedReportService.php` line 112 is `ReportParameters::apply($instance, $saved->paramet |
| **SW-209** | ✅ **fixed** `d1db794f` + review | medium | S | *(found by the review of SW-176.)* `ScopesLedgerReport::hydrateLedgerScopeFromQuery()` validates `period` against `/^\d{4}-\d{2}$/`, so `WithholdingTaxReturn`'s quarter-shaped `2026-Q1` is rejected and a saved Form 41 view reopens on the full YEAR in the browser — the one surface the delivery fix deliberately leaves untouched | `Filament/Admin/Pages/Concerns/ScopesLedgerReport:74` |
| **SW-201** | ✅ **fixed** `71227984` | medium | S | Deleting the last rate rung of an ACTIVE tax code silently re-rates billing to the 14% VAT floor | `Filament/Admin/Resources/TaxCodes/RelationManagers/RatesRelationManager:111` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** Deleting the last rate rung of a live tax code leaves it active in every picker with no rate, so every supply under it silently re-rates to the 14% VAT floor from the next document on. CONFIRMED at HEAD (e3154f27). `TaxCode::assertCanBeActivated()` (app/Models/TaxCode.php:162) refuses to switch a rate-less standard code ON — *"otherwise it would appear in the pickers and bill nothing"* — and nothing asks the same question in the other direction: `RatesRelationManager:110` is a plain `DeleteAction` on every rung, `TaxRate` is `#[DeletionAllowed]`, and `TaxRate::booted()` only flushes the memo. With the ladder empty, `TaxCode::rateFromLadder([])` returns null → `rateOn()` null → `Vat::rateForType()` (app/Support/Vat.php:146) falls past its ow |
| **SW-202** | ✅ **verified fixed** | low | — | Clearing the Tax depreciation year renders a full schedule of zeros for year 0 | `Filament/Admin/Pages/TaxDepreciation:84` | **Measured against HEAD by the 2026-09-03 verification sweep** (69 agents, one per group of related rows; the adversarial challenge phase did NOT run — it died on the session limit — so this verdict is single-source). True of the tree the sweep read, closed hours later. `git show aa624ab1^:app/Filament/Admin/Pages/TaxDepreciation.php` has `public int $year;` and a clearable Select bound under `statePath('data')` with `->afterStateUpdated(fn ($state) => $this->year = (int) $state)` — and `(int) null === 0`, which is why this page gave a year-0 schedule where SW-037's other twelve gave a 500. `git log -S 'selectablePlaceholder(false)' -- …/TaxDepreciation.php` → `aa624ab1`, 2026-09-01 17:32:27, whose own row (SW-037, already marked ✅) names tax depreciation. At HEAD, `TaxDepreciation.php:93` carries `->selectablePlaceholder(false)`, and Filament then renders no blank option and tells the JS (`vendor/filamen |
| **SW-203** | ✅ **fixed** `71227984` | low | XS | Insurable-wage ceiling rule gte:insurable_wage_floor refuses a rung with a ceiling and a blank floor, though a null floor is legal ('no bound') | `Filament/Admin/Resources/PayrollRates/Schemas/PayrollRateForm:50` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** The insurable-wage ceiling could not be saved at all — the rule compared it against a root-level attribute that does not exist, so every ceiling was refused, including the statutory band the system itself ships. CONFIRMED at HEAD (e3154f27), and LARGER than the row states. Two probes against the real framework. (a) `TextInput::make('insurable_wage_ceiling')->numeric()->minValue(0)->rules(['gte:insurable_wage_floor'])->getValidationRules()` returns `['nullable','numeric','min:0','gte:insurable_wage_floor']` — the string reaches the validator verbatim; Filament's own `->gte()` helper (which calls `resolveRelativeStatePath()`) is not being used. (b) A Filament resource form lives at the `data` state path, so the rule is keyed `data.insurable_w |
| **SW-204** | ✅ **fixed** `71227984` | low | XS | Withholding rate rungs are stored negative by convention, but the rung form clamps minValue(0) — a seeded WH rung cannot even be re-saved | `Filament/Admin/Resources/TaxCodes/RelationManagers/RatesRelationManager:46` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** The tax-rate ladder's form clamps every rung at zero, so all eight seeded withholding rungs — stored negative by the catalogue's own convention ("WH -1%") — cannot be saved through the one screen that exists to maintain them, and next year's decreed withholding rate cannot be entered at all. CONFIRMED at HEAD (83624504). (1) app/Filament/Admin/Resources/TaxCodes/RelationManagers/RatesRelationManager.php:46-47 carries `->minValue(0)->maxValue(100)` on the `rate` field, unconditionally — it is the ONLY form that writes `tax_rates`. (2) The convention it contradicts is deliberate and documented three times: database/seeders/TaxCodeSeeder.php:219 writes `'rate' => -1 * (float) $rate` for the withholding family; app/Support/WithholdingTax.php:35 |
| **SW-214** | ❌ **refuted** | medium | S | *(found by the review of SW-092.)* The payroll double-pay guard keys on `asset_id`, which is NULLABLE. A portfolio-level run keys `payroll:approve:none:2026-08` and its clash query is `where('asset_id', null)`, which matches nothing — so a portfolio run and a mall run for the same month lock different keys, see no clash, and can both pay the same employee. The guard must ask which EMPLOYEES a run covers, not which property it is filed against | `Services/PayrollService:64` | **Measured against HEAD by the 2026-09-03 verification sweep** (69 agents, one per group of related rows; the adversarial challenge phase did NOT run — it died on the session limit — so this verdict is single-source). TWO measurements, both against the claim. (1) The stated mechanism is false. app/Models/Payroll.php:272 is `->where('asset_id', $payroll->asset_id)`. I compiled that exact clash query with a null asset through the real Eloquent builder: `… and exists (select * from `payrolls` where … and `asset_id` is null and date(`period_month`) = ? …)`. Laravel converts a null value to `whereNull` (vendor/laravel/framework/src/Illuminate/Database/Query/Builder.php:989-991, `if (is_null($value)) return $this->whereNull(...)`). It does NOT 'match nothing' — two portfolio-level runs clash correctly. (2) The scenario needs a payroll with `asset_id = null`, and no door produces one. app/Filament/Admin/Resource |
| **SW-215** | ✅ **fixed** `ac295bd0` | high | M | *(found while writing the SW-199b fixture.)* `InvoiceItem::saved` → `Invoice::syncTotalsFromItems()` → `recomputeTotals()`, whose auto-status block overrode anything outside its manual-override list — and `draft` was NOT in it. So **writing a line onto a draft invoice promoted it to `issued`**, measured through the real create page: the operator picks Draft, the invoice is stored issued. An unissued document in front of the tenant, on the books and in the GL, with no way back (the form drops `draft` once the status moves). `InvoiceSettlement` had recorded it in writing as a hazard to route around | `Models/Invoice:994` |
| **SW-216** | ✅ **fixed** `06403e1c` | high | M | *(found by the review of SW-199b; the status half is fixed, this is the rest.)* The CAM billed estimate is **gross of every credit note**, and a PARTIAL credit moves no status at all — so nothing written as a status filter can see one, and `credit_note_items` carries **no charge-code column** to net by line. Not exotic: `CreditUnearnedBillingService::isTimeApportioned()` returns true for exactly a monthly, not-in-arrears `service_charge`, so every mid-period move-out and every mid-year resale credits these very lines. Quarterly 30,000 for Jul–Sep, tenant leaves 15 Aug, ~15,000 credited, status stays `issued` → the pool counts 30,000, and against a 40,000 share the true-up is 10,000 where 25,000 is owed. **The landlord under-recovers, silently, per tenant.** Netting needs a line-level link or a stated pro-rata rule — an accounting decision, not a one-liner | `Services/SyncCamPoolFromLedgerService:296` | **`credit_note_items.type` is the line-level link**, nullable and backfilled only where an invoice's lines are all ONE type (a mixed invoice cannot be apportioned without deciding something, so it stays null and is simply not netted — *not stated* rather than guessed). Written by `CreditNote::describeAs()`, by the CAM true-up credit and by the credit-note form's prefill; `SyncCamPoolFromLedgerService::creditedBack()` subtracts it from BOTH the pool total and each participant's share. **Three defects came out of the review, and two were the fix being worse than the bug:** the billed query excluded `credited` while the new credit query did not, so a fully credited 30,000 invoice came back at **−30,000** — signed decimal, stored silently, printed on the tenant's CAM statement, and the true-up billed the credit back; there was no floor, so several partial credits could drive the estimate negative from ordinary data; and the per-participant narrowing was a no-op in a one-lease fixture, so it could be deleted with every test green while in production every tenant would carry every other tenant's credits. `INVOICE_NOT_BILLED` is now ONE list read by both halves — a filter written twice is a filter that drifts, and this pair drifted inside a single change. Also: the move-out credit note now names the CHARGE on each line (splitting by charge alone gave three lines with one identical sentence). `ACamEstimateIsNetOfWhatWasCreditedBackTest` (9 cases, 5 mutations) |
| **SW-217** | ✅ **fixed** `1b774709` | high | M | The EG-27 unallocated notice counted a population its own statement did not show — the income statement and cash flow exclude year-end closing entries while the notice counted the null-asset closing entry (roughly DOUBLING it), the general ledger showed a portfolio-wide count beside ONE account's movements, and a balance sheet's cumulative read was worded *"This period holds…"* | `Services/Accounting/LedgerReportService:555` |
| **SW-218** | ✅ **fixed** `1b774709` | high | S | *(found by the review of SW-217 — the fix was worse than the bug.)* Fixing the screen and CSV and not the PDF left one income statement leaving the building quoting **134,300** while the screen said **84,300**. And narrowing the general-ledger count to one account left the aggregate at `SUM(jl.debit)`, so a normally-CREDITED account (bank, AP, VAT payable) read *"2 entries … totalling EGP 0.00"* — a warning naming zero money reads as "nothing is missing". `App\Support\UnallocatedNotice` is now the one wording for all four renderers | `Services/Accounting/LedgerReportPdfService:137` |
| **SW-219** | ✅ **fixed** `1b774709` | critical | XS | *(found by the review of SW-217.)* Two test-helper name collisions — `noticeOn` and `draftRun` (the latter shipped in `df45bffc`) — each a FATAL redeclaration that exits the whole suite **255 with zero bytes on both streams**. Invisible under `--parallel` (each worker loads only its own files) and invisible to a path-scoped run, which is how it was pushed. `TestHelperUniquenessConformanceTest` was red the whole time | `tests/Feature/Regression/APayrollRunCannotPayTheSameMonthTwiceTest.php` |
| **SW-220** | ✅ **fixed** `5e8374bb` | high | M | *(found by the review of SW-135.)* `UnitOwnership::scopeCovering()` is **point-in-time** (`covering(Dec 31)`) while the lease branch beside it is **overlap** — deliberately changed to overlap on 2026-08-17 for this exact reason, and the ownership side was not. On a mid-year resale the seller gets **no allocation at all**, while `areaForPeriod()` is not time-weighted for an ownership so the buyer is allocated the **whole year**. SW-135's fix removes the tell without removing the error: the buyer's over-bill drops from a whole year to the seller's half, which reads plausible where the old figure read obviously wrong. Related: `areaForPeriod()` also ignores `ownership_share_pct`, so a co-owned unit (which `DemoSeeder` seeds) counts its area once per co-owner and allocates each the full unit share against half the assessments | `Services/CamReconciliationService:563` |
| **SW-221** | ✅ **fixed** `c00e0143` | medium | S | *(found by the review of SW-135.)* `SyncCamPoolFromLedgerService::sync()` refuses only `reconciled|closed`, so on a `reconciling` pool with any non-pending allocation the write hits `CamExpensePool::booted()`'s basis freeze. Previously the estimate returned the same number on a re-sync → not dirty → no throw; after SW-135 the number moves for the first time on any pool with owners, so the first sync after deploy raises `admin.refusals.cam_basis_locked_after_billing`. A loud refusal rather than silent money, and arguably correct — but unannounced, and in no test | `Services/SyncCamPoolFromLedgerService:41` **Fixed 2026-09-05.** Confirmed at HEAD: `sync()` refused only `reconciled|closed` while the Sync button was live on any `draft|reconciling` derived pool, so one billed allocation met a refusal raised by the MODEL, worded for the edit FORM (*"The CAM recovery basis cannot change…"*), from a button that gave no sign it would not work. **And only sometimes** — `forceFill()` on a `decimal:2` cast leaves the model clean when the figure has not moved, so `$basisChanged` is false and nothing throws; SW-135 and SW-216 both MOVE that number, so the first sync after either shipped is the one that refuses. Reachable through the ordinary panel flow (generateAllocations → `reconciling`, billAllPending → `billed`; `markReconciled` is a separate act). The guard and the button now read one predicate, and the refusal names the act and the way out. **It does cost one legitimate use and the docblock now says so** — a re-check on a billed pool whose figures had not moved used to succeed and re-stamp `expense_synced_at`; that is the intermittence itself, and `needsSourcing()` tests only for a NULL stamp so a stale date raises no false flag. Review verified upstream Filament emits `disabled` only when the tooltip is blank, so the tooltip fires and the click is inert. `ACamPoolSaysWhyItCannotBeResourcedTest`, 5 cases, mutation-proved |
| **OPS-08** | ✅ **fixed** `c7f92e16` | high | M | **The suite was red on `main` and nobody could see it.** Eight failures in 7,480 — and the earlier count of seven was itself wrong, because it came from `tests/Feature/Regression` alone. Causes, none of them one defect: the ledger learning set drifted (SW-227); an explanatory **CSS comment** carrying Arabic prose shipped inside `<style>` in every PDF, so an English invoice contained Arabic (a Blade comment would not have); `RentRoll` asserted the literal `as at 01/09/2026`, true the day it was written; an isolation test set no Filament tenant, so a header action linking to `payments.create` threw `Missing required parameter [tenant]` while merely rendering; two fixtures wrote columns that do not exist (`journal_entries.description`, `custody.status`), which Eloquent drops SILENTLY — caught by `FixtureColumnsExistConformanceTest`, which the review of this batch reported as not existing on disk and which does (it is what found both, and it is registered in `gate-mutations.json`);  the PDC register grew a third filter and so earned saved views; the E2E manifest was stale; and two cases asserted contracts that had been deliberately reversed (a draft is now cancelled, a general owner request is now visible). Green: 7,480 tests, 0 red, 32,110 assertions. **With CI paused, the full suite is the only gate — and running only part of it is how the count was wrong twice** | `tests/Feature/Regression/LedgerLearningSeederAddsUpTest.php` |
| **SW-222** | ✅ **fixed** `a5cbb3f7` | high | S | *(found by the review of SW-131.)* `ReportPeriod::advanceLedgerPeriod()` advanced a `YYYY-Qn` period with Carbon's **calendar** quarter, while `WithholdingTaxReturn::periodOptions()` builds `Qn` by stepping three months from `fiscalYearStart()`. On an April year, 15 Aug 2026 produced `2026-Q2`, which that page renders as **Jul–Sep 2026 — the quarter still running**: a scheduled Form 41 delivered on a partial quarter, which is a filing position rather than a stale report. The monthly branch had the same defect one level down, naming `$month->year` where February 2027 belongs to FY2026 — a year whose picker does not offer that month, so the membership guard then discarded the period entirely | `Support/ReportPeriod:123` |
| **SW-223** | ✅ **closed — deliberate** `c00e0143` | low | XS | *(found by the review of SW-131.)* Deriving the period from `periodOptions()` narrows one pre-existing case on a NON-CALENDAR year: `?year=2026&period=2026-01` on an April year is discarded, where the old regex accepted it and rendered January 2026. It was already the pickers-disagree state `updatedYear()` exists to prevent, and it is unreachable on a calendar-year install — but it is a narrowing and it is not tested | `Filament/Admin/Pages/Concerns/ScopesLedgerReport:96` **Reviewed and closed as DELIBERATE, 2026-09-05 — no behaviour change.** The narrowing is real and it is correct: on an April fiscal year `?year=2028&period=2028-01` names FY2028 (Apr 2028 – Mar 2029), which does not contain January 2028, so the period is dropped and the report opens on the whole year. The `/^\d{4}-\d{2}$/` guard it replaced accepted it and rendered January 2028 under a year picker reading 2028 beside a period picker unable to label it — the pickers-disagree state `updatedYear()` exists to prevent. The YEAR is the senior control (moving it clears the month), so a self-contradicting link keeps the year; a link naming only a period still has its year searched for, one neighbouring year each way. Unreachable on a calendar-year install. Recorded in `ScopesLedgerReport`'s docblock and in modules/21, and the *"Still open"* note there that named it has been corrected |
| **SW-224** | ✅ **fixed** `741f879d` | low | XS | *(found by the review of SW-131.)* The period membership test runs in `mount()` only. `$period` is a public Livewire property, so a later update can still set `2026-13` and reach `selectedMonth()` — and Carbon does not throw on it: `createFromFormat('Y-m-d','2026-13-01')` overflows to **2027-01-01** and `2026-99` to 2034-03-01, so a malformed value renders a confident report for a different month. Pre-existing and unchanged by SW-131, which closed only the URL door | `Filament/Admin/Pages/Concerns/ScopesLedgerReport:229` **Fixed 2026-09-05 (`741f879d`).** Confirmed exactly as filed, and measured: **Carbon does not throw on a thirteenth month** — `2026-13-01` overflows to 2027-01-01, `2026-99` to 2034-03-01 — so a malformed period rendered a confident report for a DIFFERENT month under a picker showing its own *Full year* placeholder. Two teeth, each closing a door the other cannot: a strict SHAPE floor in `selectedMonth()` — deliberately not membership, because the withholding return legitimately carries `2026-Q1` there and `createFromFormat('Y-m-d','2026-Q1-01')` THROWS, so a membership test would 500 Form 41 (SW-131 through this door) — and `updatedPeriod()` membership for the Livewire-update door, which also clears a WELL-FORMED month from another fiscal year, the pickers-disagree state no shape test can see. `ReportParameters::apply()` writes the property directly on scheduled delivery and passes through neither hook, which is why the read-side floor exists. 4 cases, 3 mutations |
| **SW-225** | ✅ **fixed** `15035979` | high | M | *(found by the review of SW-215 — a hole its own fix opened.)* A draft ISSUED into a period that has since CLOSED committed with nothing in the ledger. `SealedPeriod::guard()` looked up the document's POSTED entry and returned when there was none — and a draft has none, because `InvoiceJournalizer` returns null for one — so it skipped exactly the document about to gain an entry; `GuardsPostingDate` is `isDirty($column)`-only by design and issuing moves no date. AR on the document, nothing in the GL, `billing:reconcile --deep` permanently red and `atriom:preflight` blocking the next deploy. **The first narrowing was too wide** — `status` dirty alone refused `issued → overdue`, which is what the late-fee flow does to an original whose month has closed — so the question is *"is this document BECOMING postable"*, asked of the ORIGINAL attributes via `LedgerPoster::postsAnything()`. And it must FAIL OPEN like its neighbour: without the try/catch a journalizer throwing on an incomplete chart turned into **66 red**, none of them about sealed periods | `Support/SealedPeriod:96` |
| **SW-226** | ✅ **fixed** `15035979` | high | S | *(found by the review of SW-215 — the other hole its fix opened.)* An abandoned draft had NO way out and suppressed its lease-month's billing for ever. `VoidInvoiceService` refused it with *"A draft invoice is deleted, not voided"* — naming a door that does not exist (`Invoice` is `#[NeverDeletable]`, the resource has no `DeleteAction`, the bulk one is hidden panel-wide) — while the form removes `cancelled` from its options; and `MonthlyBillingService`'s already-billed probe counted the draft, so the run reported `skipped: already_billed` for that lease-month for ever, indistinguishable from one billed correctly. A draft is now CANCELLED by the same action (nothing posted, so no reversal and no number burnt; the button relabels itself *Cancel draft*), and `draft` joins `cancelled` in the probe because a draft was never billed | `Services/VoidInvoiceService:27` |
| **SW-227** | ✅ **fixed** `c7f92e16` | medium | S | *(found by fixing OPS-08.)* The ledger learning set — five documents whose trial balance is seven lines totalling 15,700 — came to show **eight** lines and 18,700: the cleaning expense used the mall's own bank leaf while the receipt beside it fell to the generic `bank` POSTING ROLE, so one mall with one bank read as two accounts. The set is meant to be checkable in your head, and its docblock states figures nothing else was checking. The receipt now names the account, and the test asserts on the mall's own account **and that the role account carries nothing** — which is what that role is for: money nobody attributed | `database/seeders/LedgerLearningSeeder.php:193` |
| **SW-228** | ✅ **fixed** `fb476640` | high | M | *(found by fixing SW-227 — the seeder was the symptom.)* **A payment created outside the panel never gets a default bank account.** `RecordsBankAccount::bankAccountAssetOf()` resolves the property as `asset_id ?? bill?->asset_id ?? TenantScope::currentAssetId()`, and `payments` has **no `asset_id` column** — a receipt's books dimension is derived from the invoices it settles, which do not exist at `creating`. In the panel the operator's selected mall answers; nowhere else is there anybody to ask, so every such receipt falls to the generic `bank` role. **That is the whole online CARD channel, not an edge**: `PaymobPaymentInitiator:112` and `RecordDemoPaymentAction:56` both create the payment without a `bank_account_id`, and `PaymentMethod::requiresBankAccount('card')` falls to `code !== 'cash'` → **true**, so `card` is precisely the rail that is supposed to name one — the highest-volume inbound receipts on a live install, and the ones that land in a merchant settlement account. `PostDatedChequeService::clear()` is clean (it passes the cheque's own account). **And the gate built for this invariant is structurally blind to it**: `MoneyDocumentDoors` derives doors from Filament SCHEMAS, so an API, gateway or console creator is not a door it can see. That is the exact state the bank register exists to end: `MatchBankStatementLineService::candidatesFor()` finds candidates BY the chart account, so unattributed receipts and one named bank's postings are offered together. The concern documents the null case as "do nothing", which is right for a GUARD and leaves the highest-volume inbound channel undefaulted. The honest fix defaults after allocations exist, when exactly one property is involved | `Models/Concerns/RecordsBankAccount:153` |
| **SW-229** | ✅ **fixed** `71227984` | low | XS | *(found by the review of SW-220.)* Nothing refuses two ownership tenures of one unit whose dates OVERLAP by a day — the `saving` guard checks a row's own dates, not its siblings'. `TransferUnitOwnershipService` writes `ended_at = $on->subDay()` so the service never produces one, but the form and the importer can, and that day is then counted twice: measured, a seller ending 1 July beside a buyer starting 1 July gave the resold unit 50.068% against an identical full-year unit's 49.932% | `Models/UnitOwnership:saving` | **Designed by the patch fleet, adversarially reviewed, then applied and tested here (2026-09-05).** Nothing stops a unit being owned more than 100% on a day: two possession tenures whose dates overlap each bill the shared day in full and each contribute its whole area to the CAM pool, so the unit over-recovers and everyone else under-recovers. `app/Models/UnitOwnership.php:176-181` - the only `saving` guard compares a row's own `started_at` against its own `ended_at` and never looks at siblings; there is no unique index (`2026_08_15_100000_create_unit_ownerships_table.php:103` is a plain `index(['unit_id','started_at','ended_at'])`). Doors: `TransferUnitOwnershipService:93` writes `ended_at = $on->subDay()` so the service never produces one; `UnitOwnershipForm` offers free `started_at`/`ended_at` DatePickers with no such rule; there is NO |
| **SW-230** | ✅ **fixed** `bff5ee36` | medium | S | *(found by the review of SW-023.)* `WriteOffInvoiceService::reverse()` cannot refuse anything — no status check, no closed-period check — and the GL void it triggers is an `afterCommit` job whose handler catches `\Throwable` and only logs. So if the write-off's own period AND today's are both closed, `reversalPeriod()` throws inside the job, the row is soft-deleted anyway, the new void guard stops blocking, and the write-off's `Cr AR` stands with no row behind it. The sanctioned route re-creates SW-023 in that window. Also: `EditInvoice:396` catches a `DomainException` for *"the GL void lands in a CLOSED period"*, which that job can never raise — dead code for the case it names | `Services/WriteOffInvoiceService:158` **Fixed 2026-09-05.** Confirmed exactly as filed: `reverse()` had no status check and no period check, and the ledger void is dispatched as an `afterCommit` job whose handler catches `\Throwable` and only LOGS — so the failure is silent by construction. With both the entry's own period and today's closed, `reversalPeriod()` threw inside the job, the row was soft-deleted anyway, and the write-off's `Cr accounts_receivable` stood with no document behind it: **the unbacked-credit state SW-023 refuses through the void door, re-created through the SANCTIONED route** — worse, because the operator was told it worked. `JournalPostingService::openPeriodForReversalOf()` is `reversalPeriod()`'s own loop with the throw lifted out, read by `LedgerPoster::canVoidEntryFor()`, so the guard and the act cannot drift. It falls through to TODAY's period exactly as the act does — requiring the entry's own period would refuse the ordinary recovery of an old debt and push the operator into a worse workaround — and a source that never posted stays freely reversible, so a closed month cannot strand a row the ledger never knew about. A second reversal is refused too. Both refusals EN+AR. The row's second half is closed with it: `EditInvoice`'s catch for *"the GL void lands in a CLOSED period"* was dead code for the case it names, and can now fire. `AWriteOffReversalCannotStrandItsLedgerEntryTest`, 7 cases, 3 mutations including one that makes the guard STRICTER than the act |
| **SW-231** | ✅ **fixed** `c00e0143` | low | XS | *(found by the review of SW-023.)* `admin.refusals.invoice_void_has_write_off` carries no `:number`, unlike every other write-off refusal (`write_off_not_live`, `write_off_already_full`, …). `PercentageRentCalculationService::voidLocked()` and `CamReconciliationService::voidAllocation()` surface it as a toast from the sales-declaration screen, where the operator cannot tell which invoice is meant | `lang/en/admin/refusals.php` **Fixed 2026-09-05, and the row under-stated it.** `invoice_void_has_write_off` carried no `:number` while every sibling write-off refusal in the same file does — but the captured-cash PAIR beside it is the same defect and is the *more likely* refusal on exactly the screens the row named, so five call sites were reworded, not two. **The screens that raise it most are not the invoice's own**: a void cascades from `SalesDeclarationActions::voidLocked` and from the CAM allocations relation manager, and both callers' own docblocks name captured cash as the expected outcome — so an operator was told *"this invoice carries a write-off"* about a document they never picked and could not identify. The CAM door voids **up to two** invoice ids in one loop, so the toast could not even say which of the two refused. Nothing in the suite had ever read the toast: `PercentageRentImmediateBillingTest` drove the cascade end-to-end and asserted `toThrow(DomainException::class)` with no message argument. `$invoice->number` always exists (290/290 on the QA baseline). `invoice_void_eta_filed` deliberately untouched — module 16 is `Modules::FROZEN`. `AVoidRefusalNamesTheInvoiceItRefusedTest`, 7 cases, mutation-proved |
| **SW-233** | ✅ **fixed** `dfc71136` | high | M | *(found by the review of SW-031.)* `ReportService::openInvoicesAsOf()` uses the same hand-kept `issued|partially_paid|overdue` list, so a header-`disputed` invoice is invisible to **AR aging, the collections worklist and the CSV** — the mall's own arrears understated by the whole document. MF-07's shipped position is that a disputed figure sits BESIDE the aged one *because deducting it would understate what the mall is owed*, and this drops it entirely. Fix by reading `->acceptingSettlement()` there too, as the final account now does; the disputed figure beside it already comes from the item flag | `Services/Reports/ReportService:851` | **Far wider than filed.** That hand-kept list appears **23 times in `app/`**, and every copy omitted `disputed` — which `InvoiceSettlement::LIVE` has classified as owed since its first commit and which `InvoiceForm` still offers as one of the two statuses an operator may set. Measured: a tenant whose only open invoice was disputed read **0.00 outstanding and NOT delinquent**, invisible to AR aging, the collections worklist, the CSV, the tenant list, the lease summary and **their own mobile balance**. **Two questions, two scopes, one register**: `Invoice::stillOwed()` is the selection counterpart of `collectableBalance()` (what the mall is owed) and `chaseable()` of `chargeableBalance()` (what may be dunned or penalised). That split existed for the AMOUNT and had never been applied to the SELECTION, so the overdue scan and the dunning sweep skipped disputed only as a side effect of which three strings somebody copied. 15 occurrences routed. **The review caught three things.** Two files I reported as routed were never edited — my script hit an assertion and died before reaching them, and I read its partial output as done. **`paid` had to join `disputed` in `NOT_CHASEABLE`**: `InvoiceSettlement::LIVE['paid']` states in writing that an invoice can carry that status with a standing balance, and `Invoice::saving` recomputes `balance` on a header-dirty write while leaving the status alone — measured through that path, a late fee applied to an invoice the operator sees as **PAID**. And `stillOwed()` added a correlated write-off subquery the call sites already had, +19% on the aging shape. Also routed `BooksReconciliationService`'s `outstandingAR` control total, whose own comment claims it matches `outstandingBalance()`; `LeaseSummary` now sums COLLECTABLE rather than raw balance. `ADisputedInvoiceIsStillOwedTest` (10 cases, 5 mutations) |
| **SW-234** | ✅ **fixed** `5987a29d` | medium | S | *(found by the review of SW-233.)* Five surfaces answer *"which invoices are overdue"* with no status list at all — `balance > 0 AND due_date < now` on the **raw** balance, so they include `draft`, `disputed` and `written_off` and ignore write-offs entirely: `ActionRequired:104` (the dashboard's overdue count AND amount), `InvoiceResource:101` (the sidebar badge), `InvoicesTable:206` (the "Overdue only" filter), and the tenant/lease invoice relation managers. Measured on `mall_management_qa` all three definitions currently agree (11 invoices / 342,977.75) because only `overdue` and `partially_paid` rows are past due — so the divergence is **latent, not present**, and the first partially-written-off or disputed arrear makes the dashboard disagree with the report it links to. Route them through `Invoice::stillOwed()` | `Filament/Admin/Widgets/ActionRequired:104` | All five routed. **And the report they deep-link into had the same defect one level down**: `arCollectionsByTenant()` selected what is still OWED and then summed the RAW balance into both the total and the aging buckets — so the selection had been fixed at the chokepoint and the amount had not, and the worklist chased money the operator had forgiven. Both now sum `collectableBalance()`; `LeaseSummary` too |
| **SW-235** | ✅ **fixed** `5987a29d` | low | S | *(found by the review of SW-233.)* `Invoice::collectableBalanceSql($table)`'s docblock claims the `$table` parameter defends against an aliased self-relation — *"the scope below passes the query's own table, so the common path cannot get it wrong"* — but `$query->getQuery()->from` returns the whole aliased expression, so `whereHas('lateFeeInvoice', fn ($q) => $q->stillOwed())` compiles `invoices as laravel_reserved_0.balance - COALESCE(...)`: a syntax error on **both** drivers. Pre-existing in `scopeWhereCollectable()`; making `stillOwed()` the idiomatic scope invites it into exactly such queries | `Models/Invoice:487` | `Invoice::qualifierFor()` takes the ALIAS off the FROM expression rather than passing it whole. The test EXECUTES the self-relation query rather than compiling it, because the old form produced valid-looking PHP and SQL no driver will parse |
| **SW-236** | open | high | M | *(SW-134 piece b.)* A posting-role re-point leaves CLOSED-period entries on the old account with no way to clear the resulting drift, so `billing:reconcile --deep` — and therefore `atriom:preflight` — is red permanently with no operator action available. Needs the sibling of `posting_month_overrides`: either an account override pinned per entry, or an explicit accept-the-current-entry flag that `matches()` honours. **Blocked on SW-237's policy decision** | `Services/Accounting/LedgerPoster:524` |
| **SW-237** | open | high | — | *(SW-134 piece c — an ACCOUNTING DECISION, not a code choice.)* Should re-pointing a posting role restate history at all? Yardi's answer is that a GL mapping change is **prospective**: past entries keep the account they were posted to and only future documents use the new one. Atriom's ledger is DERIVED, so it does the opposite by construction — every open-period document silently re-derives. Deciding prospective would mean freezing the resolved account onto the journal line (or dating the mapping), which is a real design change and touches all 24 journalizers. **Ask the accountant before building either half** | `Services/Accounting/AccountResolver:20` |
| **SW-238** | ✅ **fixed** `b3ce3c2a` | medium | S | *(found by the review of SW-210.)* `CreditNoteJournalizer:71` debits `sales_returns` — contra-REVENUE — for a `security_deposit` line, which is the identical mistake SW-210 fixed on the write-off side and is still live. A credited deposit invoice therefore reds `deposits_tie_out` with no write-off anywhere near it: measured, a fully credited 100,000 deposit leaves the GL saying 100,000 held where the truth is 0. The fix is the twin of SW-210's — reverse what the line credited, resolved from the charge code, frozen on the credit-note line rather than derived — and it carries the same PROSPECTIVE requirement, or it re-posts history into closed periods | `Services/Accounting/Journalizers/CreditNoteJournalizer:71` **Fixed 2026-09-05 (`b3ce3c2a`).** The journalizer splits its debit by the note's FROZEN `deposit_amount` — maintained from its own lines via `saveQuietly()` (invisible to the immutability hook that now REFUSES the column past draft), so the figure follows the lines while they are written and can never drift a posted entry. **PROSPECTIVE**, because SW-216's backfill typed HISTORICAL lines and keying on the type would have restated closed periods — SW-236's drift, the hazard SW-210 was reworked for; legacy rows carry 0.00 and post unchanged. `DepositBilling::depositPostingRole()` is the ONE resolution shared with the write-off door. An untyped line is NOT STATED and takes the `sales_returns` floor. **The SW-239 registration proved itself mid-fix**: it exposed TEN test files writing prose into the Select-backed reason column — green over dead fixtures — all repaired. 7 cases, 3 mutations |
| **SW-239** | ✅ **fixed** `74769c3c` | low | XS | *(found by the review of SW-210.)* `invoice_write_offs.reason` has no `ValueSets` entry, so the column accepts anything — a test wrote `bad_debt`, which is not one of `InvoiceWriteOff::REASONS`, and it saved. That is the unenforced-column class EG-37 exists to close, and `ValueSetCoverageConformanceTest` does not catch it because `reason` is not one of the classification SUFFIXES it sweeps. Two parts: register this column, and decide whether `reason` joins the suffix list — there are other `*reason` columns and a sweep that adds them all needs each one classified or exempted | `Support/ValueSets.php` **Fixed 2026-09-05 (`74769c3c`).** `invoice_write_offs.reason` registered off `InvoiceWriteOff::REASONS` — the third declaration form, so the model and the registry cannot drift — and the registration immediately caught its own SECOND violator: the SW-230 test fixture, still writing `bad_debt`. The coverage gate then demanded the SHAPE decision be finished rather than left implied: `reason` stays OUT of the suffix sweep, recorded with its reason — eleven of the schema's twelve `reason` columns are the operator's OWN WORDS (`waive_reason`, `void_reason`, `disputed_reason`, `lease_events.reason` where the words deliberately WIN over the key), and a suffix rule would demand a value set for prose. The two closed sets, this and `credit_notes.reason`, are registered by name |
| **OPS-10** | ✅ **fixed** `33c269a6` | medium | XS | **The QA baseline was stale, so `composer qa` AND `composer test:mysql` were both broken** — `docs/qa/scripts/baseline.sql` is a DUMP, not a migrated database, and it predated `payment_methods.requires_bank_account` (2026-09-02). Every race worker and every MySQL-tier test refused with *Unknown column 'requires_bank_account'*, which reads exactly like a guard doing its job rather than like a stale fixture. Rebuilt with `composer qa:baseline`. **`race.sh` now takes `RACE_DB` from the environment** (default unchanged) so the next stale baseline can be worked around in one command instead of debugging a phantom refusal | `docs/qa/scripts/baseline.sql` |
| **OPS-11** | ✅ **fixed** `33c269a6` | low | XS | `LoggedValuesResolveConformanceTest` red on `main`: `marketing_post.audience` is a logged code column with no vocabulary, so an Arabic audit diff prints the raw `both`. Registered against `admin.marketing_posts.audiences` — **the group the post's own FORM renders from**, not one chosen because its keys overlap, which is how `expense.category` once got the retail list | `Support/ActivityVocabulary:180` |
| **OPS-09** | ✅ **fixed** `06403e1c` | medium | XS | **Twenty-eight tests red on `main`**, all one shape: a fixture writing free text into `credit_notes.reason`, which is a registered `ValueSets` classification, so the wildcard saving listener refuses it. `PortalCreditNotesTest`, `VatReturnCreditNotesTest`, `MonthlyCloseCreditNotesStayInTheirPropertyTest`, `AVoidedCreditNoteIsNotOnTheVatReturnTest`, `DerivedMoneyConformanceTest`, `DerivedMoneyColumnsNotClientWritableTest` — plus `CreditNoteCrossPropertyGuardHoldsForOwnersTest`, fixed the day before. The prose moved to `notes`, where it belongs | `tests/Feature/Portal/PortalCreditNotesTest.php` | **The first pass reported sixteen and swept six files; the review measured twelve more in six others** — `StatementExplainsEverySettlementTest`, `CutoverAndChargeTermsSurviveTest`, `UntestedModelsTest`, `AdversarialSweepFindingsTest`, `DocumentLinesLoadOnEditTest`, `PageStateRefreshesAfterActionTest`. Enumerating a set like this from the files you happened to open certifies the coverage you happen to have. One of them was worth more than the count: `StatementExplainsEverySettlement` asserted the tenant's statement printed the free-text reason, and it had been passing on the TAIL of a raw lang key (`admin.enums.credit_note_reason.Unearned billing on termination`) since the day it was written |
| **SW-232** | ❌ **refuted** | high | M | *(found while testing SW-097.)* **An ordinary model update writes an activity row whose `properties` come back EMPTY** — probed on `Employee` and on `Tenant`, so it is not one model. `ActivityLogging::for()` composes `logFillable()` + `logOnlyDirty()` + `dontLogEmptyChanges()`, and a row IS written (so spatie decided the diff was non-empty), yet `json_encode($activity->properties)` is `[]`. If that holds on a real install, the audit trail records THAT something changed and never WHAT — which is most of what the 2026-08-24 denylist flip claims to have bought (1,034 audited columns), and `ActivityVocabulary` renders a diff that would then always be blank. The gates over the trail check the vocabulary and the coverage registry, not that a written row carries values — the "gate checks a weaker property" shape again. Probe it against MySQL before concluding: this was measured on sqlite `:memory:` | `Support/ActivityLogging:253` | **Measured on MySQL 8.0.33, and the finding is WRONG — it read the wrong column.** The spatie version we ship writes a model diff through `ActivityLogger::withChanges()`, which sets **`attribute_changes`**, not `properties`; `properties` is legitimately `[]` for a model-event row and carries only what a hand-written `activity()` call passes. Probed end to end on a real `Charge` rename: `attribute_changes` = `{"old":{"name":"Base Rent"},"attributes":{"name":"Base Rent PROBE4"}}`, and `ActivityLogChangeRenderer` renders *Name: Base Rent → Base Rent PROBE5*. Both readers already prefer `attribute_changes` and fall back to `properties`, and the move is documented in three separate places in `app/` — `ActivityLogChangeRenderer`'s docblock names both shapes explicitly. The tell I ignored the first time: my own `whereIn('properties', ['[]','{}'])` count answered **0 empty rows** while the raw dump printed `[]`, because a JSON column does not compare against a string literal. **Two measurements disagreeing is the finding** — the one that agreed with my hypothesis was the wrong one. Same shape as the −14,000 GL figure earlier in this sweep, summed on `posted` alone |

<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Feature flags
    |--------------------------------------------------------------------------
    |
    | Toggles for optional product surfaces. Flip the matching env var and
    | (in production) re-run `php artisan config:cache`.
    |
    | (The `owner_portal` flag was removed with the /owner panel on 2026-07-27 —
    | owners are now admin-panel RBAC users, not a separate portal.)
    |
    */

];

*.log
.DS_Store
.env
.env.backup
.env.production
.env.cloud
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
/coverage
/storage/coverage
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db

# Brand-assets zip (regeneratable via brand-assets/render.mjs)
/atriom-brand-assets.zip

# Playwright test artifacts (regenerated on every globalSetup login)
/storage/playwright-state/*.json
/storage/playwright-report/
/storage/playwright-artifacts/
/tests/.playwright-artifacts-*/

# Stray SQLite DB artifacts
/mall_management
*.sqlite
!database/database.sqlite
/storage/certs
# Backup archives: a full database dump (tenant tax cards, signed leases, every
# money record). Committing one would put all of it in the repo history forever.
/storage/backups


ca.pem
# Ephemeral agent worktrees — a nested git repo, never part of this one.
.claude/worktrees/

# Generated QA snapshot — rebuild with `composer qa:baseline`, never committed
docs/qa/scripts/baseline.sql

# Race-harness scratch output (docs/qa/scripts/race.sh) — per-run artefacts, never committed
docs/qa/scripts/race_*.out
docs/qa/scripts/race_ids.json
